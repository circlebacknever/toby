0 08 95 ci
0 73 0 08 95 ci
0 rows updated another writer won reload and decide
01 title paper sparse mark giant topic
02 cover index paper dense toc grid
03 13 content paper or ink dense reference
1 move it onto the type
12 more protected helpers
14 fin ink sparse one closing claim
150 ms between 0 4 and 0 6 lower bound of 12
16 9 1920 1080 px native
1px hairline ring border hairline c8c3b2
1px stroke monochromatic currentcolor or text primary
2 masking at low level
20 more optional fields
2d a dark field additive blending so overlap glows and a faint full frame wash each frame so trails fade
3 cards side by side each with eyebrow section heading diagram svg body formula block
3 column dense grid of definition blocks
3 discriminated union with assertnever
30 line block of useeffect machinery duplicated everywhere
311 thank you for reporting the raccoon
312 ms is correct
3d build characters and props from non uniformly scaled low poly spheres
3d effects of any kind
4 3 grid of cards or 3 4 pick to fit slide count
4 column grid of cards each card naming a section
4 column stat grid with cycling accent stripes
4 interface with implementations
42 3 ms n 120
4mb images for 40px avatars
4mb version when you need a 40px thumbnail
4xx except 408 429
500 items two passes filter
500 orders means 500 sequential database round trips
500 string lowercases keystrokes keystrokes
500 user ids 500 round trips to the database
95 of cache get calls return null ttl expired between visits
a 1 second ttl is a bug magnet
a 4mb jpeg expands to tens of
a 5x improvement in the wrong
a bare display only falls back to a stylesheet display none
a base method s behavior is overridable from any subclass and
a batch import endpoint that grew
a behavior you inferred or a default you picked is a design decision and design decisions have other homes
a behavioral outcome can be pinned by example one named scenario or by property an invariant that holds over a range of generated inputs
a blank line followed by a one line comment makes phases scannable
a blow up is a free win and any search will take it before it ever learns the real thing
a boundary carrying no such question gets the same three lines and keeps moving catalogued as stop inflation in references checks md
a boundary with no open question gets three lines and keeps moving
a bounded pool with timeouts configure the pool to fail fast when
a breaker prevents nothing
a bug fix without a regression test is incomplete work and the bug can return without notice
a bug fix writes no entry either
a bug in the lock release path takes out the cache for thirty seconds for
a bug or a security finding needs no routing because the fix is the finding
a busy engineer should read it once and trust it
a button that checks permissions canrefund
a bw emph 3px coloured rule beneath the intro to separate it from the toc
a cache earns its place by measurement
a cache is performance complexity
a cache layer brings real and recurring costs
a cache queue batcher or circuit breaker added because of an anticipated problem nobody has observed
a cache that just works
a cache uses a hash table but hash tables serve unrelated callers so they stay apart
a callback used only as a jsx dom event prop onclick doesn t
a caller doing the ordinary thing reads only the first two or three parameters
a caller facing parameter is the kind this skill tells you to minimize meaning a function argument or a component prop
a caller holding the base type meets the gap with no warning
a cancel request from the account holder on an open order returns the order with status cancelled
a candidate passes when its complete contract satisfies all of
a car a head and a tail are the same primitive which is what makes them read as a family
a card with position relative an svg illustration inside annotation pins numbered 1 n overlaid and a side legend mapping numbers to terms
a catch block that has never caught anything is a comment with a runtime cost
a change in performance class linear to quadratic sync to async is a behavior change so leave it
a change ships one of two ways a test covers the touched path and passes or the change is mechanical and you show why the edge can t fire
a change that looked tactical and turned out to touch permissions was strategic the whole time
a change with no measurable effect gets reverted unless it also simplified the design
a changed line chains three or more navigations through different objects accessors to reach a final value or call
a changed method s body is mostly a chain of getters and fields pulled from one other object with little use of its own class s state and that other object could own the work
a changed or new function branches on a bool or enum parameter near the top of its body into substantially different code paths and the parameter s only job is choosing which path runs
a changed or new signature has enough parameters that a caller has to check the declaration to get the order right or the same two or three parameters appear together more than once in the diff
a changelog or version history
a check is worth a sharp adult s time only when it forces them to generate an answer from their own model
a check you could have run and skipped leaves the criterion unmet
a circuit breaker is a stateful wrapper around a downstream call that
a claim about how the code behaves carries a path line or enters the criteria list as an assumption an explorer s or sub agent s summary included
a claim with no evidence yet is unevaluable
a class s public methods
a clever one liner nested ternary or dense chain that packs several branches or side effects into one expression and makes debugging worse
a clich means that step got skipped
a client wrapper that grew organically
a closed vocabulary also fights the rule that says quote the exact surface
a codebase with if process env node env production in ten modules has spread one decision across ten files
a column rename gets touched in
a comment pitched at the code s own level just restates it and rots in place
a comment that narrates what the code plainly says a name that records history while hiding purpose
a common attempt at tidy separation
a common drift in growing rn codebases
a common growth pattern
a common growth pattern
a common growth pattern
a common mistake imported from java c into go
a common mistake on a phone
a common pre encapsulation interface
a common reflex when adding error boundaries to a react app
a common slow render the team blames on react then tries to memo their
a comparison table a rejected alternative callout and a chose a over b because c caption all name a real option so they ship
a competent caller could use it correctly from the comment alone including the common error cases
a component reaches the live value through a
a component s props a hook s return type a repository s query methods a cache s get or load contract a message contract across a process boundary
a conditional that has not changed in a year is not this smell
a conditional that has not gained an arm across repo history is fine
a consequence sentence is bounded by the path the reviewer traced and a severity label by information they never had
a consumer that needs one method gets an interface with one method
a control behind a gesture the player has to stop and open is a paused game control and the game is not paused
a copy of the explanation at each site drifts out of sync and gives no signal that it has
a correct result reached an odd way is a gap the phrasing hid
a criteria line for a copy change is the ceremony this file exists to avoid
a criterion counts as met on two runs of one named command quoted under the criterion s own wording the run against the code as it stood before the change or with the new path disabled then the run after
a criterion with nothing in the diff serving it is the finding quoted in its own pre code wording
a cross module reason duplicated into code at each site duplication
a custom hook composable rune for that effect
a data hook catches every fetch error and returns empty data
a deadlock or serialization
a decision process branching lines or y fork
a decision spanning modules stated once and pointed to
a deck on dns doesn t need to be reframed as mission ops but a placeholder dashboard with no real subject does benefit from it
a decorator that adds little is a shallow pass through in disguise
a deep barcode module owns the protocol
a deep ordersrepository owns the data access knowledge
a default lowers the burden and keeps the coupling because the caller still reads the default to know the behavior
a definition plus its one use is not a pattern
a design or coverage finding names the skill that owns the fix so the next agent starts where the evidence ends toby swd interfaces for a contract toby swd modules for placement toby swd complexity for an error path or a cache toby swd testing for coverage toby swd clarity for a name or a comment toby swd strategy for a design that got worse
a design smell from references smells md
a design smell holds three different lines the entry name from references smells md the file line and the code there that meets the entry s fires when criterion
a design smell matching a named entry in references smells md only when the entry s criteria can be quoted against the code
a developer should guess what it holds or does and what it does not
a diff with nothing wrong gets one line no findings plus residual risk if any named specifically with why it stayed unverified
a direct wrapper around a native module
a discriminated union expresses the
a dispatch that cannot cross the street in a job s time is a dead button
a factory that builds one type
a feed repository owns the contract
a few characters do not
a file system module uses block for both a physical disk block and a logical
a filled record its preamble its bindings and a retired entry are in examples md
a find query is better than thirty findbyxandyandz only if find is also easy to call for the common case
a finding clears the bar and goes in plain or it s cut
a finding is a fact and its consequence stated flat
a first sentence that only frames the answer buries it
a fixture or seeded sample
a flagged slice meets the observation bar with the flag on
a flat array of successes can express
a four tab app passes the current user through navigation
a fresh synonym on second mention reads as a second thing
a freshly missed item comes back soon after two or three others and a solid one comes back later after six to ten
a full bitmap to draw a 40 40 thumbnail
a full restructuring proposal is reserved for modules that are exported have several callers cross a service boundary or are costly to change
a function renders user display names
a function routes a notification to a channel
a function s signature
a function the consumer calls
a game that never pauses cannot park its controls in a menu
a game tuned on a mouse can be unwinnable on a thumb
a general purpose interface compresses the cluster
a geometry mark in the corner at decorative scale
a getamount would mean only the bytes of the
a go server to server client
a good module is deep a simple interface over substantial functionality the interface is the cost the module imposes on the system and the implementation is the benefit
a good question earns a good answer and that s the only acknowledgment it needs
a green type check a lint pass or an existing suite that never enters the changed lines proves the repo still builds
a growing nest app threads request handling through a stack of middlewares
a hairline geometric primitive
a handler that mirrors it
a hard external deadline will not survive the extra time and the cost is understood and accepted
a heading holding the behavior in one sentence plus who asked quoted and when
a heading with a hit proves the sentence was pasted into a description
a headless combobox state machine
a helper that also logs is not equivalent to code that didn t
a high method count by itself proves nothing
a high traffic product page caches an expensive featuredproducts query for
a hook returns a loosely shaped object
a hook s return type a component s prop contract and a store
a hunch wrapped in might possibly is still noise
a known expensive trigger if it crosses the network so debounce the fetch and
a kv block at the bottom for scope units convention out of scope
a labeled block in the nearest file when a separate surface would add churn
a language without implementation inheritance reads this as the general two way coupling caution
a large or heavily
a large share of production failures come from bugs in barely exercised error paths
a lateral rewrite that only swaps one style for another is churn
a learner who met it as the guard clause reads the early return as a second thing and spends a turn reconciling them
a learner who only watches forgets the explanation
a lesson walkthrough or explanation in conversation stays in chat because toby explain and toby learning own that and they answer in prose
a light conversational touch is allowed
a lighter touch goes in a heading or a name never inside a claim the reader has to act on
a line a second is noise
a line after is toby s reply
a line checkable only by reading the diff is the diff with a checkbox on it so rewrite it or cut it
a line every generation when nothing changed is the feed flashing
a line that names what a number just did stays
a line you could delete without losing what happened goes
a list row is split into rowcontainer rowinner rowtext
a list the player scrolls is built once and mutated in place never re rendered under a moving thumb and ordered by a stable base cost so rows do not reshuffle as scaled prices change
a list view re fetches the full dataset on every keystroke of a filter box and
a literal you quoted while proving another finding still counts here
a local flag or toggle
a local script or temp command
a logical block number is eventually used where a physical one was
a long conditional where an early return or a named predicate shows intent
a long function that is one coherent deep abstraction stays whole
a long method that is one coherent deep abstraction with a simple signature is fine
a long method with a simple signature doing one coherent job is fine
a lot of many huge massive tons of plenty of quite a few
a market as a backdrop number is statistical
a market whose point is the crash is behavioral because the crash has to emerge from the agents
a match against a named entry there counts as a finding covered under what to catch
a match against any item calls for a redesign now while it is still cheap to change
a match against the file s fix here half is a countable win like any other in this section so fix it in the same pass
a match becomes a finding under that skill s proof rule for smells the entry name the file line and the code there that meets the entry s fires when criterion
a measurable predicted improvement and a baseline to compare against
a measurement over time subject timeline ticks as axis model envelope for the fit threshold rail for the alert band
a method signature shaped exactly to one caller produces a one call shaped hole the next caller cannot use without widening it
a method that takes the operation and computes the new state internally
a migration has three parts a schema change an idempotent data
a miss drops them back a rung on the next slice
a missed cleanup costs less than a churning diff or a silent behavior change
a missed minor issue costs less than a finding that turns out fake or trivial
a missing fact gets the fact then the question again
a missing key by n callers results in n independent loads callers
a missing or malformed variable stops boot with a message that names it
a module carries two categories of information that can t live in the code itself
a module has more callers than authors so the author takes the harder side
a module shaped to exactly one caller leaves a one call shaped hole the next caller has to widen
a module that reads process env for its own needs has taken a hidden dependency and leaked a decision that should live in one place
a module that swallows every network error so callers can t tell a message was lost hasn t reduced complexity
a muddy answer marks a muddy spot so reopen it
a mutating post can t reuse it as is
a mutation is safe to retry only when the request carries a key the
a named test or verification gap only when a real one exists
a narrowed one drops it from tasks that need it
a network failure or the 10s timeout rejects fetch it never returns a status
a network or graph subject matrix grid or triangulation mesh as backdrop coordinate stamp for nodes
a new component is added with onsaveclicked and submithandler
a new field added to the base is visible
a new field or parameter representing a recognizable domain concept is typed as a raw primitive and the same parsing or validation for that concept already appears at two or more sites
a new or changed class s public methods are mostly getters and setters for its own fields with no operation that names intent
a new or changed method s body is a single call to another method passing the same arguments through unchanged
a new or changed module s public surface parameters methods required setup takes about as much explaining as its internals would
a new or changed public interface whose comment leans on call order words first then after references internals runs past four sentences or leaves a caller unable to use it correctly from the comment alone
a new parameter or option has no current caller using anything but its default and grepping for callers turns up no concretely named near future use either
a new processor is a new gateway implementation plus a config entry
a new step is a component in steps plus a transition in machine ts
a new system to monitor redis or whatever is now in your
a non obvious block
a non trivial field
a null object that responds to every call the real one does lets the common path run with no if x is none
a number isfinite guard catches a physics blow up the eye misses
a numeric ceiling on function length comments are always failures a blanket duplication is evil and a tdd mandates test first rule are also absent deliberately
a one file edit following a pattern already in that file is in this skill s own skip clause so make the edit and say what you changed
a one line change note only when the diff s intent isn t plain from the diff
a one off detail gets a sentence
a open writechunk close
a parameter picking a value the operation acts on passes
a parameter picking which operation runs is the smell
a parameter that leaks a lower level mechanism into callers
a pass with nothing fired says so after reading the eight
a pattern from the bad old days still appearing in new code
a pattern in growing apps state starts in a component gets lifted to a
a pattern that adds complexity for negative benefit
a pattern that compounds for a year and a half
a pattern that gets there fast
a pattern that grew up because someone wanted control
a pattern that grows in react codebases
a pattern that grows in typescript codebases around knex prisma
a pattern that produces cache bugs
a pause an instant answer was free and free answers don t encode
a payments integration built as a throwaway spike is a payments integration built as a throwaway spike
a phone at 20fps is unplayable however good the layout looks
a place for implementation rationale which is agents md s job
a plain text flow when relationships span more than a sentence a diagram only when the structure is relational
a pnpm dev restart looks identical to a kill and any
a premise can have no target and be a pure toy so do not staple a bureaucracy onto one that refuses it
a probability distribution bell curve outline
a probationary bleeding heart and a functioning operator are different people
a profile screen with 50 avatars each a 4mb camera roll image
a prose file the repo already keeps for stated behavior a spec directory a decision record tree
a provider at the tree root is the injection mechanism
a public api s blast radius crosses company
a pure toy or sim has no outcome no verdict no win no opponent so skip the outcome laws
a question something you d ask the author only when the answer needs information the code can t give runtime config an external service product intent
a queue or pipeline stacked horizontal bars
a re testable prompt here is re defend the river thesis cold next week retrieval of a reasoning path carrying its known weak point forward
a reader scanning the component linearly never sees what triggers
a reader who acts on the first half of the sentence before finishing it does the right thing in the second version
a reader who already knows the material skims past with nothing lost so the sentence comes out
a reader who sees only the name should know what the system does in that scenario
a reading can be strong on one and weak on another
a readme md describing a defunct api is worse than no readme md
a readme md here only exists because the context contents are non obvious to developers onboarding to this part of the codebase
a readme md is for the human reading the code
a readme says what this module does why it exists and what a caller needs to know before calling it
a recap that recites the lesson back
a recited definition is where tutor voice tics hide best so run the draft past toby voice before it goes out
a recursive algorithm nested squares
a red flag list nothing runs is
a reference card on a protocol stack should be dense enough to reward sustained attention
a rejection is a redirect that costs you one directed candidate
a related frontend case a usermenuwrapper that renders usermenu and
a rename does not count
a renamed series reads as a second series
a repetition of interface comments
a reply that avoids every banned word and lands on nothing has failed in the way that matters most
a repo fact with no path line is your preference
a repo rule break only when the rule can be quoted at path line
a report that closes with what didn t land is written in the order that flatters it
a reporting service writes a row to events and then carefully invalidates
a repository sketch the team accepted without much thought
a request that can t get a connection in 2 seconds fails and the caller sees
a retry after a lost
a retry and timeout policy alone causes threads to pile up and the
a retry carrying the key
a retry decorator on each call site interface callers add retry
a retry interval almost never because the module sees the actual round trip
a retrying transport needs a retry interval
a review finding once the failure scenario states the fact flat
a review with no findings is the normal result
a right from memory answer pushes it far back hesitant medium missed brings it back soon and drops it a rung on the ladder
a rocket doesn t make a claim truer
a run is a story serialize the threads
a run that writes none names its reason from that list
a run where none fired says so and why
a run with nothing to report prints nothing
a sandbox keeps its sliders thumb reachable while it runs
a screen where nothing moves until the player acts reads as a form
a search endpoint that grew query parameters over time
a search hit is a pointer
a second independent call is the one expensive move here so gate it hard
a second name for a concept that has one is a question for the user
a security or authorization outcome auth denied a validation rejection a permission failure is never an error to define away or mask into success
a self consistent wrong answer is a wrong model so surface the belief and repair it
a semicolon a colon or an em dash joining two clauses means the relation went unstated
a sentence the product claims with no test defending it is the finding at the entry s heading
a separate denormalized table updated on writes queried directly
a service calls a downstream service
a service method that works
a service s endpoints
a shallow wrapper with a single caller that only renames another call and adds no type name or boundary value
a shared logging behavior or shared validation behavior is a helper object the class holds
a short entry point comment that stays short only because a query or command object absorbs the complexity is not a pass
a signal or transmission subject signal rings for emitter coordinate fan for sector coverage
a sim that only nudges a hidden probability earns one note every time not enough control
a single boundary around the entire app catches everything
a single claim centered with a small eyebrow
a single dense expression that computes one nameable result is one abstraction even when its internal steps have no good individual names
a single find method that covers every filter needs an interface that
a single red dot as the only color
a single red dot toby accent is allowed when the dot marks the consequence anchor periapsis threshold breach decision point
a skip is a request to keep digging
a slice flag ships with the slice that deletes it
a slice is the criteria that land observable together at one seam
a slice ships when all four hold
a slide presenting a single derivation or worked example can breathe
a slide should reward 30 45 seconds of attention
a slide with only a geometric mark and no accompanying claim has no purpose
a small answer is a valid result
a small filter fits
a smell costs future readers and future changes
a smooth explanation they nod at is gone by the next task
a specific candidate place plan or name is one point in a wider field
a specific complaint wish or constraint is one expression of a wider concern
a specific example is one member of a class
a split that creates two shallow units in place of one deep one is a loss however tidy it looks
a spring nest go codebase and the modular structure that beats it
a staging flag still set after its slice ships is a config option nobody decided to add
a stale authoritative file actively misleads
a step an operator can approve names four things the file it touches at path and whether it s created edited or deleted what changes there concretely enough to disagree with the criterion it serves by its wording and what proves it with the result that would mean it failed
a step handed over with no reason reads as a chore assignment
a step that ran differently from its approved wording with no note saying it moved is the finding
a storyline advances one step at a time on milestones fired once
a strategic move that touches large parts of the codebase or blows the
a structural change a moved or split module a changed public api a cross module decision that leaves an existing agents md or readme stale
a structural change shipped without updating an agents md it invalidated
a structural smell spotted but left alone gets one line naming the smell and the skill that owns it kept apart from what changed
a subtle case that hurts at scale
a subtle case where complexity arrives invisibly
a suite that locks in implementation details does worse than nothing
a surface other teams build on doesn t get broken without a
a swallowed or dropped error a caller needs to stay correct or recover
a swift biometricsbridge that exposes isavailable createkeys
a switch field type inside jsx is the smell
a switch or if chain is a design smell in two forms
a switch over a set that has been stable for a year is fine so leave it alone
a symptom of scattered data access
a tactical change usually has one
a task creates moves splits merges or places code or when ownership
a team adds barcode scanning
a team adopts memoize all functions and computed values to avoid
a team builds request handling as two classes
a team is about to render 200 items in a list
a team is building their own combobox
a teardown returned from the async function
a temporary comment dates from 2019
a ten item cart is eleven queries before anything renders
a test asserting on internals that would break under a behavior preserving refactor
a test passes fakes
a test suite exists for three jobs
a test that asserts behavior through the public interface
a test that fails because you renamed a helper while preserving behavior is detecting the diff and nothing else
a test whose result depends on what ran before it is already broken
a thin caching decorator around an underlying productsstore it composes
a third takes this lock and a second one in the opposite
a throwaway spike carrying acceptance criteria is paperwork
a thumb holding a stick owns the pixels under it so nothing critical goes there
a time series horizontal rail with a tick
a token and a page size cost a bit more
a token refresh requires re navigating with new params or every screen reads
a toy has no win to measure
a trajectory or path subject route trace for the path uncertainty fan for forecast coordinate stamp for waypoints
a transient error handled at the caller when an internal retry would erase it
a trivial diff gets at most one finding
a try catch guard or branch defending against a condition that can t occur the kind toby swd complexity s error ladder already defines out of existence removing it is one fewer branch or catch block
a try catch guard or validation for a condition already ruled out by the surrounding types contract or an earlier check only when that proof can be quoted from the code
a tutorial on internals
a typed key value cache
a typed object or discriminated union as the return type
a typed storage module owns these decisions
a user lookup capability
a userrepository six months in
a vague bar collapses into taste and this one does not
a vague this might be buggy is noise
a value or a unit goes in the interface comment toby swd clarity owns and a cross module or externally forced constraint goes in agents md under toby swd docs
a value under a type must behave the way every user of that type expects
a variable across a large span a field an argument or anything exported needs a precise name
a variable whose entire span of use fits in a few visible lines can be terse a loop index i
a visible state panel
a web server s per url handlers each call get param name which throws when a
a workaround that adds a special case a flag or a surprise dependency is debt you are choosing to take on with full awareness
a working reference for orbits in classical newtonian gravity
a wrapper that applies the categorization
a written file approved before the first edit
a wrong answer that contradicts something they said is a slip so give a light you mean x and move on
a year out the code says what it does and nothing says what it was for
a young codebase wires the orm into business logic
a zero downtime cutover needs more a dual write dual read window held
a zustand slice that grew organically
about 12 glyphs system wide
above or an unexpected slow query
abstract documentation survives code changes
accept in case the caller is composing
acceptable only when the meaning is visible at a glance
accepted keys are orgid uuid
accepted keys are part of the contract documented in prose silently
accessors as the public surface
accessors as the public surface a module whose surface is mostly per field get set is definitionally shallow
accessors as the public surface an interface built mostly from per field get set methods exposes the data layout with extra syntax costing a caller as much as the implementation it s supposed to hide
accessors as the public surface an interface that is mostly per field get set exposes the data layout with extra syntax so it costs a caller as much as the implementation would
accordion covers collapsible single open by default pass multi for multiple
accounts for the counter evidence does the reading handle the parts that cut against it
accrete the design from many small correct decisions instead
accretion becoming a grab bag
accumulates a grab bag of helpers because adding one is easier than designing a
accuracy turn or an error on the exact target elicit self repair something s off in the tense try again then have them state the rule
achieving atomicity is the
across components all owned inside one library that s deep by design
across processes you still get one call per process usually acceptable
across two deploys which is a larger interface this one doesn t express
act like a guest
act on a partial success
action handlers and permissions
actions reflects the operations the current user is allowed to perform
active boolean createdafter instant createdbefore instant
active filter is awkward because a primitive boolean can t be optional
active filters by active state where active excludes
active interpretation the validation rules the performance defenses
active is encoded in every caller
active press bg steps darker surface pressed ink pressed accent pressed border stays
adapter interface gateway storagedriver notifier one interface one implementation per provider selection from config at the composition root
add a cache only on measured improvement
add a debug panel so i can see state
add a note only when the user asked for rationale or when two rules conflict and the choice matters
add a variant to domainevent and the default arm stops compiling until the new case is written
add flags invites form defaulted off so the invite entry point stays dark
add item cartitem void dedupes by variant
add one line of fix direction when it isn t plain
add only when measured thread pile up is hurting users
add placeholders left in a production path at file line and what you found and left alone one line each handed to the skill that owns it
add the cascade or delete sessions in the same transaction
add who asked quoted and when
adding a dependency is out of scope so note it as a follow up
adding a field beside an identical one already in that file
adding a kind is one entry
adding a new derived listing means adding a method and an entry to
adding a new derived view adds an
adding a new step
adding a second scanning screen takes one line
adding an optional parameter with a default to an exported function is contact
adding any of them here would put this skill at odds with the skills that wrote the code in the first place
adding the next concern means another middleware that reads req user
adding the next filter changes every call site
adding the next state concern a wishlist
adding virtualization to a 100 item list because lists should be
addnote text string promise result void
addnote text string promise success boolean error error
addnotification n set s notifications s notifications n
adds a phone number column to the users table on upgrade drops it on
adds or changes a function method class component props hook return
addtocart item set s cart s cart item
adjacent columns for last and
adjacent layers should present different abstractions
adjective stack inflated verbs reader state assertion vague magnitude mission stating
admin tool that injects events every migration that backfills
admitted confusion is useful when labeled a caveat reading mechanism observed in three independent runs cause unknown beats a confident sounding theory
advanced or rarely needed config moves to a separate options object with defaults that work
affected sites get a one line pointer comment see event ordering in agents md
after a few items cover the answer and make the learner produce an earlier one from memory
after calibration of the resistance in the runway light connection
after do not call this from a request handler
after do not run the seed script since it truncates the table first
after how often refreshing a user session token fails
after if the build fails clear the cache
after load the fixtures
after remove the four screws holding the cover to the housing
after say what the check actually proves and what stays open
after sessionstore caches the token
after test cancel rejects shipped pass
after the behavior settles decide what durable tests should protect the contract and write them for the selected behavior
after the cache never invalidates because the ttl is set at construction
after the first edit it returned the same order before is a memory
after the handler validates the payload then initializes the worker pool
after the migration touches the session table and the audit log
after the retry loop was swallowing the timeout
after the task give a short account
after the task name anything that matters
after the user chooses a behavior delete the experiment or fold the chosen behavior into normal design flow
after this the cache is either plainly right slow database query
after two years no one knows which controllers depend on which
after writing code read it as a developer seeing it cold
agency over dice every outcome the player cares about traces to a choice they made and saw coming
agents md and readme md are out because toby swd docs puts one of each per module root and behavior spanning roots would split
agents md in a trivial or leaf folder over subdivision push it up
agents md serves the agent writing it
agents writing code here
aggregate if it must surface let it propagate several levels to one handler that addresses the general case the single handler at the top of a request loop
aggregate let the exception propagate to the single dispatch loop at the top
aggregated handling rung 3 at exactly the place that
aim at the desk and the policy and let the clerk reading the script sound trapped too
alert body color inherit opacity 0 88
alert close margin left auto background none border none cursor pointer color inherit opacity 0 6 font size 16px padding 0 4px
alert cons background var toby cons soft border color var toby cons tint border left color var toby accent color var toby cons text
alert info background var toby info soft border color var toby info tint border left color var toby info color var toby info text
alert stable background var toby stable soft border color var toby stable tint border left color var toby stable color var toby stable text
alert title font weight 600 margin bottom 4px color inherit
alert watch background var toby watch soft border color var toby watch tint border left color var toby watch color var toby watch text
all are interfaces and everything below applies to each
all compose one deep core that owns layout truncation theming internally
all four criteria arrive at the same seam an http request to the orders api
all four with the sibling feature read end to end
all lowercase for tokens toby paper
all lowercase for tokens toby paper toby ink
all methods return plain
all three are valid
allocation wrapper that aborts with a diagnostic
allow returns retryafter none and keep the bucket internal
allowed motion opacity fade 4 8px positional slide for menus toasts hairline ring on focus 90 chevron rotation on accordion open
allowed unicode for handoffs as metadata separator for uncertainty
almost never the right
almost never useful when the consumer isn t itself
almost no hit rate benefit
also drives section accents on long pages
also hides the design problems it pretends to solve
also loads the actual content the right move is often render the page
also simplified the component
also skip test first in four cases
altitude inclination period perigee apogee vis viva kepler semi major axis eccentricity 3 986 10 m s leo debris band above 900 km passive deorbit j2 perturbation
always above the trigger
always at design time know what is expensive develop a feel for the operations that cost orders of magnitude network round trips disk i o dynamic allocation cache misses
always set a timeout cascading failure is a slow death
always show it when it exists
always supply one the service will
always use font variant numeric tabular nums on numerals
ambient unit of work unitofwork run work
an action absent from actions is not permitted
an agent that follows the instruction literally has bypassed the judgment the repo author was relying on
an agent without an agents md is operating blind
an agents md is the single discoverable entry point for an agent working in this module
an all or nothing variant rejects the whole batch
an animation that plays the same regardless of force reads as fake so let a ball arc and a body settle
an antenna or wave orbit arc
an argument can be formally invalid which sits closer to wrong than to unsupported
an argument named selection for a method that works on any range misleads
an artifact that uses ink everywhere has made nothing authoritative
an assertion should fail when the behavior under test changes
an austere premise drops the spheres for boxes lines and the palette
an auth module exposes its persistence model directly
an autonomous agent is especially prone to pure tactical execution
an axis label a kpi value and a legend entry carry only the quantity the reader came for
an axis title a legend entry a kpi caption and a slide heading each get the three word cap on noun stacks
an entry above that heading is a live claim
an entry below it is a claim with an end date and a reason
an entry whose exception covers the code in front of you is not a finding and the exceptions live only in the owning catalog
an error eliminated or masked that callers needed
an error signaled for a condition the api could define away
an error the code deliberately makes impossible validated upstream unreachable by type or correctly lets crash
an error they can t yet fix supply it briefly and move on
an exception that covers it means the code is right and you say nothing
an experiment is a probe
an hoc package it as a hook composable rune function and call it
an idiom that appears nowhere else in the repo stays out of a cleanup pass because that s a style migration noted as a follow up
an idle or manager game wants legible rates and a catalog that reveals as the run grows
an in memory rate limiter several handlers consult
an in progress status but it doesn t expose the implementation strategy
an interface comment describes the abstraction behavior arguments return value side effects exceptions and caller preconditions
an interface that accepts input from outside the program is a trust boundary a request a message a deserialized payload
an interface that is the whole state layout makes the store
an invite that creates a row and mails nobody is half built behavior a user can reach
an operation cheap once becomes a budget breaker in a tight repeated loop and allocation that accumulates forces later reclamation
an operator approving a plan is approving the boundary as much as the work
an optimization with no measured improvement left in the code
an orbital subject orbit marker content scale orbital lattice as decorative backdrop
an undesigned degraded path is the barely tested error branch the hard rules warn about
an unread snapshot update is the same as deleting the test
an unrun list is a list nobody read
analytics consume the ledger
and 8 more error codes that may exist
and a reasonable surface of near future ones without becoming a god
and abort one transaction the strictest model paid for with more aborts to
and bakes it into their client
and for the design of a contract which toby swd interfaces owns
and handoff and routes method to the toby swd skills
and if so how much complexity have you bought
and internal hashing implementation
and is not empty throws cartinvalidexception otherwise
and is there a target an institution a ceremony a process and who is the underdog caught in it
and it never happens
and its value stable because a context that becomes a grab bag has the
and load latency is significant
and names the row that broke it
and one of the easiest places to leak knowledge into call sites
and per widget boundaries around known risky children a chart library
and presentation are different bodies of knowledge and a single component
and reads a torn value
and res text rides along for logging at the boundary
and servers cap the url around a few kb
and signals solid vue runes and stores svelte
and some are unused but no one will delete them
and stale arms are absent by design
and status open order by created at desc is
and the application must understand every intermediate schema
and the database has a configurable total cap
and the discriminator state loaded
and the formatter no longer knows what deleted means
and the legacy compatibility note
and the optional oncacheerror callback never as exceptions to
and the reasoning transfers
and the same list will need to be replicated in every batch importer every
and three call sites have grown their own copy of the same if chain to decide
and what its contract should be are separate questions handled elsewhere
and writes values derived from them each retry re applies a stale
android a value over 2mb throws on read the sqlite cursorwindow limit
android size limits it batches writes or drops oldest items if needed
angular ranges or sector coverage
angular sectors radiating from a point
animated slide transitions beyond a 100ms opacity fade
animated transitions beyond cut and 100ms fade
annotation labels in mono if they reference a value p95 312 ms sans otherwise
annotation pin numbered circle leader line framed label all in toby info
another holds the lock across a network call and
another multitenancy is a third audit is a fourth
answer first in plain words
answering it is in scope
any criterion unmet or unverified is named in the first line with which of the two it is and what is missing
any public api change
any quick fix compromise you took the reason it qualified and what the sound design would have been
any reactive cleanup you did beyond the literal request
any screen that needs it
any verb the player drives during play stays on screen and moves to where the hand is
anything on the operating guide s or toby swd environment s ask list migration install seed snapshot deletion process or port appears as its own step with the exact command
anything under retired stopped being true on the date beside it
anything where the edge could fire is edge crossing
anyway so running the filter at 60fps when they re typing is wasted work
aop advice or handlerinterceptor
api doesn t the right move sits at the server cdn layer where the
api getorder id then setorder catch e
app layout header toolbar avatarmenu and every intermediate
app post api orders import async req res
app post api orders import async req res
app use audit reads everything written above
app use checkpermissions reads req user req route writes req allowed
app use checkratelimit reads req user writes req ratelimitstate
app use injecttenantcontext reads req user writes req tenant
app use loadcurrentuser reads from req writes req user
appear in the signatures of placeorder or repository methods
appears to deadlock and new requests get rejected
appends after that need no asking
applied across the three with the idiom of each
applied after the first three pass
applied to the user fields that are unset are unchanged
applies the profile changes to the user with this id and returns the
apply a child throwing during render is a real error
apply all tokens above
apply at content scale with the named label or at decorative scale quiet backdrop
apply progressive disclosure to the signature
apply the pull complexity down check
apply the same test whenever you later change a public interface before touching code
apply the test what would this look like if designed with two gateways in
apply these to any boundary decision
apply to both html and pptx output
apply toby artifact paper via a wrapper if needed
applycoupon and rollback semantics
applycoupon code string promise result void validates against items
applycoupon code validates sets may throw a typed error
applycoupon expresses intent and the state layout and the invariants live
approval for one of those applies to that command for this task
approval for the heavy command applies to that command this task
approval needed on its own pnpm db migrate against the local database
approve controlled release with named rollback owner on record sayo mission ops
approve proceed with the action
approximate or bounded state the approximation or bound
appstate listener restore on resume
archival actions defer file review snapshot block
are computed at navigation time and become stale if the user remains on
are conventionally namespaced with a colon e g user theme
are derived from the current user s role and the order s state
are escalation tiers reach for them once a measured bulk insert is the
are naturally efficient simple choices the everyday layer that lands before any
are the pieces independently understandable
are these byte offsets or
aria correct prop bundles
arrives from a fourth re renders large lists memoization decisions
artifact copy stays evidence led
artifacts with a real subject use that subject s own vocabulary
artificial coupling diff visible a constant utility function or type dropped inside a class or module it has no substantive relationship to because that s where it was convenient to put it
as a labeled loan the next person can see and pay down
as a slow query that requires a panicked production fix
as shallow as a module gets
as soon as possible
as the view narrows widen the field of view or pull the camera back so a five inch screen stays legible
as y instructions in the comment
ask before any code when a wrong guess writes data changes a public contract or a stored format moves money touches permissions ships a user visible string calls an external system or costs more to unwind than the change cost to make
ask before anything on that skill s ask list the full suite a migration an install a dev server
ask before automated tests browser automation screenshots or broad repo commands because each one adds latency to the user s loop
ask can the caller pick a better value than the module can
ask first always naming exactly what goes
ask first unless the command directly implements a plan the user approved
ask for a contribution only when it changes the real solution a decision with several valid answers a policy choice an algorithm or structure a behavior with tradeoffs a naming or modeling call
ask for one verdict the chosen candidate a one line reason per rubric item and any over hiding risk
ask for the naming call because product vocabulary you invent outlives the implementation and the rewrite that deletes this module keeps the noun
ask the one why that separates a memorized answer from an understood one right what happens when the input is empty
ask the user to test or run the smallest useful check
ask what would make each one change
ask what would make each one change
ask whether the design is at least as good after the change as before it which no other question here does
ask whether their first guess about behavior is correct
ask whether to use a different port reuse the running process or stop it
ask which the user wants
asked 2026 03 11 by the user cancellations only inside a day
asked 2026 07 24 by the user let people cancel an order from the account page
asked 2026 07 24 by the user no cancelling once it s out the door
asked 2026 08 02 by the support lead they invite someone and then have no idea whether it worked
asking them to generate without one floods them
assert exact deltas the balance dropped so the fine works is the test crying wolf when income in the same step swamped it
assert on the specific value or field that proves the behavior happened
assert the replay never contradicts the result the odds stay inside their floor and ceiling a conserved quantity stays conserved and every position and score stays finite over a long run
asserted with no evidence back to the source before you evaluate anything
asserts it hasn t moved since the read
assign by series index
assignuserrole sets the user s role
assumed standard behavior marks nothing and settles nothing
assumes circular orbit j2 perturbations not included
assumption surfacer that argument only works if we assume x
assumptions still waiting for confirmation
async clear promise void
async clear promise void
async createbulk orders neworder promise importoutcome
async find id string
async find id string promise product null
async function callapi path string opts requestopts promise any
async function callapi t path string opts requestopts promise t
async function featuredproducts promise product
async function get t path string promise t
async function getproduct id string promise product null
async function getrecommendations userid string promise recommendation
async function getrecommendations userid string promise recommendation
async function getuserprofile userid string promise userprofile
async function placeorder input neworder promise order
async function placeorder input neworder tx transaction promise order
async function placeorder req neworder
async function placeorder req neworder uow unitofwork
async getorder req req request
async getorload t key string ttl number load promise t promise t
async getproduct id string load promise product null
async getproduct id string promise product null
async invalidateproduct p product
async list promise result feeditem
async listing category string
async listing category string promise product
async load promise feeditem null
async present opts paymentoptions promise paymentresult
async refresh promise void
async save items feeditem promise void
async save p product
async save p product promise void
async save t tokens promise void
async search q string promise result feeditem
async themepreference promise light dark system
async tokens promise tokens null
async updateproduct id string changes partial product promise product
at a few ms each the
at every allocation is pure complexity
at every cache call site
at every call site
at most one load runs per key per
at scale when even the joined query is too slow
at the end of the task report whether the process is still running
at the turn everything goes still and silent and the reward never comes
at turn 1 the rules are close by and the risk is the trained default warm up hedge closing offer
at turn 10 the banned words are still gone and the writing is worse
atomic either the order is
attempt will return the original failure
attempting to refund an authorization that was never captured raises
auth api user dto go
auth handled globally by the response interceptor
auth internal store user row go
auth is one well defined
auth state in a
auth theme cart coupons and
authenticate as separate methods is the same defect as a js wrapper
authenticate in try catch and handle biometrynotavailable
authenticate nativemodules rnbiometrics authenticate
authenticate prompts the user simpleprompt does the
authenticate will fail silently with no error
autherror has one global handler navigate to login clear session
authority decision card dark red top hairline pill in the corner
authz require p role order read
authzguard is one module owning authorization decisions and responsebuilder
automated tests browser automation screenshots and broad checks wait unless the user asks for them or they are required to run the experiment surface
automatic retry is safe only when repeating the request is safe an
avatar initials only no faces no abstract art
avatarsize 40 onclick fn isselected false truncatenameat 20
average user visits their profile page once per minute
avoid generic lifestyle photos decorative stock art repeating patterns textures and gradients
avoid threading experiment state into durable project flow
await asyncstorage multiremove authtoken refreshtoken feed cache v2 user theme preference
await asyncstorage setitem feed cache v2 json stringify items
await asyncstorage setitem hasseenonboarding true
await barcodenative setcameraindex 0
await cache invalidate product listing oldcategory forgot the new category
await cache invalidate products id typo plural
await cache set featured fresh 600
await cache set key fresh 600
await cache set profile userid fresh 60 60 second ttl
await cache set user profile profile 3600 1 hour picked by author
await cache set user profile profile staleness user profile
await inventory reserve input items
await inventory reserve req items
await inventory reserve req items tx
await inventoryrepo reserve input items tx
await payments authorize input payment
await payments charge req payment
await payments charge req payment tx
await paymentsrepo authorize input payment tx
await sleep backoff attempt
await sleep backoff attempt
await sleep retryafter res
await startnative camera rear
await this cache delete product listing p category
await this cache delete product p id
await this cache invalidate product id remember
await this cache invalidate product listing p category
await this cache invalidate product listing updated category and this
await this cache invalidate product p id
await this cache invalidate this listingkey p category
await this cache invalidate this productkey p id
await this cache set key fresh ttl
await this evictderivedfrom p
await this inner save p
await this inner save p
axis labels 12px text secondary 3a3d42
axis lines 1px solid border strong 7d7967
axis titles 13px text primary 1a1c1f
b a retrying transport the client is constructed with interface callers
b has the simpler caller side interface and absorbs the there will be more call
b upload bytes key
back off honor retry after delta seconds or an http date
back up and ask from a different angle or ask what context would help them answer
backdrop where rows columns mean something
backend and frontend agents md and readme md
backfill and a reverse schema step
background process left running with no mention the user finds it later doesn t know what it is kills the wrong thing
background toby ink 2 2a2d33
background toby paper 3 fffefa
background var surface card
background var surface card overflow hidden
background var surface card padding 20px
background var toby info color var text on dark
backpressure has business value because serving i can t reach
bad clarity outlasts every other kind of complexity
bad test coupled to internals asserts on incidental data or doesn t test what its name claims
badge mono compact chip with a count or token
banned word swap cloze the word that carries the learning six words of periphrasis worse than what it replaced
banned word swap this is the load bearing assumption of the design
banned words live in the operating guide already loaded
bar vertical or horizontal
base behavior and changing basecontroller requires reading thirty files
baseline and no idea whether the cache will help
batch is too big chunk the input above some threshold and what a partial
batch operation one statement
batched native api one crossing
batches of 1000 or so to avoid blocking the database with a single huge
batching candidates hides cause and effect
be a short note
because hit rate was low
because that policy is a property of the org computed inside the service from
because the route is serializable and small
because they collapsed into status s discriminator
been staring at this for an hour paste it
before 34 words to remove the cover assembly first remove the four screws that attach the cover to the housing and then after taking the cover off the housing remove the packing and throw it away
before a step name what it turns on
before accepting a snapshot update read the diff and confirm every change is intended
before adding a helper module component hook or error type search the repo for the operation
before adding one ask whether the behavior belongs in the underlying module
before adding one check whether the module can compute or decide the value itself
before and the eager load hint is the only change
before calling a clarity pass done check the code you touched against every red flag below
before calling a docs change done run both red flag lists against the file you
before calling actions refund is one if statement away from a bug
before calling test coverage done check it against every red flag below
before calling the change done state in one line which anti pattern above the
before changing an existing callable surface list its callers in the repo and the behavior each relies on
before clear the cache if the build fails
before do not call this from a request handler
before don t run the seed script it truncates first
before either check that this skill should be running at all
before exposing a parameter ask whether the caller can choose a better value than the module can
before finishing check the change against every red flag below and fix anything that applies
before finishing check what you did against every red flag below
before introducing an inconsistency both must hold
before introducing any convention naming structure error handling style test layout inspect the local file and project and mimic what s already there
before loading the fixtures while running the migration causes the seed to race the schema change
before runway light connection resistance calibration
before sessionstore caches the token
before sketching state in one sentence what this module encapsulates
before test cancel rejects shipped fail expected 409 got 200
before the cache never invalidates the ttl is set at construction
before the handler performs a validation of the payload and then does the initialization of the worker pool
before the handoff every heading appears verbatim in a test
before the migration touches the session table and the audit log
before the response was lost
before the second file exists name the two conventions nearest to this work that the repo already has even from another layer and say which one you re extending
before touching code recover the mode the criteria list the plan and which steps are checked off and the last checkpoint decision from the thread the diff the plan file the behavior record and the repo
before user session token refresh failure rate
before you touch anything say which number goes down lines branches state variables duplicated blocks or named traps removed
beginner who can t produce yet lead with input they can follow and ask them to show they followed point choose answer before pushing production
behavior are repeated in every screen that touches the feed five forms of
behavior change in production code needs a test to follow it
behavior documented here that belongs in an interface or field comment
behavior every usememo adds a closure allocation a comparison of
behavior intentionally changed update the test to describe the new behavior
behavior is obsolete remove the test and explain why in the commit message
behavior record append criteria 1 and 2 to docs behavior md bind criterion 1 s
behavior record entries written edited or retired and the result of the check
behavior selected by environment which gateway which storage driver which log sink is one decision made once at the composition root
behavior split from presentation a headless hook owns the state machine and each screen composes it
behavior still valid production broke it fix the production code
behavior that defies convention a constructor that spawns threads a method that mutates unrelated state an effect that runs on a non obvious trigger
behavioral risk a refactor a public api or module boundary change or an
behaviors it calls three functions
behind dense plots only
behind ground station coverage diagrams
behind it sit a fetch a cache a default for the offline case and a refresh policy
behind orbital diagrams to indicate the gravitational frame
behind the atomic guarantee in the comment
behind them sit the variable names the parsing the coercion the defaults the validation and keeping secrets out of log lines
being non null and position 0 being non null as a stale data state where
being passed between them is req a giant bag that each step reads from and
belongs inside whichever module owns the underlying decision
best served by customer id status created at desc
better place boundaries at recoverable units
better when possible redesign the semantics so the special case stops existing
between cost and benefit
between them cover index then content then references
beyond good design performance work has three layers
beyond the signatures above toby artifact ships 60 components total
big bang redesign trying to fix the whole architecture in one pass is the waterfall failure mode
big topic in 96 128px inter 600 tight negative letter spacing
big topic the toby wordmark mark a quiet decorative orbit lattice
bind arrowleft and arrowright keys via useeffect window addeventlistener
bindings the sentence quoted verbatim in the test description
biometrickeysexist nativemodules rnbiometrics biometrickeysexist
biometrynotenrolled and biometrylockoutpermanent separately
blip never reaches the component
block within a file
blocks that look alike but answer to different reasons are separate decisions that happen to share text
bob and sway on a shared clock with a per thing offset so the crowd does not pulse in unison
body of knowledge theme is another feature flags a third
body paragraphs tables cards diagrams interactive controls
body text on ink fdfaf1 primary a8a39a muted
body this cannot be undone
boolean dryrun boolean skiptaxvalidation
boolean names read as predicates cursorvisible isready haschildren
border 1px solid t hairline
border 1px solid var border hairline
border 1px solid var text on dark muted color var text on dark muted
border 1px solid var toby info tint padding 4px 10px
border 1px solid white space nowrap
border left 4px solid var text muted
border radius 6px padding 8px 16px
border radius 999px font size 12px font weight 500
border radius var radius base
border radius var radius base background var surface card overflow hidden
border radius var radius sm
both a persisted format and money are strategic triggers so the design lines are owed before the plan can name a file
both are a module that is not closed to modification
both are a module that is not closed to modification the next case edits shared code or several files at once
both are as simple as the slow
both are deliberately sparse because they bracket the dense reference content
both areas share one move
both get worse when handled tactically a try catch at every call site a cache added because something feels slow a special error path nobody tests
both go in the handoff
both halves carry weight
both meters above their watch threshold for two consecutive samples t 04 08 t 04 12
both new tests fail before the
both the engineer who wrote it and the one who approved it have left
bottleneck and leave them on the shelf until then
bottom mono stripe three captions
bottom mono stripe with 2 3 captioned facts contents convention owner
bottom mono stripe with two captions source line on the left begin again at 02 pointer on the right
bottom variant has a drag grip
bound rendered window recycle aggressively
boundaries the user keeps their nav state just this route is broken
boundary aggregates more than it should because no smaller boundary was
boundary because none owns a distinct piece of knowledge
boundary written once per toby swd modules and toby swd interfaces the module exposes seatcountforday workspace date and owns the storage layout behind it
bracket below an axis names the interval as a thing
branch exists because the caller lacks a value the type could hold
branch picks which function runs bodies are one liners closed set
branches read different fields set is closed
branches that select a small behavior by a tag value become a lookup map
breadcrumb slash separated last item is current no link styling
breaker on top is duplicated machinery
breaker prevents the pile up by short circuiting calls when the downstream
bring the axis label and caveat treatment from this file regardless of library
brownfield a scattered conditional is a scoped refactor
brownfield which is nearly always the repo has already decided most of this so go find where
bucket mechanism to be usable
bucket refill it with the current time check tokens then consume
buckets tokens or time
budget and the general purpose bias
bug fix without a regression test the fix is unverified and silent re breakage is now possible
bug fixes with defined durable behavior reproduce the bug in a failing test before touching production code
bugs and regressions what breaks and for whom
bugs logic errors null handling race conditions stale state cleanup gaps invalid assumptions unreachable code data loss
build a different set for a different mood and keep the single rationed red
build a proof of concept
build a toby artifact style html deck paginated prev next react jsx only when the user has asked for a produced artifact deck or slides
build new state tinted patterns the same way
build the slice prove it record it
build the smallest concrete instance yourself first then explain against it the way you would a line of code
build the thing that was asked for in pieces someone can watch land
build time env expo public vite app config ts follows the same rule one typed config module imported values no raw process env or import meta env reads in components
build your own only if you have a reason
builder style with methods
building a chart plot or data visualization references charts md
building any component callout badge stat grid worked example decision row sparkline table code block dialog references components md
building the words exactly is how a feature ships correct and useless
builds that reveal one bullet at a time
builds that reveal one bullet at a time
built for performance you ll love fails
built on a robust modern stack it s the dashboard you ve always wanted
bulk endpoint one round trip
burden drops from eleven decisions to four
buried in the prose
but it s a value object
but the design time move is cheaper and better
button class alert close aria label dismiss button
button class dec btn dec btn approve dec btn on approve button
button class dec btn dec btn hold hold button
button class dec btn dec btn reject reject button
button default paper primary ink ghost transparent consequence accent
bw accent 2px focus ring in page accent
bw emph 3px section accent strip active tab underline sidebar inset
bw hairline 1px default rule
bw strip 4px alert left edge dialog alert top
c is a different decomposition driven by b s flaw memory blowup on large
c upload source key where source is bytes or a stream
cache failure semantics fall through silently as part
cache failure surfaced as exceptions to every caller
cache get key cache set key val ttl exposed to callers
cache invalidation list maintained in service code
cache key construction ttl policy eviction rules and load through semantics
cache logic in handlers is the scattered antipattern of example 1
cache outages are surfaced via metrics
cache owned by the data module that produces the key
cache reduces p95 by 41 is a heading
cache what format it s stored in what version it is is reflected in two
cached redis get key
cached redis get key
cachedproductsstore earns its place as a real module
cachedproductsstore is the one module that
caches are one of the most common forms of small thing that goes everywhere
caches earn their complexity by hitting often enough to
caching because it already owns the data access contract
caching holds until the price changes and nothing here tells the cache the price changed
caching the repository is properly example 1 from caching md
cadence two sample rule
cadence two sample rule named rollback
calendar today gets 2px toby info border bold info text
call isavailable first to check whether
call setx after computing what x should be
call site where the author has the least context
call sites lose their copied checks because deliver handles an unavailable
call the client normally and retry is a property of the client configured
call visualize read me first then override its css variables with toby artifact tokens
call visualize read me first then override its css variables with toby artifact tokens before generating output
called out in the different layer check as legitimate
caller can decide if it s a 404 a validation failure an auth problem
caller can reason about
caller checks cache get then chooses what to do on miss
caller facing information the guardrail step 7 of the procedure
caller handles concurrent misses
caller handles the 0 row case reload maybe retry
caller makes eleven decisions for what is conceptually one action
caller s common case burden
caller s filter combination becomes a new method
caller site uses it
caller still needs to know that a payload signature is per device so
caller wants to know
caller writes one line
callers also have to
callers can t accidentally produce an unsafe variant
callers can t drift the state into invalid combinations
callers can t trigger n 1 because
callers cannot see and do not manage
callers composing multiple operations must thread tx through
callers handle the typed errors at one place usually a response
callers have to read the body to find the write
callers make one decision which intent down from eight
callers manage isopen highlightedindex
callers must is in the comment four times
callers must know how
callers must know to set
callers must know what s expensive and the cost is
callers must store and pass it back on every render
callers need this so it must
callers never touch rows
callers only see deadlocks that persist
callers receive plain domain objects with all relations populated as
callers retry on their own
callers should check success and show the error if false
callers should treat position 1
callers while a single load refreshes the cache in the background
callers write rl allow userid and know nothing about the lock
callout alert toast state tinted panel using the soft tint text family
calls don t appear in logs and the dashboard is misleading
calls must happen in that order or it breaks
can be understood alone
can create the resource twice because the first attempt may have committed
can t finish in time compare ctx s remaining time against the next backoff
can we ship it like this you can
cancel esc overlay click
cancel reason string promise result void
cancel reason string promise success boolean error error
cancellation deduping retry cache refetch on focus request waterfalls
cancelling a shipped order returns 409 and leaves the order untouched
candidate a exposes the implementation
candidate a the literal mirror of the native module
candidate a the one big component
candidate a the schema mutation script
candidate a too thin
candidate b hook owns the state machine
candidate b smaller surface typed state
candidate b the deep module
candidate b three named intents over one core
candidate b too rigid
candidate c the value object
cannot be guaranteed the contract changes to expose it returns include
canvas three js mount or dom board then floating paper ui
cap or soften a force near a singularity
capped is a leak about the implementation
cards and kvs are secondary
cards stay at 12px radius and only outer containing frames get 18px
cards ticker upgrade names and the end screen get the wider voice
cards toby paper 3 with 1px border hairline ring
careful performance behavior documented nowhere is the same defect as a
carousels or display none sections during streaming
carries a permanent flag so callers don t re retry
cart invalid payment method invalid shipping unavailable
case cancelled return do nothing user closed the sheet
case click return placemarker state e x e y
case completed navigatetoconfirmation result transactionid break
case expose unitofwork isactive as a one method check and keep the tx
case failed showfailure result reason break
case key return applykey state e code
case loading return spinner
case not found return ordernotfound
case paste return inserttext state e text
case still has to make eight decisions
case success return orderview order q order
case the user needs to see and it shows up as a typed result discriminator
casing uppercase eyebrows tracked 0 12em for section labels
catch at compile time the
catch e if cancelled seterror e
catch e sentry captureexception e
catch it here and retry the same way
catch regressions when something changes
catch return caller cannot distinguish no orders from failed
categorical section accents when a page has multiple sections give each its own color from the chart palette
categorize by status class retry in the client wrapper
caught mid stream their working memory is full so flag the lesson in one line and resurface it at the next seam
caveat next to the figure never in a footnote
caveats sit next to the claim never in a footnote
caveats sit next to the claim they qualify never push uncertainty into a footnote
cb filtered map p i
centered single bullet wisdom slides
centred single claim the most fundamental fact the deck taught
chance lives upstream in what they can read aim wobble a keeper s lean a wake risk that climbs as the night gets loud
change and pass after quote both runs
change came closest to and what stopped it
change copy never the structural classes
change one and every reader has to change
change one behavior parameter path or surface
changed code bugs regressions missing tests security issues and repo rule breaks
changed is who calls it
changequantity itemid string qty number void validates qty 1
changes to the current session does not commit the caller must commit
changeuserdisplayname updates the user s display name
changeuseremail initiates an email change sends a verification email
changing a key requires editing every site that
changing deleting weakening or snapshotting a test and whenever a
changing one island leaves the rest drifted and the reader worse off than before
changing the role triggers a re evaluation of permissions across the
changing the staleness policy
changing what it returns or what its callers must handle is a trigger
channel channels notification channel
channels dict str channel
characters are a number and a line personality is a few tuned floats and a blurb that names what the float does his aim drifts on its own
charge payment payment promise chargeresult
charges must be idempotent per order id attempt
charging the payment method
charging the payment method
chart axis legend and kpi labels
chart palette is positional so use in fixed order by series index
chart palette positional order for series and categorical accents
chat a short framing line the comprehension check question and any branch options
cheap to compute the memoization itself costs more than what it saves
check a verb can physically succeed before you ship it
check against reality with the narrowest useful step
check banned words invented foils and the patterns in references examples banned writing patterns md
check before handoff run the grep in references behavior record md and report what it printed
check correctness with a script
check forms for right answer subjects predict the result before you run or work it find the defect in a version you made wrong on purpose defend the option that got rejected say what breaks downstream if a condition changes name the case left unhandled
check members list suite
check test cancel rejects shipped unmet if the order changes
check test cancel returns cancelled order unmet if the status stays open
check test cancel scopes to owner unmet on any 2xx
check test expired link creates no account unmet if an account exists after
check test full sku ranks first unmet if any name match outranks it
check test idle workspace returns zero unmet on a missing row
check test invite appears pending unmet if the address needs a reload
check test invite email link opens accept unmet on zero or two emails
check test name match outranks foreign sku unmet if the sku match leads
check test seat churn counts once unmet at two
check test usage page shows completed day count unmet if it shows a partial day
check that suite unmet on any ordering change
check the command that shows it holding named before any code
check the finished work against all eight before writing the handoff
check the finished work against all eight entries in references checks md plus the red flag list of every skill on the active skills line before writing the handoff
check the loop against this list before handing the result back and name any
check the near future variants against your design
check the orders list suite unmet on any changed row
check the service should hold product logic and half the code here is redis
check three real sizes before shipping 390 844 portrait 768 1024 tablet 1280 800 desktop for overlapped controls cropped panels and buttons the thumb cannot reach
check whether the inference follows on top of the three axes
check x y 1 at x 0 y comes out to both 1 and 1 one input producing two outputs so the circle sits on the wrong side of that rule
checkbox checkboxgroup group has row modifier for inline layout
checkout cart cart user user shippingoptions shippingoption
checkout confirms a paid order
checkout context shape steps read flow state only from checkoutprovider
checkoutprovider this is the central note
checkoutprovider tsx supplies flow state via context
children plus a small subcomponent api compound components
choose deliberately and fix a real over hiding risk before writing any body
choose the primitive for the structure of the subject a network topology crosshair or grid
chunking retries multipart threshold
ci is still running
circle cx 118 cy 19 r 2 fill var toby accent
circle cx 32 cy 32 r 1 6 fill var toby ink
circle cx 32 cy 32 r 28 stroke var toby ink stroke width 1 4
circle cx 56 cy 20 r 4 fill var toby accent
circuit breaker because we should
cites mechanism threshold sampling rate source sample size
cites the source historical anchor for the claim
claim a win only when it s countable
claim about that output
claims cite a source or mechanism
clamp a runaway speed
clamp an ultrawide so the playfield does not stretch
class cachedproductsstore implements productsstore
class cachedproductsstore implements productsstore
class hierarchies built heavily on implementation inheritance tend to be the hardest parts of a codebase to change
class legacybankgateway implements gateway
class postgresproductsstore implements productsstore
class stripegateway implements gateway
classify the failing test before changing it
classitis over subdivision many shallow modules whose interfaces sum to more complexity than they remove frontend over componentization
cleanly clearly honestly no fluff in plain english just the facts simply put in a nutshell
cleanup lives on the effect
clear removes everything including auth tokens use deletespecific keys
clear the home indicator with env safe area inset and cap a portrait bottom sheet near 40 46vh so it never swallows the play
clearer tidier more idiomatic count nothing down
clickable taxonomy clicking a term or node expands its definition or sub structure inline
client error caller s fault
close by handing the learner the cue to retrieve it later next time you meet a case like this the question to ask is
close by making the learner give the reason back in the form they would reuse the one line rule the note or the thesis and the strongest counter it must answer
close on a graded rank laned by how they played with a verdict in their own stats
closed and using it later blows up
closeonesc and closeonoverlayclick should both be false for destructive
closes resumes calling when test calls succeed
cloze the word the learner must produce
coach in chat against the work in front of you
code ahead of its plan an edit written before the step covering it was approved or a step that ran differently from its approved wording with no edit to the plan
code and comments track the mechanics while the outside reason that forced them lives in this file
code background var toby ink 2 color var text on dark
code block dark theme
code block lifted syntax colors for dark bg
code com 7c7568 comments muted italic
code com color var code com font style italic
code comments docstrings error messages
code copy position absolute top 12px right 12px background transparent
code is read far more than it is written
code kw 8ec8d8 keywords info teal lifted
code kw color var code kw
code level critical path redesign is the last resort
code ln color var text on dark muted margin right 16px
code nothing reaches the unit tests are green
code num color var code num
code num e8a48e numbers warm coral
code str a8c98a strings green lifted
code str color var code str
code that fights the language s idioms
code that hand rolls what a library already in this repo provides
code that ignores a pattern this repo already uses
code that s correct but over built awkwardly named or non idiomatic
cold start behavior when the cache restarts every request misses
collaborators each controller injects
collapse to one row component
collapsing distinct error paths or hiding a failure path is a behavior change
color var text muted font weight 600
color var text muted letter spacing 0 04em line height 1 5
color var text muted margin left 4px
color var text muted margin top 4px
combinations the caller has to decode
combine 8 props to express a single intent
combine into one method or hide the order behind a factory builder
combobox covers select and autocomplete searchable by default pass searchable false for plain select
combobox usage now reimplements the same usestate dance
command palette full screen search with grouped items keyboard footer
comment at a different level than the code
comment for c is short and mentions no internals so it passes the test
comment for getorload four sentences the docstring above
comment for ordersrepository insert
comment for productscache getproduct
comment for update profile
comment for updateuser updateuserrequest
comment invalidated at a distance a change makes a comment elsewhere in the codebase away from the edit itself factually wrong a caller s documented assumption an invariant description at a distant call site a doc comment describing behavior that just changed
comment invalidated at a distance a change makes a comment somewhere else in the codebase wrong a caller s documented assumption an invariant note at a distant call site a doc comment describing behavior that just changed
comment mentions session connection cursor
comment on the non obvious the choice the constraint the cost
comment passes four sentences no ordering no exception names no
comment repeats the code no information beyond the declaration
comment test catches problems early
comment that says callers must call x before y
comments docstrings conventions and control flow a reader trips on
comments one per rpc
commit subjects and bodies pr prose
common callers just call
common case requires a caller to make 20 decisions most of which are
common errors handled at every call site
comparative panels two side by side panels update from a shared control
compare a few approaches
compare by referential equality or to the dependency arrays of other
comparing two options that both exist is content
completion give claim and evidence blank the warrant here s the claim and the quote
completion on the next similar instance give the whole structure and blank the part where the real choice lives
complex than the loop version and in most languages and orms it s
complex than the slow version so they sit outside the speculative
complexity from example 2
complexity inside the cache layer
complexity than slow the decision is made when the table or the query
compliance the change against what was agreed
component is back to rendering
component s body where a reader naturally meets them in the order they
component s props list carries currentuser though only the leaf uses it
componentdidmount window addeventlistener resize this handleresize
components are forced to know about a value they have no use for
components call cart add item
components in libraries like svelte error boundary or the framework s
componentwillunmount window removeeventlistener resize this handleresize
compose freely and bring the component vocabulary onto slides
composes them as injected fields narrow and named for
composition mode pick one from below
composition mode pick one from skill md different from the one used last time
composition vs inheritance shows up most concretely here
compress with a value object or query parameter that lets one method cover the cluster
computation against data that has since moved a lost update that commits
compute the new array including duplicate handling item with the same
concentrate the discipline for touching it in one place so locks checks and assumptions stay off the call sites
concentrate the discipline inside the owner
concentric arcs decaying outward
concentric circles radial spokes
concept one rich decision or idea to reason through
concepts a caller needs the abstractions a caller operates with to use this correctly
concretely don t import private modules to test them
concurrent requests all miss all call load all hit postgres
concurrent updates in any rdbms will produce occasional deadlocks
conditional has been stable for a year
conditional that grows per domain change a switch or if chain that takes a new arm every time the domain gains a case or the same branch decision copied across call sites
cone widening with distance
confidence bands fill at 18 opacity over the series color
confidence high ci 95
confidence medium ci 80
config that changes without a redeploy remote flags remote config is its own deep module
config ts the only file that touches the environment
configprovider holds the typed object and components read it through a useconfig hook
confirm every symbol this change didn t define library function config field env var cli flag component prop error type against the installed source or the pinned manifest unless this repo already calls it somewhere you can cite at path line
conjoined methods one can t be understood without the other
conjoined methods test if you can t understand one method s implementation without reading another s they are conjoined a red flag whether or not they sit in the same file
consequence what breaks and for whom in plain words at file line returns last month s balance to a logged in user on first load api account ts 42 claim only what the trigger and path reach
consequential exported or public has or will have several callers crosses a module service process boundary is a shipped component s props or encodes a contract costly to change later persisted format published api anything other teams build on
consistency rule one name one purpose
consistent order or it trades the lost update race for a deadlock
console error parsed error flatten fielderrors
conssoft f6e2dc constint e9b7ad constext 7a2d22
const app new app db logger gateway
const authcontext createcontext authstate null null
const cached await asyncstorage getitem feed cache v2
const cached await cache get featured
const cached await cache get key
const cached await cache get profile userid
const cached await this cache get t key
const cart setcart createstore cartstate items coupon null
const cb createcombobox items products getid p p id
const cb usecombobox items products getid p p id
const contacts await contactsbridge getallcontacts 1 bridge call
const coupon writable coupon null null
const ctx usecontext authcontext
const data loading error refresh usefeed a hook over feedrepo
const data product isloading error usequery
const db new database config databaseurl
const debouncedfilter usedebouncedvalue filter 100
const error seterror usestate error null null
const error seterror usestate error null null
const featured await cache getorload featured 600 expensivequery
const filter setfilter usestate
const filter setfilter usestate
const filtered usememo fuzzyfilter products filter products filter
const fresh await expensivequery
const fresh await load
const fresh await load
const fresh await profilerepo find userid
const gateway gateway gateways config gatewayname
const gateway gateways config gatewayname the rung 2 map from the conditional ladder
const granted await barcodenative requestpermission
const granted await ensurepermission owns ios android nuance
const handleclick usecallback onselect product onselect product
const handler handlers get message type
const handlers new map string messagehandler
const height setheight createsignal window innerheight
const i seti usestate 0
const ids await contactsbridge getallcontactids 1 bridge call
const inflight this loadingmap get key
const items await api getfeed
const items setitems usestate feeditem
const items writable item
const loading setloading usestate true
const loading setloading usestate true
const logger new logger config loglevel
const native await nativemodules paymentsheet present opts
const onresize setsize width window innerwidth height window innerheight
const onresize setwidth window innerwidth setheight window innerheight
const onresize width window innerwidth height window innerheight
const order await db transaction async tx
const order await orders insert input
const order await orders insert req toorder
const order await orders insert req toorder tx
const order await ordersrepo insert input tx
const order setorder usestate order null null
const orders useorders 500 items
const orders useorders already sorted by total descending lowercased customerlower added
const parsed schema safeparse process env
const preview previews file kind
const previews record filekind fc file filemeta
const priceformatted usememo formatprice product price product currency product price product currency
const product error loading refetch mutate useproduct id
const product setproduct usestate product null null
const products productsstore new cachedproductsstore
const q useorder id returns the discriminated query result
const q useproduct id
const query createquery querykey product id queryfn api products get id
const res await fetch path opts
const res await fetch path opts signal timeoutsignal 10 000
const result await nativemodules paymentsheet present
const result await ordersservice create order one db round trip each
const result await ordersservice createbulk req body orders
const result await payments present
const results setresults usestate
const schema z object
const session this auth forrequest req
const size setsize usestate width window innerwidth height window innerheight
const state setstate usestate idle denied scanning error idle
const state usebarcodescanner oncode handlecode
const sub subscribe opts oncode
const tagsclass usememo tag tag product category product category
const themepref await asyncstorage getitem user theme preference
const titleprops usememo id product product id title product id
const updated await this store update id changes
const user route params
const user setuser usestate user null null
const usestore create set
const width height createwindowsize
const width height usewindowsize
const width setwidth createsignal window innerwidth
constructor private cache cache
constructor private inner productsstore private cache cache
constructor private store productsstore private cache cache
consume flow state from context
consumer drags in the whole users package and the whole interface even
consumer side narrow interfaces
contact alone stays tactical
contacts photos calendar events serializing the whole list once is
contacts push await contactsbridge getcontact id n bridge calls
content scale 120 220 px the mark is doing work
content scale marks get a small uppercase label in mono 0 12em letter spacing text muted sized 11px
contention because of the partial import
context implemented via asynclocalstorage context propagation
context plus one opportunistic improvement to code you were already in
context where context is the skill grammar and collocation cards need a sentence frame so the usage shows
contextmenu right click wrapper around menu
continuously through the change
contract a route should have
contract exactly like the fall through rule
contract for that capability and a deep module hides the protocol behind a
contract in the type system so the caller no longer needs to remember
contrastive filler sounds like it s not just fast it s transformative this isn t about code it s about people the move make the claim and stop
control heights pair with space paddings
controller share a two way coupling
convenient for any future need
convert from specific to set before doing anything else
convert it with the ladder in replace the growing conditional a single stable dispatch point over a closed set is not this flag
copies drift with no signal to the reader that what they re reading is stale
copy and adapt these when you need a quick demo paragraph or callout body
copy of the whole transaction repeated below
copy the approach and write fresh words for the moment in front of you
copy the approach and write your own words for the moment in front of you
copy the approach and write your own words for the moment in front of you
copy the pattern and write fresh material for the moment in front of you
corporate grandiosity sounds like leverage our robust comprehensive platform to seamlessly unlock value the move say what it does in words a person says out loud
correct the targeted pattern or the one that blocks meaning and let the rest ride in a fluency turn
correct under heavy contention where optimistic retries would thrash paid
corrections are compensating entries
correctly and no amount of good naming supplies them
costs no more complexity than the slow version
costs one extra argument on the repository method
count how many modules would need to change if it changed
count what an error signal costs before adding one
counter case so this is not read as never split
counter claim test could someone who read this the opposite way say the same sentence
counting ships first because the page has nothing to render without it
counts as a duplicate rule
counts map string int
counts map string int
cover the contract s distinct observable outcomes then stop the success path each documented failure mode and the boundary conditions
cover today s needs and one or two near future variants you can name
coverage driven test written to hit a line
covers a single file where information leakage s two module trigger never fires
cpu work in serialization
cqrs style read store most complex so reserve it for measured need
create an agents md at the root of a module package or feature that owns a real body of knowledge
create db migrations 0043 invites sql invites table id org id
create index idx line items order id on line items order id
create index idx orders open on orders customer id created at desc
created after 2024 01 01 created before 2024 12 31
createdafter and createdbefore form a half open interval after
createdbetween from 2024 01 01 to 2024 12 31
createkeys nativemodules rnbiometrics createkeys
creates a new slice
creates a second row
createsignal and creatememo are fine grained and unnecessary
createsignature nativemodules rnbiometrics createsignature
createsignature signs a payload using the
createstore in a module
createuser u user error
creation grouping every feature concatenated into one prop surface
credentials or settings or otherwise changes state outside the edit
criteria 1 and 4 land at the members screen and 2 and 3 land at the mail and the accept route
criteria are fixed text once coding starts widening one is a note in the next report
criteria in the wording agreed before coding
criteria object outgrows the url the same object moves to a
criteria reaching different seams are different slices a route and a scheduled job two screens an api and a migration a caller can see
criteria read back off a diff agree with that diff including where it was wrong
criterion 3 came out of the ambiguity pass
criterion 3 cites a repo fact so it gets none
criterion 4 is a
critical path entirely so the timeout never even comes into play
critical section is one short block no network call inside it one
cross check table below
cross cutting interceptors logging metrics can still exist and they re the
cross module decisions any design decision that touches several modules and can t be encapsulated in one of them is recorded here once
cross process locking add a short lived distributed lock redis
cross reference a called method from its call site
cross tree shared value
crosses functions classes services files packages components hooks
crosshair with bracket marks around a target
crossing 900 km enters the leo debris flux watch band
cta verb the actual next step
ctrl h lg 44px touch target otp slot
ctrl h md 36px default btn input combobox
ctrl h sm 28px btn sm toolbar btn pagination cell
ctrl h xl 56px topbar
ctrl h xs 20px badge very compact pill
ctx carries a deadline and sleepctx
current three js lights physically so old intensity numbers render dim
currently encoded nowhere callers can trust
currentuser and lastvalidation are instance variables that both sides mutate
currentuser is needed by avatarmenu four levels deep
customer id status queries and customer id status created at
cuteness is a choice
cuts is the failure mode
cutting by layer is the classic wrong cut catalogued as layer cut in references checks md
cycle the set so each item is retrieved cold after a gap
d invites showing the columns
dark ink panel caret
dark means judgment toby ink 1a1c1f for decision surfaces evidence panels summaries section dividers
dashboard surfaces alerts when p95 latency exceeds 200 ms over a 60 second window
dashed path between two anchored points
data and is non destructive only if no phone number values were
data driven dispatch a lookup map when the branch selects a small behavior by a tag value
data for a short window
data loss needs a path to the write
data order order undefined
database aborts one transaction and the application is expected to retry
database url z string url
databaseurl parsed data database url
datepicker calendar inside a popover trigger shows iso format selected date in mono
db execute insert into orders values order
db execute update users set last seen now where id user id
deactivate can transactionally write the audit log and revoke
deactivateuser marks the user inactive and revokes all sessions
dead function or class a function method or class that compiles and could run but nothing calls it anymore
dead function or class a function method or class that compiles and could run but nothing in the system calls it anymore
deadlock retry logic at every call site
debounce the filter input
debris flux excluded above 900 km
dec actions display flex flex direction column gap 8px min width 120px
dec border 1px solid var border hairline border radius var radius base
dec btn approve color var toby stable border color var toby stable
dec btn background var surface card border 1px solid var border hairline
dec btn hold color var toby watch border color var toby watch
dec btn on dec btn approve background var toby stable color var text on dark
dec btn on dec btn hold background var toby watch color var text on dark
dec btn on dec btn reject background var toby accent color var text on dark
dec btn reject color var toby accent border color var toby accent
dec meta font family var font mono font size 11px
dec title font size 14px font weight 600
decide then update the lock releases at commit
decide what information matters
decide where code lives and which module owns which knowledge
decide whether an error path a retry a cache or an optimization has
decide whether this change leaves the design better or worse before
decision cache 5 minute ttl repository level
decision row title meta approve hold reject buttons in matching semantic colors
decision the comment states and a cache has it only when the contract says so
decision tree branching process y fork or tree branches
decisions are imperative and short approve controlled release hold file escalate
deck artifact glossary mechanism explanations tables diagrams derivations examples summary
declared type differing from the real one a value typed as a broad supertype but actually a specific subtype with different behavior match them
decorative chrome chart frames ornamental backgrounds
decorative emoji sounds like doing the work of emphasis or structure
decorative gradients on title slides
decorative motion fails the test
decorative orbit lattice large quiet far right
decorative scale 40 80 px the mark is textural
deep but assumes all in memory
deep implementation inheritance hierarchy subclasses you can t read without reading the parent parents you can t change without checking the subclasses
deep teach only the few that trip everyone false friends irregulars the step people reverse
deepest covers current and near needs
def allow self client id bool
def confirm order order id str order
def confirm order order id str order
def confirm self order id str order
def consume self bucket n bool
def create self draft newuser user
def deactivate self user id userid reason str user
def delete self user user none
def display label self
def display label self
def display name user
def display name user
def display name user
def display name user
def expunge self user user none
def find by email self email str user none
def find many self filters order by none limit none offset none list user
def find one self filters user none
def find self order id str order none
def find self product id str product none
def find self user id userid user none
def for customer self customer id str since date list order
def get bucket self client id bucket
def get product listing category str list product
def get product product id str product
def get tokens self bucket int
def list orders for display customer id
def list self query userquery page user
def listing self category str list product
def place self draft neworder order
def process self payment
def query self query user
def refill self bucket now none
def refresh self user user none
def remove session store sid
def remove session store sid
def save self user user user
def schema down self
def schema up self
def send notification user
def send notification user
def session self session
def test cancel returns cancelled order
def trim text start end
def trim text start end
def update product product id str changes dict product
def update profile self user id userid changes profilechanges user
def update self product id str changes dict product
default is no cache
default methods on an interface or trait or a generic module that makes hidden assumptions about its argument can reproduce it
default no memoization components re render
default permits the read then write race above which is why one of the two
default return assertnever e
default temporal axis decoration
default text t textprimary on paper t textondark on ink
default to no memoization memoize the one measurably expensive computation
default to the current diff when no scope is given
default to the highest fidelity dependency that is fast and deterministic
defaults and the validation and the contract gets short
defaults and torch availability checks can all be handled without callers
defend x or find a reading that doesn t need it
defending a direction signals you ve decided what they re after before they have
defer rl mu unlock
defer test writing and test runs while the behavior is still being discovered
defer the next decision until the debris model is updated
deferring cleanup to after this there is always another after this
define a named type with named fields instead
define it outside the component or memoize it
define the error out of existence before writing any handling ask whether the operation s semantics can be redefined so the condition is no longer an error
defining out doesn t
definitions of active and validation specifics move to
delete comments whose content is already obvious from the adjacent code including comments that just restate the name
delete every note re read the procedure and confirm the reader can still finish the task
delete it and let callers use
delete it and use cache
delete key string promise void
delete key string promise void
delete removes the value
delete schedules a deletion
delete something then read what is left
delete the final clause of each sentence did the sentence lose information
delete the first sentence of the reply then the last what went missing
delete the throwaway surface
delete the wrong row gets recycled and component state attaches to the
delete this variable fail if absent becomes ensure this variable no longer exists throw if an index is out of range becomes return the overlap empty if none the error case disappears the api gets simpler the module gets deeper
deleted node modules next or dist to make a build work sometimes correct often a workaround for a real problem that is now masked
deletedat time time db deleted at json
deletekeys nativemodules rnbiometrics deletekeys
deleteuser id string error
deleting a user leaves their sessions in the table
deleting files dropping data force pushes hard reset killing processes clearing caches deleting volumes anything starting rm rf
delta down color var toby accent
delta flat color var text muted
delta up color var toby stable
demanding a runtime consequence of it would therefore drop every real one or invent a consequence for it
dense reference grid of cards high information density multiple kv pairs per section several columns
dense slides multiple cards per slide several key value pairs tables
dense table of contents
density itself carries the authority
density of usememo doesn t track render frequency
density over whitespace slides and dashboards run dense
dependencies and a tiny bit of work on every render
dependency injected scoped connection is invisible
dependency is to remove it from your critical path
deploy config is the set of values that change between environments database urls service endpoints credentials log levels pool sizes feature service handles
deploy varying config is separate database urls credentials log levels pool sizes timeouts that differ between environments
deprecated until callers migrate and proto field numbers are never reused
depth is the goal and small functions serve it only when they deliver it
derived view adds an entry there
deriving from one table is a deliberate design decision and the decision is
describe behavior at the public interface inputs go in outputs come out observable side effects show up where callers would notice them
describe purpose rationale constraints and structure
described as first read then parse the tell
describes ordering call x first then y
describes the data flow into the screen and what happens
description apply toby s artifact design system whenever a visual artifact is being produced an html or react page or widget an svg or html widget a diagram image graphic chart dashboard slide deck mockup printable reference card or the copy inside one
description brainstorm by widening from a single example to the broader set behind it
description collaborate to build a single file html simulation game or toy in toby s style a real system with its shortcuts named out loud deadpan comedy where there is a target a paper and ink look across canvas 3d or dom and an ending that reads the run back when the game has one
description this cannot be undone
description use when the user asks for an explanation walkthrough rationale trade off architecture note or teaching on any subject code math a science a language literature medicine while the work or study continues
description use when the user wants to learn or be taught any subject while working through it code math a science a language literature medicine practicing drilling contributing the next piece of real work or being quizzed on what they should retain
description use whenever writing or finalizing output that carries toby s voice a substantive reply code findings a commit message a pr description a doc a comment a plan or any generated artifact and whenever the user says voice toby voice or voice pass or asks for a rewrite banned phrasing or tone repair or wording help
deserves a toast that s one configuration change in one place
design each boundary to serve more than one caller and stop one named variant short of speculation
design first measure second optimize third
design it twice option a keep one component push the knobs to sensible
design the callable surface before the implementation
design time moves are
design what the change cost the next person to touch this and which document it left wrong
destructive confirm info and exposes the differences between them as
destructive red default blue success green
destructive request name the target delete the next build cache
destructure a positional tuple and remember what each position means
detachedinstanceerror are part of the contract
detachment problem are all internal
detachment semantics returned order is bound to a session that just
detail screen for the product with this id
detail that tracks the code belongs in code comments where it s next to the thing it describes and gets updated when that code changes
detailed text that no longer holds misleads every reader who trusts it
details caller safe text bound to reason never internal diagnostics
detects threshold breach within 50 ms
determine whether a cache is a deep module or a maintenance liability where
determines what s possible
deterministic means controlling time and io and pinning randomness to a recorded seed so the run reproduces
diagnose a weak argument by its weak link missing evidence evidence present but unconnected or a faulty warrant
diagram led the visual chart diagram animation dominates each panel
dialog alert variant adds a 4px red top strip and red title
dialog covers modal and alertdialog backdrop centred panel
dialogs to prevent data loss
did a rule vanish while text moved text moved between files reads as a tidy diff
did anything get committed to a number a position a refusal a next step
did the migration run not yet
differ but the module principles don t
different codes for the same condition
different error type than getuser
different layer check and it is an unusually costly one
different seams different slices
different stacks same checks
difficulty is structural before numerical six 62 rounds finish 5 of the time no matter the tuning because the odds multiply as p n
direct manipulation the learner drags rotates or repositions an element in a diagram
disable prev on first slide next on last
discarded candidates can stay in the experiment notes
discovery slicing the plan the operator approves approval stops the behavior record
discriminated union with an exhaustive switch when branches read different fields
discrimination someone who understood and someone who only felt they did give different answers
discriminator names the four legal combinations loading success error
display flex flex direction column gap 8px position relative
display flex gap 12px padding 12px 16px
display grid grid template columns 1fr auto gap 20px align items start
display inline flex align items center justify content center
displays the system biometric ui and returns a typed result
dispose geometry material and label textures on teardown
distinct subjects authentication is one body of knowledge authorization is
distributed locking move inside the cache module where they re written once
distribution histogram kernel density box violin
div class alert alert watch
div class alert body retry volume sits 18 above baseline
div class alert title watch retry pressure div
div class dec actions
div class dec body
div class dec meta owner mission ops sayo confidence high ci 95 cadence two sample rule div
div class dec title approve controlled release of m 01 v4 2 div
div class ds eyebrow ds eyebrow quiet contents div
div class ds eyebrow ds eyebrow quiet convention div
div class ds eyebrow ds eyebrow quiet owner div
div class ds eyebrow style margin bottom 36px reference deck r 04 2 div
div class ds footer span class ds footer id 01 14 r 04 2 title span div
div class ds mono style font size 13px margin top 6px 14 slides 18 terms 8 symbols div
div class ds mono style font size 13px margin top 6px mission ops sayo div
div class ds mono style font size 13px margin top 6px si j2000 right handed div
div class pin style top 40px left 120px
div class stat delta stat delta up 0 03 c vs 24h ago div
div class stat grid stat grid 4
div class stat label drift observed div
div class stat source sensor s 1 n 240 div
div class worked body orbital period is approximately 92 8 minutes div
div class worked body period of a circular orbit follows kepler s third law t a with 3 986 10 m s div
div class worked body substitute a r h with r 6371 km and h 420 km div
div class worked caveat caveat assumes circular orbit j2 perturbations not included div
div class worked math t 2 a div
div class worked math t 5568 s 92 8 min div
div class worked row worked row derivation
div class worked row worked row premise
div class worked row worked row result
div class worked step derivation div
div class worked step premise div
div class worked step result div
div classname slide slides i render div
div size width size height div
div span class stat value 0 31 span span class stat unit c orbit span div
div style fontfamily var font mono fontsize 14 margintop 6 value div
div style fontsize 11 letterspacing 0 12em texttransform uppercase color t textmuted label div
div style position absolute bottom 80px left 80px right 80px display grid grid template columns 1fr 1fr 1fr gap 48px
div style position absolute inset 0 display flex flex direction column justify content center padding 0 80px
div style position absolute right 120px top 50 transform translatey 50 opacity 0 16
div style position absolute top 64px left 80px display flex align items center gap 16px
div use cb rootaction
divergent change diff visible one class or module gets modified for many unrelated reasons in the same change a sign it carries more than one piece of knowledge
do it in the same pass
do not agree a plan in scrollback because nobody can check it off
do not ask what genre ask two things
do not count a large volume of working code as progress when each piece adds a special case or a hidden dependency
do not default to the minimal diff which rots a codebase one reasonable looking commit at a time
do not drop it
do not drop words to get under it
do not force a clipped reply when the moment does not call for it
do not have before the deadline
do not invent foils
do not let a refactor sprawl across the codebase
do not lift it to a parent or store
do not reach for a rarer synonym in the same slot because that produces stilted prose
do not reach into internals to make it testable
do not restate the obvious
do not settle for a vague name
do not ship on assumed equivalence
do not treat it as discipline
do not trigger for a plain text answer or explanation in chat where no visual was asked for that prose belongs to toby explain or toby learning
do not trigger on general brainstorming language like help me think through this let s explore or brainstorm with me this skill waits for explicit invocation
do not trigger on general requests to make a game a sim a toy a visualizer or a simulation and do not trigger when a game is only mentioned in passing
do not turn a local placement issue into a module tree redesign
do not write them
do the design pass first see toby swd strategy and toby swd modules then write tests against the interface you settled on
do the same for generated docs updates broad validation codegen and repo wide maintenance
do they share a design decision knowledge
do you want me to keep going go
doc headings variable and test names
docs behavior md or whichever file this repo already keeps for stated behavior
docs for a later one are a guess
document at the point of surprise
document each decision once in the most obvious place
document the surprise at the exact point a linear reader meets it and again where they would otherwise act on the wrong assumption
does a description edit change what fires a widened trigger noun loads a skill on tasks it should skip
does each resulting interface beat the original s
does it avoid becoming a god interface
does it remove duplicated nontrivial code
does not cache the result
does not mutate text
does not produce one
does not serialize complex types beyond json non serializable objects
does this rule contradict another skill quote both at path line
doesn t track derived reads
doesn t trade the finder explosion for a different problem
doesn t wait staring at a blank screen
dom the palette carries a board of cards and panels and the red is the deny stamp
domain knowledge what s stored in what layout what versions exist belongs
domain order is a plain class
domain user objects with all relations populated as needed
domain user only carries what the rest of the system needs to know about
don t animate automatically
don t assert on private state
don t bury it under environment setup
don t cache it
don t conflate the two artifacts
don t create one for a leaf folder a single file utility directory or a folder that exists for file organization alone
don t create one for a leaf utility an internal helper or anything not meant to be consumed outside its immediate author
don t error hunt
don t flag an interface known to caller and implementer a dispatcher and its handlers several implementations of one interface or a decorator and its object
don t flatten a tiered api into one list
don t flatten everything to the same density
don t hand down the meaning as settled
don t hedge a known claim or strip a qualifier from an uncertain one
don t improve an existing convention casually
don t index for measured query patterns only
don t inflate a trivial helper into artificial depth
don t inflate a trivial helper into artificial depth to dodge the label
don t invent a third layout
don t invent separate components when the existing primitive already covers the case
don t know any of that
don t let both sides write the same fields
don t let it happen by accident
don t let it repeat draw content without replacement reward varied play and punish the streak and let the world s reactions escalate as the run goes
don t let the implementation quietly widen the contract
don t make a beginner fish for a form they have never seen
don t report a point estimate without an interval if the interval is known
don t restate the code back because they have the diff
don t rewrite a test to make a cleanup pass
don t ship it
don t ship that sounds like a person
don t skip to
don t sweep up other processes that look similar
don t take it
don t try to work around it
don t turn a plain question into a built artifact on your own
don t turn a plain question into a built artifact on your own
don t use arial times or helvetica
don t verify that helper x was called before helper y unless that call sequence is the observable contract which is rare
don t wrap around
done means the bot bands hold the invariants hold both orientations play live and reduced motion is honored
donut charts use bar
downsides of global state
downstream doesn t pile up threads
drag coefficient for this body shape is not measured here estimate 25
draw flavor without replacement so nothing recurs
draw from the mission ops domain instead
draw the rest from a muted set teal 2e6a78 green 4d7a30 amber b07020 deep rust 7a2d22
drawing out past the learner s reach is stalling and an adult on a deadline feels it
draws the eye hard so use sparingly
dread the red stops meaning you did this and means already here it arrives stays shows up where the player did not act unlabeled
drift posterior 0 73
drift posterior trending high defer decision until two sample rule passes
drilling one item smooth before moving on feels like learning and predicts forgetting
drop a hunch you couldn t prove because it doesn t belong here
drop shadows glow gradients
drop user scalable no for a reading heavy dom game that wants pinch zoom
drop what you can t prove
drop yes no multiple choice does that make sense and anything answerable from the last three messages
dropping a column added on the way up loses whatever was written to it
dropping a section is a claim you checked it and found it empty
dropping or narrowing one changes what the user asked for
dsn estrack goldstone madrid canberra cebreros pass length x band s band rssi ber link quality ground station ascending node elevation max 48 fold back to s band
duplicated documentation is as dangerous as duplicated logic and worse than absent documentation
duplicated knowledge diff visible one rule or decision written out in two or more places so a change to it has to land in all of them or they drift apart
durable implementation everything else which is the rest of this file
durable work that stalls on a value no amount of reading can settle drops into toby swd experiment for that one question then returns with the answer
during a user led loop automated checks that add latency need approval
during discovery and deleting or folding in the experiment once the user
during discovery user feedback and manual observation may be the validation source
e instanceof httperror e status 500 e status 600 e status 501
each addition looks small and the total weight shows up only in retrospect
each almost free and the slow render disappears without any memoization
each also gets one name per thing held identical across every chart in the artifact
each archetype re lays out a twitch game puts live controls under both thumbs with the scene clear between them a manager sim pins the speed knob and the buy verbs and tabs the browsing away
each call has a few milliseconds floor
each card section number mono muted section title tagline meta
each case owns a client a policy or private state set is open
each cb filtered as p i
each channel owns its client its address lookup and its own answer to is
each comment is one to two sentences
each contributes distinct functionality despite the shared structure
each cost is real so pay it when it s earned
each costs three to five words to unpack
each criterion in its pre code wording marked met with the check that proved it or unverified with why
each decision needs a rationale field and a named rollback owner
each deck gets a per artifact logo a hairline geometric primitive one short lowercase inter 600 word naming the topic a single red dot as the only color
each encodes a real distinct intent
each entry is the pattern what it sounds like and the move that replaces it the plain sentence the pattern was hiding
each example shows a real pattern you ll meet in
each example shows the tactical version the smallest change that works and
each handler module calls registerhandler at load
each image is just an image together with
each implementation is a module
each implementation registers itself
each interface is the narrowest one that supports its function
each is a common source of leakage and
each is a follow up none is in this plan
each is a small component
each is deep behind a small interface
each is one body of knowledge the platform s
each load contends with 999 others
each mark clarifies a claim that lives next to it
each mark has one purpose
each module s interface comment is now two or three sentences
each named dialog composes one deep core under the hood
each of these needs a change this skill s out of scope note already excludes a signature a public return type or which module owns what
each one contradicts a position toby swd modules toby swd clarity or toby swd interfaces already states for this repo depth over line count comments as the legitimate carrier of what code can t say knowledge gated duplication interface first design
each one doubles the paths through it and nothing tests the combinations
each one edits send
each one should tell the reader something a generic version leaves out
each one shows the criteria in full the slice order and its name the wiring line that proves the slice is reachable and where a stop earns its place
each operation has its own message and is its own rpc
each owning module to clear its data and the question what should be cleared
each panel or slide earns its own breathing room
each parameter forces every caller to answer a question
each pass has its own bar and writes its own findings so the fourth one gets the attention the first one did
each read is a hidden dependency in a module that otherwise looks pure
each renders the common case with zero required decisions beyond user
each round trip is a few
each row component re derives a sorted copy of the list
each rpc has one effect that the
each site writes its
each slice is now a deep module
each slice owns one body of state and the
each stays out unless a criterion names it and that criterion s source line quotes the user or the ticket
each stays out unless a criterion names it and that criterion s source line quotes the user or the ticket
each step component renders its own content and fires transitions via
each step updates the diagram and adds an annotation
each test has a definite answer which is why it gets run and a fifteen item checklist does not
each ttl is documented with the reasoning
each type lives in the module that owns the decision it represents
eager load via the orm cheapest tell the orm to join when fetching
eager load with hints or purpose built read model
eager loading is the naturally efficient simple choice
early return or predicate extraction the same side effects run before the return and the extracted predicate has none of its own
earn the good line through structure a barrier to clear a plateau that breaks only when a real move is found a surprising regime that is reachable and never free
earned and the system stays simpler to operate easier to test and clearer
ease out cubic bezier 0 2 0 0 1
edge case escape hatches skiptaxvalidation partial failure semantics
edit api invites ts add createinvite orgid email actor returning the
edit api routes ts register post orgs id invites
edit screens members tsx add inviteform and a pending section reading
edit where the quick patch adds a special case or a hidden dependency
edit which toby code review owns
editing files inside the planned scope is fine
effect slightly slower than no cache plus all the operational
effectiveness none of which existed before
efficient choice from the skill same complexity as scrollview much
effort well past that band has crossed into the big bang redesign anti pattern
either active is a real database column with a clear definition or the
either retry on transient failure nope
either the behavior was stated and never proved or the test was deleted and took the requirement with it
either way a criterion whose check cannot run at all says so before anyone approves it
either way it s one decision in
either way tx does not
elaborate recovery for a rare unrecoverable error that should stop in its safest state
elev card 0 1px 0 var border hairline
elev hairline 0 0 0 1px var border hairline
elev high 0 12px 32px 10px rgba 28 31 35 0 24 0 0 0 1px var border hairline
elev low 0 1px 2px rgba 28 31 35 0 06
elev mid 0 2px 8px 2px rgba 28 31 35 0 10 0 0 0 1px var border hairline
elev pop 0 6px 24px 8px rgba 28 31 35 0 18 0 0 0 1px var border hairline
elevation pick the lowest layer that solves the problem
ellipse cx 32 cy 32 rx 28 ry 10 stroke var toby ink stroke width 1 2 transform rotate 22 32 32
else if e instanceof autherror navigate login
else if e instanceof notfounderror navigate 404
else if istimeout e
else if query error
email invited by created at expires at accepted at
email string db email json email
empty input and timeouts untested saying done when it s untested is just a lie
empty state dashed hairline border info teal geometric icon title body optional action
empty string not set distinction impossible without field mask
encoded in every component
end each pass with a short status and the next dial to turn
endpoint get api products id
endpoint is too slow the work is somewhere else optimize
endpoint with 10 query parameters
enough that the memo will hit
enough to produce occasional stale reads under load while delivering
ensure it is absent
ensure no session with sid exists
entries tagged diff visible are provable from the file list and the diff hunks alone including any function body the diff adds or changes
entry is a heading holding the behavior in one sentence trigger plus observable result at the public interface toby swd testing tests through naming no function file or internal state
entry to evictderivedfrom in one place
enumeration widens while yes no narrows
environment keep it as an optional argument with that computed value as the
equation block centered mono formula with a 2px info colored top accent
err db transaction func tx gorm db error
err db transaction func tx gorm db error
err db withcontext ctx transaction fn
err withretry ctx db func tx gorm db error
error apierror null set only on transport http failure never on 404
error boundaries async error handling render perf memoization virtualization
error ladder retry transient at api layer route boundary for unexpected typed result for not found
errorboundary fallback hardcrashscreen last resort full unmount acceptable
errorboundary fallback routeerrorscreen per route boundary
errors and lockout handling all live inside
errors arrive from three
errors is err errconnreset
errors is err errserializationfailure
errors retry define out non errors cancellation becomes a status and
errorstate err query error
esc and overlay click are enabled and treated as cancel
esc closes tab closes and confirms
escalate each beat out scales the last the pothole gets logged then a caseworker then standing in court and a beat that does not climb gets cut
escalate to the design it twice loop when the interface is consequential
escalate too when a routine interface s first comment fails because that failure means the abstraction is wrong and earns one more try
escape hatches in the surface
estimate from access patterns under 50 rarely justifies caching
estimate the hit ratio a cache that s invalidated on every
estimate the staleness tolerance some data must be fresh
even a silent toy carries the voice in its labels
event driven invocation is
ever used by the one above it none independently meaningful
every acceptance criterion against the diff
every behavior record sentence against the tests
every call to keep them atomic
every caller catches and retries http errors
every caller free of it
every caller now handles every http status
every caller now owns
every caller that formats a name now depends on this branch
every claim number or comparison links to a source a measurement or a mechanism
every component re implements cancellation on unmount error semantics
every consumer of user is now coupled to schema columns json wire format
every consumer reads this paragraph
every criterion below carries all three lines because a criterion missing one is the thing the skill tells you to drop
every durable feature loads toby swd strategy a full design pass before any code on strategic or greenfield work its brownfield read and reactive investment pass on tactical work
every entity has behavior in every state including the one where nobody wants it a loose ball rolls and decays until a paw grabs it
every entry below is either
every entry s exception makes a review here match what toby swd modules toby swd clarity or toby swd interfaces would call fine if an agent had just written this code
every example is single use written for one moment
every extra module is an interface to learn more pieces to track and a new risk of duplicated logic
every field on every message is required to
every flag that fired goes in the handoff at file line under its entry name including the ones you fixed on the spot
every for you says what blocked you
every function nobody could place is in it
every heading is quoted verbatim in a test
every key that uses this pattern
every label and description is inter
every label earns its place or it is cut
every later domain case reopens this code and parallel chains drift apart
every lever feeds a formula a button an upgrade a shenanigan moves a number the resolver reads or the player clocks it as decoration within minutes
every line it does print is a sentence the product claims and no test defends
every line it prints is a sentence the product claims and no test defends and each one gets named in the handoff
every method along the chain takes a tx argument it doesn t use directly
every method appears so the interface only
every middleware knows about fields the previous ones produced
every module below main ts takes db logger and gateway as constructor arguments
every new call site is another chance to get one wrong
every new file kind edits this function
every one that fired goes in the handoff at file line under its entry name including the ones fixed on the spot
every parameter a caller must manage is overhead distributed across every future call site
every plan step against what landed
every query filters on tenant id first so the planner takes the tenant index instead
every read tries the cache cache returns null almost always
every reader also repeats the same facts about thing its name that it is a string that it holds a url its default
every render error anywhere
every request now waits for a free connection
every rpc client method wraps its call in it
every rule here is already the operating guide s the scope below included
every screen reimplements the same offline timeout unknown error handling
every screen takes user in its route params and forwards it deeper
every screen that uses storage gets a copy of
every screen that uses this module
every semantic hue has a three step palette
every sentence in artifact copy must pass cite negation and substitution then survive the reader skim cut
every sentence must pass all three
every sentence panel or slide must do one of these
every sentence stays true and complete with the tone stripped out
every slide has in order
every slide is one tone
every special case a caller must branch on is complexity
every toby artifact deck opens with a title slide and closes with an end slide
every transactional call site uses withretry
every transition resets what it depends on
every value below references tokens defined in skill md
every value gets a unit 312 is a defect
every value has a unit
every word must add information drop redundant type or class name words fileobject file no hungarian notation
everything below lands easier once the reader holds this model
everything else auth recommendations current user
everything else receives values from it
everywhere even though only one migration cared about it
evict for never editing fifteen services
evict the right keys
evidence card ink on paper slide
evidencetable tbl with num class for right aligned mono columns
exact equality assertions flake or lie on such outputs
exactly the book s hidden control flow case and react effects are its modern form
exactly the fields needed produced by one query
exactly the fields the list view needs
example 2 retries a conflict once the database raises one
example claim line bound orbits are ellipses
example orbit marker uncertainty fan coordinate stamp 34 05 n 118 24 w
examples a service a bounded domain package a frontend feature a design system package a store module
except through one of those wrappers
except to pass to the next call
exception a broad interface whose implementers each use the whole of it or a standard library capability interface is fine
exception a class with one cohesive responsibility that legitimately needs many small methods like a serializer with one method per field type is fine
exception a comment describing something that didn t change like an invariant that still holds or a unit that s still correct is fine
exception a comment describing something the diff left alone an invariant that still holds a unit that s still correct
exception a dispatcher and the handlers it selects between several implementations of one interface a decorator and the object it wraps or an interface known to both caller and implementer are fine
exception a fluent builder or a functional pipeline map filter reduce a query builder where each call returns a new value of the same conceptual type is fine
exception a function that s uniformly low level throughout a tight numeric loop a parser is fine
exception a hunch that something elsewhere might describe this doesn t clear the bar
exception a layer with a real near future need for a stable entry point is fine
exception a method whose whole declared job is combining or comparing data across objects a comparator mapper reducer aggregator is fine
exception a parameter that selects among values within one coherent operation is fine
exception a partial implementation the type system marks as partial or a caller visible capability check such as supportsx that callers are expected to call is fine
exception a primitive used in exactly one place with no duplicated logic elsewhere is fine
exception a record kept deliberately as plain data with its behavior owned by another module is fine
exception a self evident value in a narrow obvious scope is fine a loop s 0 1 1 an http status checked once inline
exception a self evident value in a narrow scope a loop s 0 or 1 an http status checked once inline
exception a signature where each parameter is independently necessary with named or keyword arguments keeping the call site readable is fine
exception alerts toasts use a single 4px coloured left strip from the callout family and that strip is the documented pattern
exception an interface covering today s needs plus one or two named near future variants is fine
exception anything exported or public that code outside this repo might consume and anything reached through reflection dependency injection dynamic dispatch or a test only harness
exception anything exported or public that consumers outside this repo might use or anything reached through reflection dependency injection dynamic dispatch or a test only harness is fine
exception blocks that share text while answering to different reasons to change stay separate however identical they look
exception constructing a plain value or a pure helper is fine
exception handling and performance optimization are two of the largest sources of complexity in any codebase
exception n edits that are independent facts which happen to co occur like a changelog entry a version bump and a config update landing in one pr are fine
exception one exhaustive switch over a closed discriminated union guarded by a never or unreachable check or a single lookup map dispatch is fine
exception only flag what you can name a specific file line and the exact clause the diff makes false
exception output parameters are the idiom for result err style returns a documented caller provided buffer used for performance or a method whose whole declared job is mutating the receiver it was called on
exception placement where the two things share real knowledge passes toby swd modules decompose by knowledge test or placement that follows an established repo convention is fine
exception small utilities are sometimes unavoidably shallow and that s fine
exception some small utilities are unavoidably shallow
exception when a path has a stated budget it must meet to be correct a latency or memory limit a frame deadline a no allocation policy a known hot path treat that budget as a design input and build to it from the start
excludes pending verification is the day every site needs review
exhausted because queuing forever hides the exhaustion as a hang
existing gateway code stays untouched
existing module that adds no new boundary
existing paymentprocessor hardcodes one gateway
existing steps stay unchanged
expected hit ratio 80 product catalog changes hourly reads are constant
expects the caller to coordinate them
expensive re running this scan on every keystroke is the bottleneck
experiment code left behind after selection
experiment on the operating guide s experiment triggers spike proof of concept throwaway compare options tune it while i watch
expired token is rejected
expired token isn t mistaken for a transient error
explain a mathematical function against a function they ve written its domain and range against the parameter and return type
explain first usually a missing index or bad query plan
explain from the worker s seat name the decision say why it wins here name what it costs keep moving
explain in chat against the code or the material in front of you
explain the broad command then offer the narrowest check that protects the touched work
explain the decision and the tradeoff it creates
explain why it matters here
explicit visible error handling for a failure that can happen
expo projects or react native fast image bare rn so off screen images
export const config object freeze
export const useauth create authstate set
export const usecart create cartstate set get
export const usetheme create themestate set
export default function deck
export default withwindowsize withcurrentuser withtheme mycomponent
export function authprovider children propswithchildren
export function dispatch message inboundmessage promise void
export function registerhandler type string handler messagehandler void
export function usebarcodescanner opts oncode c string void
export function useonlinestate online offline unknown
expose a warm key value
expose and it s where typos and version mismatches happen
expose enough state for the user or agent to inspect the result
expose the current inputs outputs candidate values and selected state
expose the platform api verbatim
exposed vs intent methods
exposes the state and trusts every caller to guard it
expunge detaches the user from the
extend it to orders both pass
extending a class to inherit behavior is the costly form and mixins hooks and helper objects are usually better
extension rules and invariants where new files belong conventions that must hold patterns to match which upstream or downstream modules a change here affects
external deadline accepted cost
extra hazards specific to navigation
extra protocol detail field mask became a caller obligation and the
extract it as a general purpose helper
eyebrow h1 short paragraph at the top describing the deck s scope
eyebrow label 11 pt 0 12em text muted on top
eyebrow per column naming the term cluster
eyebrow uppercase 0 12em 11 pt mono t textondarkmuted at top left section 03
eyebrow uppercase 0 12em 11px muted section name and slide id for example 05 17 numerology
f x x sends 3 to 9 and 3 to 9 as well which means two different inputs are allowed to share an output
factor code together only when the instances share the same knowledge so a change to one should change all
facts anatomy terminology pathways recognize then cloze the term the learner must produce then free recall the set in order
fail performance improved significantly
fail the method is fast reliable and well designed negation the method is slow fragile and arbitrary nobody writes that negation the original is empty
fail this dashboard unlocks new possibilities substitute this stapler unlocks new possibilities still works meaningless
failed null as skip is the same problem as the untyped map and the
failed untyped map pushes type errors to runtime that a typed surface would
fails all four tests
fails one run in nine
failure looks like to the caller
failure named at multiple levels
failure named invalidation is a property of the data but it s
failure named one component bundles three distinct intents confirm
failure named route params carry runtime state when they should carry only identity
failure named the contract leaks positional layout the caller must memorize
failure named the hook exposes internal state management decisions as
failure named the slice exposes the state layout and leaves the operations to
failure named this is a grab bag
failure named this is a sqlalchemy bindings layer pretending
failure named this is a wrapper around asyncstorage dressed as a module
failure named tx is a pass through variable that every method must
failure rolls the transaction back so the snapshot the body computed against
failure where they cannot know if it exists
faint geometric backdrop behind the claim at opacity 0 12
fake precision sounds like this improves performance by roughly 40 with nothing measured
fake the channel or simulate the environment and test each side against the contract
fake when the real thing is slow external or hard to set up a hand written stand in with realistic behavior
fall through with metrics outage is the cache s problem
fallbacks apple system sans serif for inter ui monospace monospace for jetbrains mono
false if the client is over its limit
family tree on top of those for shared behavior
faster than nothing performance work with no measurement behind it
faster than the real thing more faithful than a mock
fastest path from slow to added redis involves no measurement no
fat interface diff visible an interface or abstract type carries methods a given consumer never calls so every implementation and every test double must satisfy surface that path does not use
feature card a concrete behavior the measurement that backs it
feature envy a method that reads and calls another object s data or methods more than its own doing that object s job from inside the wrong boundary
features checkout components pricerow is a leaf component folder no file
feed changed arithmetic and changed predicates the inputs a read glides over negative zero empty and the value either side of every boundary
feedcache owns its versioning and the
feel free to reach out the move stop at the last useful sentence
feels random feels unfair agency over dice surface the gamble references gameplay md
fence the pure logic state sim resolver rng behind a clear marker so a one line slice lifts it into a node test
fetch api feed headers authorization bearer token
fetch api feed q query headers authorization bearer token
fetch api products id
fidelity real model through openly vibes based
field label fieldset form structure wrappers
field the class is now deep a few intent methods substantial invariant
field type to input a form that renders from a schema maps field type to a component
fields build a query that returns them directly
fields that are set in the request are
fields with non obvious units ownership null meaning bounds side effects or invariants get comments
file edit following a pattern already in that file a review or cleanup pass over code
files or state changed outside the immediate scope
files to know and the responsibility each owns a short annotated list of only the files a new agent must understand to work here
fill none stroke var toby accent stroke width 1 25
filter client side when the set is small
filter criteria value object on the wire
filter expressions pass user email x y style filters
filter fields are optional and and d
filter item t query string boolean
filter item t query string boolean default substring match on getlabel
filter o o customer tolowercase includes filter tolowercase
filter order customer id customer id
filter sort in render
filtered map o orderrow key o id order o
filtereditems reflects the current
filterinput value filter onchange setfilter
filterinput value filter onchange setfilter
filters are and d unset filters are
finally await mutex release featured lock
finally if cancelled setloading false
financial data for instance
find id string promise order null
find id string promise product null
find id string promise product null
find id string tx transaction promise order null
find method per filter combination
find one and find many accept sqlalchemy
find one design imperfection in the code you touched an obscure name a leaky abstraction a dead branch a comment that lies duplicated logic
find the real thing to aim at insurance is a fog bank
find the weakest link in this argument theirs or one you hand them
find where the time goes is it a database query
findusersbyorg orgid string users user error
findusersbyorg orgid string users user error
fingerprint in which case authenticate will throw
fire it on the beat and gate every line on a real change a number moved a threshold tripped a thread advanced so an idle moment stays silent
fires when a changed line chains three or more navigations through different objects accessors to reach a final value or call
fires when a changed line checks against multiplies by or branches on a bare literal with no obvious meaning from the surrounding lines or the same literal shows up at two or more sites for the same concept
fires when a changed line compares against multiplies by or branches on a bare literal with no obvious meaning nearby or the same literal repeats at 2 sites for one concept
fires when a changed line or block behaves differently now and the comment immediately above or beside it visible in the same hunk still asserts the old version
fires when a changed or new function branches on a boolean or enum parameter near the top of its body into substantially different code paths and the parameter s only job is choosing which path runs
fires when a changed or new signature has enough parameters that a caller has to check the declaration to get the order right or the same 2 3 parameters a range a coordinate pair an address triple show up together in multiple signatures in the diff
fires when a diff hunk adds a case or branch to an existing tag dispatch conditional or adds a conditional over a discriminant that another conditional in the changed files already switches on
fires when a method s body is mostly a chain of getters or fields pulled from one other object with little use of its own class s state and that other object could reasonably own the operation
fires when a new field or parameter representing a recognizable domain concept is typed as a raw primitive and the same parsing validation or formatting for that concept already appears at 2 other call sites
fires when a new or changed class or module s public methods are mostly getters and setters for its own fields with no operations that name intent
fires when a new or changed method s body does exactly that one call through same signature nothing added
fires when a new or changed module s public surface parameters methods required setup takes about as much explaining as its internals would
fires when a new parameter config option or extension point has no current caller using anything but its default and no concretely named near future use
fires when the concrete input or state that triggers it and the real caller route input or test it comes from
fires when the diff adds a general purpose constant or helper inside a specific unrelated class or module and nothing about that class s own job explains why it owns this
fires when the diff adds a method to an existing interface and an implementer in the diff fills it with a stub throw return null empty body or the diff adds a consumer that receives a wide interface and calls only one or two of its methods
fires when the diff adds inside a function or class that is not an entry point factory or designated config module a constructor call for a type holding network disk clock or external service state or a direct environment variable read for that unit s own use
fires when the diff adds or changes a function that writes into a received parameter as its way of producing a result where a return value could carry the same information
fires when the diff adds or changes a subtype or implementation whose method overrides to throw unsupportedoperationexception or notimplementederror narrows an accepted argument range or returns a value the base contract disallows nullable where the base is non null a different unit or meaning
fires when the diff changes a function s behavior return type error conditions or an invariant and a comment elsewhere found by searching for callers or the described behavior still asserts the old behavior
fires when the diff changes a function s behavior return type error conditions or an invariant and a search for callers or the described behavior turns up a comment still asserting the old one
fires when the diff changes what a line or block does and the comment immediately above or beside it visible in the same hunk still describes the pre change behavior
fires when the diff removes its last caller or a repo wide grep for the symbol name turns up only its own definition
fires when the diff removes the last caller of a function or class or a repo wide search for the symbol s name turns up only its own definition
fires when the diff s stated intent is one change but the edits inside a single file or class serve visibly unrelated purposes a pricing rule tweak and a display copy tweak landing in the same class in the same commit
fires when the diff touches many files with small similar shaped edits a field added to n structs a case added to n switch statements that all encode the same underlying decision
fires when the diff touches two or more modules to express what is really one decision the same knowledge duplicated across them when it should live in exactly one place
fires when the request holds words for a behavior the user s or the ticket s and the record doesn t state it yet
fires when two or more blocks encode the same decision the same validation rule the same format the same threshold and you can state the one reason that would change every copy together
fires when within one function in the diff some statements read as named steps of a plan and others are inlined mechanics that could just as easily be a named step
firing an out of process event that would commit independently
first baseline the current latency and find where time goes
first exposure of an item is short the item one anchor a meaning an image a cognate an etymology hook move on
first parse then validate then write that produces shallow modules and leaks one decision across many
first the interface is colocated with the implementation
first validates that the cart belongs to the user
five methods five different uses each describing both behavior and which
fix at the call site
fix by exposing the lower module to callers redistributing responsibility so the call disappears or merging the two
fix here correct the comment to match the new behavior a one line edit no boundary crossed
fix here delete it
fix here name it
fix here rewrite the comment to match what the code does now same hunk roughly the same line count
fix here the fix stays local and clears the countable win bar same as anything else in what to look for
fix it when the cleanup is small local and directly supports the change
fix per the decompose by knowledge information leakage and split merge checks the knowledge is the request wire format and it
fix stop the cache from serving last week s prices
fix the render loop call first
fix with a context provider at the navigation root
fix with a shared object
fix with a shared object between the endpoints or a context kept small and preferably immutable
flag don t fix the fix crosses a module boundary or changes a public signature
flag don t fix this skill reports and edits nothing
flag it only when grouping would cut what a caller has to track to call it correctly
flag it only when the diff visibly alters the exact thing the adjacent comment asserts
flag it only when the diff visibly changes the exact thing the adjacent comment claims
flag it only when you can state the input that triggers it and why no guard catches it
flag selector argument diff visible a boolean or enum parameter picks which of two different behaviors a function runs
flags exceptions or special cases the interface is asking the caller
flaky test passes on retry fails at random
flashlist v2 is a new architecture only
flatlist falls back to the index only when no
flatlist for small lists flashlist v2 for large or image heavy ones
flatlist scrolling drops frames
flatlist still ships in the box with no extra dependency and
flatlist virtualizes so only the visible items and a small overdraw zone
flip between dependencies hidden across them and no information hidden by any
flood of cache misses hitting the database is a different kind of bad day
floor the type and the hit targets for a thumb roughly a 12px body 15px panel titles 44px targets
flow state lives in checkoutprovider
fluency turn non blocking error let it ride and recast reply naturally using the correct form without flagging it
flutter dart provider riverpod plays the role of context here
flutter route params are arguments and the same
flux a particle field nbody an orbit sandbox gridlock a traffic sim denial of service a dom card swipe satire spot kicks a penalty duel raccoon syndicate a heist idle
focus 2px outside ring in toby info teal 2px offset
focus defaults to the cancel button
focus management are all internal
focus rings are info teal 2px outside ring at 2px offset
focus when the dialog opens pass primary for confirmations and
focused tests on the affected file lint on the touched directory type check on the affected package
fold it into the general case so no caller has to know it exists
fold the chosen behavior into the normal project path
font family var font mono font size 13px line height 1 55
font family var font sans cursor pointer
font family var font sans font size 12px color var toby info text
font size 12px font weight 500 cursor pointer
font size 13px line height 1 55
font sizes scale with viewport so use clamp for display sizes
font weight 600 font size 11px font family var font mono
footer index i total slides length section slides i section
for 3d pull a current three js as a module no bundler one file still
for 50 orders with 5 line items each 1 query to load orders 50 queries
for 500 users that s 501 queries to draw a table
for a beginner lead with input they can follow
for a bug fix whose purpose isn t obvious say why and reference the tracker
for a design decision that spans modules put it in one discoverable central place and point to it from the affected sites
for a message channel boundary the signature is the message contract the request and response types what survives serialization and the delivery guarantees
for a pr inspect the pr metadata and diff with the tools available
for a routine interface or when only one passed take the first passing candidate
for a routine interface write one design run the comment test once ship it if it passes
for addresses 50 queries for line items 250 queries for products
for any substantive copy work load references copy md
for anything beyond a one line change don t implement the first idea
for attempt 1 attempt
for attempt 1 attempt
for authority section dividers glossaries formula slides summaries decision points
for bars place values at the bar tips when space allows
for broken code run the narrowest test show them the red and ask them to read it before you touch it
for by holding a lock so the transaction stays short and touches rows in a
for consequential interfaces write the comment before the body the comment is a design tool the cheapest way to find out the abstraction is wrong
for consequential or exported interfaces write the interface comment before the body
for const id of ids
for const order of req body orders
for data that shouldn t be cached every read must be fresh
for decks identify which slides earn interactivity before writing any slide
for diagrams needing numbered callouts use the annotation pin pattern from references components md
for each one ask whether a parameter governs the concept
for every change state in one clause the edge you checked and how you know it held the covering test or why the edge can t fire
for existing code first learn the local vocabulary and comment style
for high cardinality update endpoints admin tools that legitimately edit
for higher volume systems this same logic moves to an event cdc stream and a
for larger apps this is what react query swr rtk query are designed for
for let attempt 1 attempt maxattempts attempt
for let attempt 1 attempt maxattempts attempt
for li in o line items another n 1
for li in o line items another n 1
for lists of images that scroll past the viewport pair with flatlist s
for live system state
for methods specifically the reflex to keep functions small is subordinate to depth
for mysql load data infile
for new architecture fabric turbomodules the bridge is faster but
for non critical dependencies the recommendations panel on a page that
for o in orders
for o in orders
for order in orders
for postgres specifically copy is dramatically faster than insert for
for reading tables diagrams definitions worked steps lists walkthroughs
for routine private helpers run the same test mentally and leave no comment when the surrounding code already makes the contract obvious
for scroll linked animation animated event
for such a path the binding number is the worst case under load because the average can hide a blown limit
for sustained offline state surface it once globally via the platform s
for that state and this page renders only the states it owns
for the dread register the feel inverts anticipation over surprise the unstoppable approach with no ceiling
for the list view
for the very large user uploaded image case display a full screen
for typescript and js the same rule holds
for user id in user ids
for values that are
for values units ids coordinates
for very large batches 10k rows in a single insert chunk insert in
forbidden emoji unicode dingbats
forbidden inner shadows glow multi layer ambient key shadows colored shadows
forbidden spring bounces scale up entrances parallax particle effects animated illustrations
foreground background transitions losing state
forever in the pool queue which appears as a hang
form checkbox list checkbox group
form is the default and the bulk form is the exception
form radio list radio group
form state below media queries intersection observers undo redo
format is the operating guide s
four commits on main are not on your branch and force pushing drops them
four cuts worked end to end brownfield and greenfield live in references examples md
four interfaces between the request handler and one database row
four kinds each with its own home
four kinds of feature cut into slices all four in one invented repo orders a members screen a billing module a search path
four questions asked early
four sentences no internals every caller question answered
four sentences or fewer for the whole entry point
fourth time this week the same file has beaten us
fragment accepting twelve init parameters costs a caller as much as the
frame a diorama from above or a clamped orbit for a system you study
framework demands one or two override points so resist building a deep
framing does the query object s interface make the common case easy
fresh eyes are cheaper than another hour
from applycoupon are returned as result the cart state is unchanged on
from cache when the lock holder finishes
from payments import gateway paymentrequest
from the cache configuration if unset
from the cost to benefit asymmetry
from the processor are advisory and arrive out of order
frontend form a component that takes another s prop contract and then drops or reinterprets a prop so code that swapped the two would break
full ink background 1a1c1f
full session line session t 04 12 m 01 v4 2
full signatures and behavior are in the interface comments in gateway py
full test suite for a three line change slow hides the relevant signal in noise
full width table with mono cells group cells with subhead rows or coloured family stripes
func admincheckout u adminuserlookup orgid string error
func c ordersclient getorder ctx context context id string order error
func checkout u userlookup orderid string error
func checkout u userlookup orderid string error
func istransient err error bool
func r eventsrepository evictderivedfrom ev event
func r eventsrepository insert ev event error
func retrygrpc t any ctx context context maxattempts int fn func context context t error t error
func rl ratelimiter allow userid string bool
func s eventsservice record ev event error
func s userserviceimpl getuser
func withretry ctx context context db gorm db fn func tx gorm db error error
function and caches the result
function assertnever x never never
function createcombobox t opts items t getid t t string
function homescreen route navigation
function hook composable rune components call it shared knowledge lives
function istransient e unknown boolean
function orderpage id id string
function orderpage id id string
function productcard product onselect product product onselect p product void
function productcombobox products products product
function productlist products filter products product filter string
function productpage id id string
function productpage id id string
function productpage props id string
function receive prop bundles that wire aria attributes and keyboard
function reduce state state e domainevent state
function renderpreview file filemeta reactnode
function renderpreview file filemeta reactnode
function tabs user navigation
function that returns a promise resolving after the fresh response
function usecombobox t opts
function usecombobox t opts
function usecombobox t opts items t getid t t string
function useorderscreen orderid string
function useorderscreen orderid string orderscreenstate
function useproduct id string product null error null boolean promise void p product promise void
function useproduct id string productquery refetch promise void mutate p product promise void
function withwindowsize p component react componenttype p windowsize
gate complexity on evidence if efficiency requires added complexity that is small and hidden behind the interface it may be worth it it s still incremental be wary
gateway charge req authorize and capture a single payment
gateway gateway from config
gateway py the gateway interface and its provider implementations
gateway refund charge id amount cents partial or full refund
gatewayname parsed data payment gateway
generality across unrelated purposes is sprawl
generated work keeps toby s voice unless you ask for another and a game is you asking
generation the answer cannot be read off the screen or passed with yep
generic codes go toothless
generic containers returning a tuple pair untyped object where the caller reads getkey or 0 with no semantic label
generic names data value result status flag count are a smell when the scope is non trivial
generic q4 revenue sales pipeline customer success copy makes the visuals look like a misfit
generic tutorials unless the user asks
genre theme and tone are the creator s to pick each run the example games show the range the style already covers
genre theme and tone are yours each time
geometry as decoration is allowed only when it clarifies
get and set are gone from the interface
get api users filter url encoded json page token page size 50
get api users org id active true role admin role editor
get form while the filter stays small
get it right and future changes land without rework
get it wrong and every change racks up interest a new flag here a parallel branch there
get key string promise string null
get longer and longer all components subscribe to the same store and every
get returns the cached value or null if not
get t key string promise t null
get the structural choices right where the
get them wrong and no amount
getactiveuser additionally returns an error if the user is inactive
getactiveuser id string users user error returns error if inactive
getid item t string
getlabel item t string default string item
getorload t key string ttl number load promise t promise result t cacheunavailable
getorload t key string ttl number load promise t promise t
getorload t key string ttlseconds number load promise t promise t
getting them wrong costs
getuser id string user error
getuser id string users user error
getuser id string users user error
getuser id string users user error
getuser returns the user
getuser returns the user with this id or a
getuserwithpreferences id string users userwithprefs error
getx setx makes the
git grep qf b test spec echo unproven b
git holds the change history
give each case an object behind a shared interface
give each one tender exact line fired when their flaw bites
give me the counterexample that would sink your thesis
give plot beats a priority lane that jumps the flavor queue or a growing feed starves the ending
give the reason and the new wording and wait for a yes before the next edit
give the reason when asked or when the answer would surprise them
global toby voice controls register
global toby voice controls register
glycolysis converts glucose to two pyruvate works because blanking two tests nothing
go by the sub task in front of you because a subject label misleads
go data oriented only where it earns it thousands of particles want flat typed arrays and one draw call
go s convention makes
goes from 2 seconds to 50ms
good code is self documenting is false correct that before you start
good names reduce the need for comments but they never remove it
graceful degradation in the face of cache outage
gradient fills under lines
grammar spot the form in input fill a frame with the form blanked produce a sentence that needs it
green on your laptop red in ci
greenfield build the dispatch from the start
greenfield builds it in brownfield offers it as a scoped refactor
greenfield designs adopt the split directly
greenfield no sibling feature no convention to inherit nothing nearby resembling the change
greenfield s extra work before the second file exists
greenfield so the design pass runs through toby swd strategy before any code and a plan file is written whatever the slice count
grep docs behavior md sed s while read r b do
grep for the identifier as a string and confirm export and visibility status before flagging
grep the identifier as a string confirm export status before you delete
group 1 is observed with the flag on
group 2 waits for a yes
group labels hint shortcuts c separators danger variant
groupby p p category
grouprenderer g groupheader name g
grpc defines explicit retryability via status codes
grpc retry logic at every call site
guard it with assertnever
guard the check that should stop it and why it misses quoted from the code or none searched where you looked before writing this line assume the code is right and you are wrong
guard the loop so one thrown error does not blank the screen
guardrail check did anything caller facing get hidden
guardrail check does any caller need to know the session was already
guardrail check this complexity is related to the transport s own job and it
guardrail did anything get hidden that callers need
guardrail did anything needed get hidden
guardrail don t pile the context with everything
guardrail don t shatter into so many slices that every component imports
guardrail eliminating masking or crashing is correct only when the information is not needed outside the module
guardrail if a caller must know whether the upload was durably committed before
guardrail only pull down complexity that is related to the module s function simplifies callers and simplifies the interface
guardrail rare cases legitimately need set without an associated load
guardrail the transaction body must be idempotent or fully transactional
h1 style font size 128px font weight 600 letter spacing 0 024em line height 0 95 margin 0 max width 1200px
had it raised nothing three lines and keep moving
hairline geometric primitive orbit square crosshair below the title 80 px stroke t textondarkmuted
hairline grid only when it aids reading 1px dashed 55 opacity border hairline c8c3b2
hairline rules and small radii 1px hairlines do almost all the dividing
half adopted convention same concept done two ways in one codebase
half adopted conventions are worse than either option alone because they destroy a reader s ability to draw safe conclusions from a familiar looking pattern
half built behavior a user can reach is a defect so a slice depending on a later one ships behind a flag defaulted off or waits
half built experiment code is still experiment code until the user says otherwise
half of failures are stub artifacts
half right say which half holds probe only the half that doesn t
hand down the real meaning as a verdict and the argument the learner was building becomes a fact they receive and the lesson dies there
hand it to toby swd experiment say in one line what would move it to durable and write no criteria no slices no record
hand manual steps to the user only for what you couldn t run labelled run by me or for you
hand rolled control flow the language has a construct for a manual index loop where an iterator or comprehension fits a manual null check where there s optional chaining or null coalescing manual accumulation where a fold fits concatenation where interpolation fits a manual close where there s a context manager or defer
hand the loop to toby swd experiment and stand down no criteria no slices no record no stops beyond that skill s own
hand written usememo usecallback is redundant
handler map by message type rung 2 for a closed set of message types
handleresize this setstate width window innerwidth height window innerheight
handlers set type handler
handlers to the input list and items
handles small to medium lists
handlex form and match it
handling for a ruled out condition error handling for a state the types or an earlier check already exclude
happens a 400 410 style page token expired restart paging so an
happy path works ran it once
hard rules against convergence
hard rules don t signal an error for conditions a well designed api would not produce
hard to decide here
hard to describe a complete comment for the entry point has to be long
hard to handle is not a reason to reach for it
hard to name red flag if no precise intuitive not too long name emerges after real effort the thing being named probably has an unclear or mixed purpose
hard to name test the abstraction under test is wrong or mixed
hard to pick a name signals unclear or mixed purpose
hardwired dependency diff visible a unit doing domain or policy work names a concrete external collaborator itself new paymentclient new s3client a direct process env x or import meta env x read for its own use an imported i o singleton
has bugs that only surface during incidents the worst time to find
has mfa true has logged in in days 30
has tuning parameters window size threshold half open timing that
hashedpassword string db hashed password json
hasloggedinindays returns users whose lastloginat is within the given
hasmfa filters by whether mfa is enabled
have phone number set to none even though the field type is
have smaller operational footprint than introducing a cache
header base url retry policy and offline cache live inside feedrepo
header uses border strong divider
headers the length header determines body size
heading would be the claim surfaces p95 breaches 200 ms
headings carry the claim body explains or qualifies
headings carry the claim state the conclusion in the heading
headings state the conclusion
headless guard browser only globals with typeof give every wait for click overlay its own phase that clears its buttons and when a drive reds isolate a minimal repro
heavier real world weight pulls the tone flatter
heavy commands skipped and the narrower thing run instead
heavy repo command offer the narrow alternative one test file the package s
height 2px background var stat accent transparent
help but with a longer ttl and a willingness to serve up to 1 hour
helper masking the transient cases
helper packages accept interfaces return structs
helper which is what keeps it out of 50 call sites
here it is needed
hero feature cards cta layouts are allowed
hero headline what the thing does
heroicons outline tabler and phosphor regular are acceptable fallback families
heterogeneous content where virtualization breaks layout for example a
hidden control flow event driven invocation callbacks effects
hidden injection no wrapper chain
hidden n 1 is now an explicit join and callers that don t ask don t get the
hidden scratch work in a production path
hide what callers do not need and surface what they do
hides downstream failures from operators the breaker has tripped so
hides the loop but every call site still chooses and can mis choose policy
hiding any of these to make the interface look smaller is its own defect and it produces modules nobody can use correctly
hiding complexity is the goal with one hard limit
high hit ratio tolerant of seconds to minutes staleness or plainly
highlight new or signature slides by recolouring their section number to the relevant accent
hit rate miss rate miss path latency all as metrics
hit rate stays low the work it was protecting is no longer slow
hold defer the action and gather more evidence
hold this skill in force for the rest of the session once it loads
holding all of them is the god context anti pattern same problem as a base
holds a map of key promise t for in flight loads
holds the cart state
hollow outro sounds like let me know if you d like me to elaborate
home is a prose file the repo already keeps for stated behavior
hook owns the state caller reads it for rendering
hook return types component prop contracts store slice interfaces
hooks composables runes server state store slices headless components
hooks useonlinestate ts uses react native community netinfo
horizontal band defined by a min and a max
horizontal stripes of equal width
hot path stays fast even during the refresh and users see slightly old
house rules govern how toby talks to you while building
hover and base must differ by 3 1 contrast wcag 1 4 11
hover dark bg lifts to ink hover 4a4e57
hover paper bg steps to toby paper 2
how data crosses the relevant boundary
how do i know it s working
how fresh must the data be
how options look how the trigger looks the popover style
how to choose the timeout typically a few multiples of the p99 latency
how to think about and use the module
how to use it the minimal working example
how user sessions are stored and validated how the cart total is computed a description that is an order of steps is temporal decomposition
how we talk to an external processor
how you explain shifts with the material
html deck react jsx artifact
http edge cdn reverse proxy
humans using or maintaining the module
hunt for the guard that makes your finding fake then say why it still fails
hurting users today the circuit breaker is speculative complexity
i can t tell from here whether that is safe to delete
i changed three files
i don t know step down to a hint or a smaller sub question
i don t know why this is slow
i fixed the handler and the four failing tests now pass
i have no idea why this even works it works by accident
i have not measured that one so treat it as suspected rather than confirmed
i have not tested the empty list or a slow network
i know this code is ugly sorry it is ugly and it works and three things call into it
i told you the index was unused
i would guess the join and i have not profiled it so that is a guess
icon xs 12px icon sm 14px icon md 16px icon lg 20px icon xl 24px
icons are 16 20px 1 5px stroke square caps monochromatic currentcolor
id concept section mechanism render
id cover section index render
id fin section fin render
id string db id json id
id title section title render
idempotency key ord 123 attempt 1
idempotency key the same order id attempt combination will return the
idempotent method get put delete or a request carrying an idempotency key
identifiers can be long and specific priceincludingcheckoutfee ships if that is what the field holds
identifies the subject of an orbit claim
identify the smallest local refactor that would make the requested change fit naturally
identify what the learner needs to falsify or explore then pick the mechanism
identity in data via providers principle applies
identity is a separate concern owned by one module
idiom swap null undefined and empty handling match iteration order holds short circuit and laziness hold the same exception type is thrown
idiomatic go each consumer declares the narrow interface it needs and nothing
idle motion is in references visual identity md
idle never stalls instinct stays beatable a game that runs itself plays the skipped beats with an ai so the world keeps moving
idle without a render loop a slow pulse a stamp fading in under the drag
if a behavior would otherwise tempt a base class or
if a change touches production behavior during discovery keep the diff as small as the question allows and report the exact path or value changed
if a cleaner interface would reduce caller burden offer a migration path so call sites move over without silent breakage
if a cleanup isn t small local and behavior preserving leave it
if a component needs three shared
if a hard override is needed for some
if a likely next change would force callers to change or add a new special case adjust now while it s cheap
if a panel uses none of these ask whether it earns its place
if a port is occupied that s a process
if a specific endpoint holds connections too long even after scoping
if a step lives only inside a note it was never a note
if a touched meaningful module has no agents md offer to create one after the boundary is understood
if a transaction is provided the insert runs in that
if absent values over 1000 are silently capped
if after this the view is still slow then measure baseline the render change
if await mutex tryacquire featured lock 30
if behavior couldn t be proven preserved say what remains unknown
if both say the same thing the check measures nothing
if cache getorload silently falls back to calling
if cache getorload throws when redis is unreachable the caller now
if cached null return cached
if cached return cached
if cached return cached
if cached return cached
if cachedproductsstore ever shrinks to forwarding without adding behavior
if callers must invoke both halves and pass state between them the split created shallow methods so don t
if callers must show a retry after hint expose that one value
if cancelled teardown return unmounted mid start
if cart items some i i id item id return
if coverage is thin offer a focused regression test for a bug or a characterization test for behavior the code already depends on
if ctx throw new error useauth outside authprovider
if dryrun is true returns the computed order without persisting or
if e code authentication failed promptforauth
if e code invalid card showcarderror
if e code network error showretry
if e code sheet dismissed return
if e code user cancelled return
if e instanceof networkerror seterror e
if err nil return nil
if err r inner insert ev err nil return err
if err s repo insert ev err nil return err
if err sleepctx ctx backoff attempt err nil
if err sleepctx ctx jitteredbackoff attempt err nil return err
if err tx save order error err nil return err
if err tx save order error err nil return err
if err tx update error err nil return err
if err tx update error err nil return err
if error return errorstate err error
if error return errorstate err error
if every reply you write lands in the middle of this range the range is what you are missing
if file kind csv return csvpreview file file
if file kind image return imagepreview file file
if file kind pdf return pdfpreview file file
if fn closes over rows read before withretry
if get items some i i id item id return invariant
if granted return seterror camera denied
if granted setstate denied return
if handler throw new unknownmessagetype message type
if icon is provided it
if implementation reveals the abstraction was wrong change the interface and its comment
if inflight return inflight as promise t
if inter or jetbrains mono is not installed fall back to calibri sans and consolas mono
if isloading return spinner
if isnetworkortimeout e attempt maxattempts
if isretryablegrpc err attempt maxattempts
if istransient e attempt maxattempts
if istransient err attempt maxattempts
if istransient res status attempt maxattempts
if it does none remove it
if it does not offer to create one with only the facts learned from the current change
if it doesn t match a named entry in references smells md that s behavior preserving cleanup out of scope here
if it doesn t the flag is a lie
if it fails name the exact failure the comment had to describe the retry buffer
if it has to be long or describe internals to be complete the module is shallow
if it has to describe internals to be complete the module is shallow and that is a redesign signal
if it is down nobody can pay so read the rollback section first
if it is large or complicates an interface start simple and optimize only if a problem appears
if it is the user needs to know so they can decide whether to keep it
if it isn t the index does not match the query fix the index
if it needs a tease it isn t interesting
if it s right the experience is already in it
if it would expand the task name the tradeoff and let the user choose between the refactor and the smaller tactical change
if loading return spinner
if moving a control would let the learner falsify an intuition build it
if none of those apply a plain conclusion is enough
if not call createkeys
if not the clause was a rider so leave it deleted
if notification channel email
if notification channel sms
if one rare caller does give it a separate query and keep the common
if order status placed
if p99 is 50ms healthy 200ms is
if payment customer id acme
if pricerow has a non obvious contract that goes in its prop interface comment
if q status error return error err q error
if q status loading return spinner
if random and human grade land in the same place the band is flat
if res ok return res json
if responsibility moves a public api changes or a cross module rule appears update the nearest agents md when it is in scope
if row also contained
if sid not in store
if start end the result is empty both are clamped to 0 len text
if status orderstatus placed throw new illegalstate
if that refactor is small scoped to touched code and lowers future cost offer it with its cost and benefit before doing it
if the access pattern produces low hit rate the cache is just
if the admin path also needs a checkout function it declares its own
if the cache itself is unavailable falls through to load and
if the ceiling is hit with nothing passing hand the strongest candidate to the human with the precise blocker named
if the clarity issue exposes a module rule agents should remember offer an agents md note at the meaningful module root
if the consolidation establishes a module rule offer to record it in the nearest meaningful agents md
if the controller owns transaction scoping less common the service still
if the conversation is moving fast and the user is reacting to options prose
if the downstream is a critical path dependency and you must
if the downstream takes
if the experiment changed production code review the diff before the finish phase ends
if the human rejects all candidates treat their stated reason as one new flaw seed and generate exactly one more directed candidate then stop
if the interface itself splits common calls from advanced or rarely used ones mirror that split here
if the issue was perceived and never measured no cache at all
if the issue was the database is slow fix the database example 4
if the issue was we want to reduce database load a cache might
if the material wants 18 slides build 18
if the native module doesn t already offer a batched call write one
if the native side already has the data in memory often the case with
if the negation is something nobody would write the original says nothing
if the new work fits awkwardly name the current ownership problem and suggest the smallest local boundary cleanup that would make the change fit
if the opposite reading could borrow the same sentence it is a shared frame and no claim has landed yet
if the path ends at a failed request the consequence ends there
if the request takes 500ms because of n 1 queries and a slow renderer
if the same complexity is scattered across nearby call sites name the local consolidation that would remove it
if the same concept is named several ways across the codebase offer a follow up rename or convention cleanup
if the selected behavior becomes durable inside a meaningful module check whether the nearest module root has an agents md
if the sentence carries weight its content shows it
if the sentence still works it isn t about anything
if the service owns the unit of work hide the parameter behind a context
if the split is described as a sequence first read then parse then write that is temporal decomposition
if the underlying data store can t handle peak
if the user declines automated tests record the chosen verification source and keep the production change scoped
if the user says they will run manual tests or asks for no automated validation treat that as the verification source for the loop
if the user skips a question the question wasn t answerable from where they re standing
if the user will test manually show the exact thing they need to observe
if there is no unnamed audit no fake result no other function in play the word is decoration
if they directly ask which would you pick answer the question and still avoid arguing for one option when offering them
if they say any version of just do it explain while doing for the rest of the task until they opt back in
if those variants are new cases picked by a tag or type design the dispatch now
if two claims belong on a slide split them
if unsure build the html deck
if user deleted smallest change that works
if yes pick an interaction type from the catalog below
if you are unsure whether you should still be in toby squall mode you probably should be
if you can t decide what to do the caller probably can t either so throwing just relocates the problem and adds cost
if you can t fill them from the file in front of you there s no finding so drop it
if you can t keep a section accurate at its current detail level make it shorter and more abstract
if you can t name the behavior you don t know what you re testing
if you can t name where the value comes from you re guessing it can happen so move it to a question or drop it
if you cannot name why the mark fits the subject s structure pick a different mark
if you cannot name why the shape fits the subject pick a different shape
if you cannot name why the shape fits the subject s structure pick a different shape
if you change it leave no instance of the old convention behind
if you don t have measured evidence that a piling up failure mode is
if you find yourself reaching for a hypothetical example to ground the conversation stop
if you find yourself sizing a mark between these two ranges pick one of the two extremes
if you have to squint to call two blocks the same they aren t
if you only list back what they already said in slightly different words the skill failed
if you patched around a design problem to make your change fit stop and fix the design instead
if you re reaching for a 13th a text label would do the job
if you spot a real bug or a security issue while cleaning up don t fix it here because that s a behavior change
if you start a long running process meaning a dev server watcher or tunnel three rules
if you ve widened far enough that there are too many threads to track in chat write it down
if you ve written more than three findings in one pass stop and re run the bar on each
if your interface forces the caller to
if your reply sounds like the median example that is the warning
ignores the inconvenient evidence name the passage it skips
image source uri user profilepictureurl style styles avatar
images without width height in styles layout thrash
implementation detail in an interface comment shallow module
implementation details stay out
implementation details why idempotency
implementation in pkg users happens to satisfy both because it has both
implementation in the interface comment the comment describes internals
implementation inheritance a parent supplies method bodies that subclasses can use or override
implementation inheritance in mobile native code extending
implementation inheritance is the common form
implementation s job saga outbox or two phase
implemented as a property of the service
implementing a shared interface doesn t excuse it on its own
implies attenuation decay broadcast
import checkoutprovider checkoutflow from features checkout
import chevronleft chevronright from lucide react
import config from config
import createcombobox from lib combobox
import createquery from tanstack svelte query
import createstore from solid js store
import nativemodules from react native
import url https cdn jsdelivr net npm fontsource variable inter 5 index css
import url https cdn jsdelivr net npm fontsource variable jetbrains mono 5 index css
import usestate useeffect from react
import usevirtualizer from tanstack react virtual
import writable derived get from svelte store
import z from zod
imports three https cdn jsdelivr net npm three 0 184 0 build three module js
impossible to use correctly from the destructuring site because the names
improve unclear names comments and obvious on read structure in the code already being touched when the cleanup is local
in a module readme md for humans using it
in a parent child relationship where the wrapper invisibly injects props and
in a steady state loop prefer reusing memory over allocating fresh and prefer skipping or deferring work over blocking the loop to retry
in an existing codebase inspect the tests that already cover the touched behavior before adding new ones
in any of the three frameworks
in app navigation path
in databases md indexes
in decks vary the interaction type see references decks md
in decks vary the opening see references decks md
in distributed systems things fail
in existing code inspect the current error validation retry cache batching and performance paths before adding another branch
in how many situations will this method be used a method serving one call site is a candidate for inlining or for redesign into something that serves more
in its own try except that returns a 400 dozens of identical handlers
in process single flight isn t enough meaning you have many processes and
in react navigation route params are part of a screen s public interface
in react the framework s default is already the
in recall and language a miss is usually a gap a slip or interference with a similar item and a language error routes to a recast or an elicited self repair
in set produce undefined behavior
in solid this whole class of problem largely disappears because
in svelte 5 with derived the
in that case the screen shows a log in to see recommendations cta
in today s fast paced world we all know let s dive in it s no secret at the end of the day when it comes to
include a retry after hint is the day you edit fifteen components
include or exclude auth depending on whether some caller remembered to
include order shipping address order line items lineitem product
includes the wire contract rest grpc the service layer methods and
includes which relations the orm happens to lazy load and how many queries
independent set the passage and the question write a thesis on what the river stands for in one sentence someone could disagree with the disagreeable part is the spec
independent set up the context then ask the learner to produce the piece
index added just in case
index choice is a high consequence decision at the
index foreign keys you query by a line items table with
index the columns in your common where order by pairs a query like
indexes add them when something is slow produces the death by
indexes are the canonical case of naturally efficient costs no more
infodialog open open onclose close title heads up body
information about who the user is what their permissions are and which tenant
information leakage in one file pair
information leakage one design decision shows up in several modules prop drilling key schemas duplicated across endpoints
information leakage one hidden decision reflected in several modules
information leakage one hidden design decision a file format wire protocol storage layout policy shows up in more than one module so changing the decision means changing all of them
information leakage the cache key schema
information the caller needs must stay in the interface
infosoft e2e9ea infotint bfcdce infotext 2c3a3a
inheritedwidget directly is the low level primitive most apps shouldn t use
initial render is slow scroll is janky and memory grows linearly
initial wrapper around a native module
initialfocus determines which button receives
inject collaborators avoid base classes
inject the typed values a module receives config databaseurl or a client already built from it passed in where the app is wired together
ink 2 background mono line gutter copy button syntax classes kw num str com
ink allocation decide before you start which panels or slides use ink and which use paper
ink allocation decide which slides use ink before you start
ink anchored paper is the default surface but ink panels punctuate the flow section dividers quoted measurements named decisions
ink appears only at summary or decision points
ink background faint geometric backdrop the toby wordmark in the corner
ink forward decks majority dark are valid when the deck asserts more than explains so decide deliberately
ink forward majority ink surfaces paper used only for relief or sharp contrast
ink panels appear as evidence callouts inside a paper field
ink toned background 1a1c1f
ink variant for dark fill
inline svg is acceptable for static artifacts
inline trace svgs 80 120px wide sized to a table cell or kv value
innocuous and is the most common source of cache misuse in growing
input 36px height info teal focus ring at 2px offset
input and one pagination control
input cb inputprops placeholder search products
input computed output the learner types a value and the slide computes and displays a result
input use cb inputaction placeholder search products
input value through filter highlightedindex tracks keyboard navigation
inputvalue and selecteditem as react state and pass setters in
insert into orders values
insert into orders values join for in orders
insert order order promise order
insert order order tx transaction promise order
insertmany in the repository owns the bulk semantics how big a
inserts the order and returns it with its assigned id
inside a domain method
inside a route boundary around the risky widget
inside cards 18 24 px padding
inside it and the rest of the framework is reactive plumbing
inside the code being touched
inside the core so it stops being leaked through props
inspect the current behavior and the smallest safe edit point
inspect the diff and the patterns nearby and across the repo
inspect the file see the established
inspect the material pick the mode and identify teachable decisions including whether one echoes a decision from earlier in the session
inspect what s there who owns the port what s running when it started
instance variables visible to both sides are textbook information leakage across modules
integrate so it cannot blow up sub step a stiff force so one big push across a frame does not inject energy and fling a body off screen
intent method interface transcends the framework
intent owns the safe defaults focus on cancel confirm button styled red so
interact with this service to authorize a cart capture a payment and issue
interaction shades derived from ink and accent
interactivity plan walk the slides conceptually
interceptor or aop advice
interceptor that catches permanent false sustained outage user facing
interface and the implementation the same size
interface comment first so the abstraction is stable before the code
interface comment for destructiveconfirmdialog
interface first design exists to find that interface before implementation locks in a bad one
interface for arbitrary searches
interface inheritance a parent declares method signatures with no bodies and subclasses each implement them differently
interface inheritance and implementation inheritance carry very different cost profiles
interface leaks the transaction protocol into every signature
interface paid once at design time
interface should be much smaller than the implementation
interface with everything the consumer might want
interfaces in web spas are mostly invisible at compile time and fail at
intermediate screens that pass them along
internal call order private method signatures the order helpers fire and intermediate data types are invisible from outside the module and should be invisible to the test
internal collaborators stay real because mocking them couples the test to current structure
internal protocol does not
interpretation a defensible reading or argument with no single correct answer literature history essay usage and translation
interpretation and recall subjects use their own forms and the references carry them
interpretation subjects literature history essay usage and translation offer your reading as one defensible option with the evidence under it and name the counter it has to answer
interpretation subjects swap right and wrong for supported precise and accounts for the counter evidence
into the load through pattern which is what they wanted anyway
into the rest of the system
into usestore setstate s cart s cart item
introducing a better handler naming scheme is
intuition higher level inside code why this exists what a block accomplishes conceptually why a non obvious approach was chosen how a reader got here
invalid argument not found permission denied and the rest
invalidate key and the service code carries the list of keys
invalidate key string promise void
invalidation every mutation that affects a cached value must
invalidation is a decision one module owns it and callers don t carry the
invalidation is internal every mutation method evicts whatever it
invalidation rules are duplicated wherever they re touched
invariants live in callers
invariants live in one place
invisible to the contract
invite sit in a pending list until it s accepted
invoke two objects in a fixed order
ios and android differences in permission semantics
ios and android may use
ios android differences are gone from the caller s view
ios has no comparable
ios swift android kotlin same checks
ios swift android kotlin the same checks apply
irrelevant most of the time
is a one line edit at a named location
is a single expensive per row computation or a giant image being rendered
is active flag when set to false also revokes all sessions for the user
is defined and commented at
is for the admin checkout path
is in effect immediately
is information leakage because usercard does not use it
is it safe to delete the shim yes
is it safe to delete the shim yes
is one coherent deep abstraction with a simple prop interface
is required how the ledger works live in agents md
is safe a transaction that calls sendemail and then writes to the db
is safe to re run schema down is reverse only and may be destructive
is the combined interface simpler for callers
is the difference between schema migration and data migration
is the entire state layout exposed by getter and the entire set of mutations
is the over split
is this api easy to use for the common case today general purpose interfaces fail when they make the easy thing hard
is true data is undefined
is true from mount through the first response
is what getorload promises and that promise has to be visible
isavailable may return true on devices where the user has not enrolled a
isavailable nativemodules rnbiometrics isavailable
isolation level is the backstop under both
isprobablyadmin names the confidence the permission check actually has
isrefetching is true data still holds the previous value while loading
istorchavailable nativemodules barcodescanner istorchavailable
it after an invite is submitted the invited address appears in the members screen s pending list
it also uses the interface itself as the earliest possible signal that the design is wrong
it applies less to go no inheritance and composition is the default and to rust traits are interface inheritance
it arrives as free work in somebody else s review and they have to decide whether to trust it
it arrives while the abstraction is still only text
it asserts that true is true and has never failed
it belongs in one module that reads the environment validates it at startup and hands typed values to the rest of the code
it binds hardest on comments docstrings error messages setup steps teaching prose and artifact labels
it blocks for up to 30 seconds and will exhaust the connection pool
it breaks on mobile live controls relocate never hide references cross device md
it built before you started
it caches just long
it caches your last query so the page loads without another round trip
it costs least when one module
it costs the handling it forces on every caller between the failure and the code that can act on it a catch at each level it propagates through or a value every caller must check or thread upward
it defines the recommendation s slowness out of the user s
it describes ordering which is non null when
it describes the data structure positions
it does almost nothing but
it does not extend the same monolith
it does not generalize to the next thing
it does not import the config module and reach in
it does not mean one function per file
it doesn t feel like a game the world ticks and answers at once references gameplay md
it doesn t know what it doesn t know and finds out only when something breaks
it earns that cost when the case set is open and each case holds state the others do not share
it edits the code
it edits the sentence it just falsified in the same diff that fixes the code
it exists to answer a question compare candidates or find a value that cannot be known from static analysis alone
it exists to be
it exists to kill the notes you would otherwise give twice
it externally the machine enforces valid transitions and bypassing it
it for read only investigation a rename a formatting pass a throwaway
it gets retried and the second attempt succeeds
it gets sorted ten thousand times into the same order
it goes in the interface comment
it governs every reply from that point in chat and in files until the user says otherwise
it governs layout logic density and how ink and paper are distributed
it has been renamed twice since that final
it has its own latency outages and operational
it has made reliable use impossible
it has to feel alive the sim ticks on its own the world answers a touch at once and interference visibly matters
it hits the network
it in the contract
it is a broken test so fix the nondeterminism and never loop it until it goes green
it is also the open closed mechanism for cases that own state a new case is a new implementation and the code that selects between them does not change
it is always loaded so the list is already in context
it is called once per request so a config reload is 4 000 file reads a minute
it is correct until the source data changes and wrong from then until the next deploy
it is hard to read and
it is longer and it
it is now 40 faster than a number nobody wrote down
it is one line to move and it takes out most of the latency
it is paid once and every current and future call site
it is returned from the function written to the database and rendered on the homepage
it is set to true today and it will be set to true when the repo is archived
it is the entry point main server ts the top of app
it is the heaviest rung
it is the kind
it is the kind of contract redesign the comment test
it is the right design and it is also several hours you
it is three not seven
it is used by the report query which i did not check
it leaves already clear surrounding code
it leaves the system working shown by the command covering the touched area run after the slice and quoted
it lives once next to the data it reads and callers do not see it
it looks generic paper ink one red one light always breathing references visual identity md
it mechanical to enforce
it must not be baked into the build
it names no function no file no internal state
it needs a covering test or you leave it and note the edge that needs one
it owns command classification port discipline long running
it owns mode acceptance criteria
it owns placement the split or merge call and the definition of
it owns the comment test the parameter
it owns the money path other services do not move funds directly
it passes because it now checks nothing
it points the opposite way from plain words
it protects no behavior and reads like the implementation with the word expect sprinkled in
it pulls the largest inline script syntax checks it so a stray paren never ships a blank canvas and cross references every getelementbyid against the real element ids so a stale handler surfaces before a player finds the dead button
it re sorts the whole list inside the loop that reads it
it reads config constructs the real implementations and passes them down
it renders in 40ms
it reports and does not repair
it retries the 400 four times
it returns a flat dataclass with
it s a package library service or standalone feature that a human will need to orient to from scratch
it s not x it s y is filler whenever nobody claimed x
it s powerful the move give one fact instead of three adjectives
it s reachable from outside its own module
it s used or whether the change will break something else
it scatters one piece of knowledge across stages and produces shallow modules
it serves a bounded age value while a background load refreshes it so the
it should be transparent
it should hint at the structure of the subject
it should not fail when something incidental changes a timestamp a key order a formatting tweak a new optional field in a response
it slows every change lies about coverage and trains the team to ignore failures
it slows everything downstream and never shows up in a diff as the problem
it solved itself instantly the answer is trivial the win is earned through structure references gameplay md
it stands correct on its own
it tells the next agent the diff is sound which is a complete report
it tests that the output is whatever the output was
it used the first time returns the original result and never places a second
it was added for the second type which hasn t appeared in three years
it works for engineering problems vacation planning naming things career decisions creative work taste finding i think i want x but i m not sure anywhere they ve named a specific and the specific is standing in for something larger
itemprops index number lihtmlattributes htmllielement
items become harder snapshot tests get noisy you lose ctrl f to find
items coupon shipping and total reflect the current
items map item feedcard key item id item item
items t current item set
items update xs xs some i i id item id
its contract is what each field means and a
its craft is real motion watchable emergence and a knob worth turning
its diff visible half a subclass overriding an inherited method to throw or no op is already toby swd modules deep implementation inheritance hierarchy red flag
its entries are the standing claims about that area so an entry contradicting the request is a question for the user before any code
its job is to carry what the code can t the conceptual model the reasons behind non obvious decisions and the design decisions that span modules
its out of scope section rules out anything that moves code between modules or changes a signature
its state and dependencies stay inside it
job what is this artifact doing teaching a mechanism summarizing evidence providing a reference walking a process supporting a decision
joinedload order shipping address
joins happen how transactions retry what the indexes are and the
json only values ttl fuzziness
json parse cached null
json shape owned by the api layer
judge this off the diff in front of you because a file s commit history is a full file read this pass does not do
just crash the app level fallback is the just crash equivalent
just force push over main not doing that without you saying it twice
just hides the steps that take the reader s afternoon
keep cleanups scoped to code you re already in
keep compatibility when the current surface is public exported persisted or used across a service boundary unless the user approves the break
keep difficulty in the band mostly right with effort
keep everything matte and flat shaded
keep final answers on result verification and what stays open
keep implementation detail out of the interface
keep implementation mechanics out
keep it human readable so the learner rereads it and offer an anki importable export if they ask
keep it in module scope because a window side channel breaks headless tests
keep it only for a real contrast the reader needs a named export against a default one a named type against an anonymous tuple
keep it rare because a red that shows up often stops landing
keep it to one map
keep it to rules and pointers
keep lighter phrasing out of the data a slide heading can carry a plain sentence
keep math random out of the resolver
keep only artifacts that now belong to the product module docs or tests
keep outside references short when the topic is larger than the current task
keep secrets credentials tokens and personal data out of them and out of source
keep story pacing on the clock
keep task completion moving
keep tests an executable specification of behavior
keep the context small
keep the docs offer separate from the tuning loop so the loop stays fast
keep the loop short
keep the offer small and concrete tied to the module you just inspected
keep the purpose narrow enough that every variable carrying the name behaves the same
keep the representation hidden behind the module boundary where the language allows
keep the simulation loop under 60 fps and honor prefers reduced motion with a static snapshot mode
keep the switch and let the type close it
keep the test at the public boundary when one exists and avoid locking in incidental internals just because legacy tests did
keep the two module documents accurate agents md for agents writing code
keep the work behind a dev only route local flag isolated file fixture script or labeled block
keep tweaking this while i test
keeping a second copy here is how the accessors entry ended up in four files with three wordings
key constructors that callers must use
key f product listing category
key f product product id
key lifecycle info plist
key the server dedupes on before the same retry logic applies
keyboard aria open close filter and
keyboard handling focus management popover positioning multi select
keyboard nav prev next handlers
keychain keystore not asyncstorage
keyextractor and no item key item id is present
keyextractor item item id
keying convention is documented nowhere
keys versions json schemas and migration concerns live inside each module
killed a process to free a port the user s dev server is now gone
kind alert adds 4px red top hairline red title
kind click x number y number
kind key code string
kind paste text string
kinds line 12px title 18px block 64px
knowledge with reuse elsewhere
known constraints or gotchas things that will bite a user who doesn t know them ordering requirements required environment edge cases the api doesn t protect against
known state plainly no hedge
knows the products caching scheme
kv two column definition list
label short uppercase section name title index glossary kepler worked apply fin
label sits in a small framed chip beside the crosshair
labels are where the clarity floor binds hardest
labels get the clarity floor at its tightest an axis title a legend entry a kpi caption a table header and a slide heading each carry the three word cap on noun stacks
labels should describe what each option prioritizes or trades off so they can choose based on what they care about which may not be visible to you yet
land warm unless the premise earns bleak
language production model the correct form as you use it
language splits conjugation and vocabulary have right answers while usage register and translation are defensible
languages do not matter
last one is the tax lookup which is cached but the cache key includes a timestamp so it never hits
last updated three minutes ago
lastloginat time time db last login at json lastloginat
latency and use a multiple of it adapting as conditions change
latency over time fails
laws of feel paid for with rebuilds
lay out per screen portrait phone controls in the bottom corners the camera lifted so the action sits in the clear band
layer commits to handling its own outages
layer cut a slice named for a layer when it should be named for what a person can do
layer is no improvement
lazy defaults to refuse orbit for everything plain triangle plain square
lazy defaults to refuse orbit for everything triangle because it s geometric square because it s simple
lazy defaults to refuse orbit marker for every artifact regardless of subject locator reticle as a decoration when no target needs locating calibration grid everywhere because it looks technical
lazy loading rules every caller knows to touch line items before commit
lead with the behavior that now exists stated the way the user would observe it
lead with what got simpler ordered by how much reading effort each change saves and the number each one drove down
leak through to every caller
leakage is a hidden decision duplicated across modules
learner ayer yo voy al cine you ah fuiste al cine ayer
leave a comment naming what the sound design would be and why you skipped it
leave code that s already idiomatic or conventional
leave it until after the release
leave the code alone
leave the mechanical parts filled in
leave the task open until the user confirms the gate
leave these uncommented operations the code already shows i increment i restatements of the name commented out code and anything the type already proves
leaves done undefined and code has to start anyway
leaves the interface entirely
ledger as source of truth the ledger reflects settled charges webhooks
ledger entries for order order id returns all ledger entries for an order
ledger py append only double entry ledger
legacymfasecret string db legacy mfa secret json
legend when 2 encodings
length alone is not a reason to split and over splitting is the more common error
length by itself proves nothing
length is a finding only when it causes a named problem
let a ball or an eye go smooth
let height state window innerheight
let id id string props
let me find which accident before it stops on its own
let products products product props
let s explore x the move answer
let s just add a flag that is the fourth flag on this function
let s run experiments
let the learner control the pace
let width state window innerwidth
lets a caller re send a placeorder that timed out without risking a double
letter spacing 0 012em line height 1 1
letter spacing 0 on display headings and body 0 12em on eyebrows only
letting the next failing test drive the next bit of implementation is tdd as a substitute for design
level moving the cache from a peer of the service to a decorator of
level the widget level boundary is the masking layer
level which is where the one handler belongs
li key p id cb itemprops i
li use cb itemaction i productoption product p li
libraries resilience4j java gobreaker go cockatiel typescript
library adds a real cost scroll position bugs focus management sticky
library substitution error type and message ordering and stability locale and timezone and precision all match
lift the cover off
lift the sorted derivation to the parent so it runs once
limit defaults to 200
limit must be 1 1000
limit must be 1 200
limits of the shallow one
line count never justifies a split on its own
line items selectinload by order ids one query for all products
linear tick marks with t 00 t 18 t 42 labels
lines intelligently handling tab vs enter ime composition events screen
lines while this one stays inside your own team
link external specs and let them stay authoritative
link to generated docs and let the single source stay current when possible
list padding sounds like a list of five where points four and five restate one and two
list takes 2 3 seconds to load over the bridge before any rendering
list them in full before
list user find map string object filters
list user find userquery q one search method
list user find uuid orgid boolean active instant createdafter instant createdbefore
list user findbyorgandactive uuid org boolean active
list user findbyorgandactiveandcreatedafter uuid org boolean active instant since
list user findbyorgandactiveandcreatedafterandrolein uuid org boolean active instant since set role roles
list user findbyorgandactiveandrolein uuid org boolean active set role roles
listbyorg orgid string user error
listing category string promise product
listing category string promise product
lists users matching the filter
lists users matching the filter
literally a string contract native bridges have an asymmetric cost
literature history philosophy essay craft and the usage and translation side of language
little caller drives the protocol
live availability data must be near fresh
live controls cannot scores rates the catalog you browse between actions can fold into a sheet
live in the types
lives inside the module where ios vs android permission flow camera index
lives once in add
load directly the caller doesn t know the cache is failing
load inter and jetbrains mono via cdn
load inter and jetbrains mono via import from cdn jsdelivr net
load it before finalizing prose do not wait to be asked
load it when the placement decision also creates a callable surface
load on miss itself so the caller never does
load promise feeditem null
load references interpretation md
load references retention md
load this file when building a chart plot or data visualization for an toby artifact artifact
load this file when building any component for an toby artifact artifact callouts alerts badges status pills stat grids worked examples decision rows sparklines in tables code blocks dialogs treeviews equation blocks
load this file when producing a slide deck html for an inline inline artifact default for teaching or pptx for a downloadable file
load this file when putting a geometric mark on a slide panel or page either at content scale the mark is the figure or at decorative scale the mark is textural backdrop
load this file when writing substantive copy for a toby artifact style artifact headings paragraphs captions callouts button text and microcopy
load this file when you need fake content placeholder data demo copy slide examples dummy kv values sample status messages
load toby voice s references ste floor md and hold the floor one idea per sentence pronouns that carry a noun one name per concept from first mention to last and the relation between two clauses named rather than welded with a dash
loading a contact list
loading boolean true from mount until the first response settles
loading state cache there isn t one refetch on focus there isn t one
loads the order with the given id and returns its data status flags
loads the order with this id and returns its current screen state
loads the product and
localstorage sessionstorage not supported in inline artifacts
log level z enum debug info warn error default info
log the detail and return the category
logging access which basecontroller did via a helper moves to a spring
logic stays in machine ts
loglevel parsed data log level
logo primitive name the shape
logout becomes a deliberate composition naming each store to drop
logs and diagnostics are a surface too
long instructions mean the control is not legible
long parameter list data clump
long parameter list data clump diff visible a signature carries enough parameters or the same small group of parameters keeps recurring together across signatures that grouping them into one object would simplify every call site
longer the call fails fast handled as a transient by the retry
look for a fundamental fix first a cache a better algorithm or data structure
look up line items by order
loop first the sim ticks something changes the player interferes it matters placeholder art before anything else and confirm the one surprising thing shows
loop of api calls n 1 over the network
loop of bridge calls n 1 over the bridge
loop of queries n 1
loop of single row inserts updates
loop which toby swd experiment owns and skip designing the cache s
lose the prop entirely and deep linking works because routes are now
lost update under concurrent writes
low information assent ok got it makes sense on a decision you flagged as dense is the nod along
low traffic call a daily cron job to a partner api
lucide chevronleft chevronright at 16 20px 1 5px stroke
m 01 through m 05 practice modules
machine ts the step state machine
magic number or string an unexplained literal doing real work in the logic a comparison a multiplier a branch condition standing in for a concept the reader has to guess at
magic number or string diff visible an unexplained literal doing real work in the logic standing in for a named concept
main ts the composition root
maintain state and pass it back into a hook every render
make a throwaway version
make changed code simpler to read and keep behavior identical
make each a module with
make eagerness explicit at the call the repo accepts a small spec of
make every artifact under this system look like it was made for this exact content
make it funnier the wider in world register references comedy and narrative md
make it only when the interface is consequential and two or more candidates passed
make one cleanup pass
make refactoring safe enough that the team will do it
make state changes total anything that should happen once nulls its own callback first
make the change reversible
make the code readable to whoever inherits it
make the common case path handle the edge input with no branch
make the experiment easy to run and read
make the investment today in this change because a deferred investment never happens
make the test pass is such a tidy stopping signal that tactical programming feels like discipline
make them produce the term and default away from multiple choice which trains i ve seen this and inflates confidence
make them say it
make these calls once
make this framing decision before the procedure because it constrains every step after it
make those choices per artifact
manage handle loop chunks order calls handle partial failure
manual test loops proof of concept checks design trials parameter tuning live feedback
manual test or an iteration driven by user feedback
manufactured suspense sounds like but here s the thing and that s where it gets interesting the move if it s interesting it survives being stated plainly
many do not and answering well is the response
many fields keep one updateuser operation with a field mask document
many thin agents md files reproduce the over subdivision problem because they rot they drift and they train readers to ignore them
map input from the real camera screen to world comes from the actual camera rig offsets along its axes intersect the ground never an approximate frustum guess
map o orderrow key o id order o
mark every one updated or deliberately out of scope before calling the change done
mark is a hairline circle two tilted ellipses a single red dot a small centre dot
mark throwaway work so reversal is boring
marked dates show a small red dot
markings before saying the experiment is finished
markpaid and cancel are the legal transitions so
marks the trajectory between the two anchored points
marks where samples were taken
mask at lowest level absorbs the transient cases
mask it at the lowest level if a low level module can fully handle the condition without the caller ever knowing handle it there
mask the transient at the wrapper rung 2
masking is correct only when the information is not needed outside the
masking works best in a widely used library method where it removes the most handlers
match density to the cognitive load of the content
match length to the change
match the ask to where the learner sits on the concept right now and climb a rung when they show the model
match the model to the system a real integrator for something physical a rate or a curve for something statistical a few rules per agent for something behavioral
match the qualifier to the epistemic state
match the scheme to the system symplectic for mutually attracting bodies small explicit steps for a swarm of test particles
match when query data productview product query data match
match when query error errorstate err query error match
match when query isloading spinner match
material icons and carbon are too dense forbidden
matrix table grid data small grid of squares 3 3
matters the ui chrome
maximize benefit per unit of interface cost
maximum 80px in any dimension
maybe depends on per row cost
mb of rgba once decoded
mean here question from every caller
means a new component in steps and a new transition in machine ts
measure the current latency median p95 p99 for the hot path
measurement validates explain analyze your hot queries before shipping
mechanical means a pure rename a dead code deletion or a swap where the edge for its class is provably unreachable and you quote why can t be null typed string no null
members can invite a teammate by email and watch the
memoization is unusual even at scale
memory pressure on large lists
mental model rationale cross module decisions
menu popover menu primitive
menubar horizontal file edit view strip
merge into one request module that reads and
merge when pieces share knowledge when the combined interface is simpler than the separate ones it can do something automatically that callers previously coordinated or when it removes duplicated nontrivial code
merging them couples decisions that should move apart and the next change tears them back out
merging them couples things that should move apart and the next change tears them back out
message assignuserrolerequest string user id 1 role role 2
message chain diff visible a caller reaches through several objects to get what it needs a getb getc getd getvalue coupling itself to the whole intermediate structure
message changeuserdisplaynamerequest string user id 1 string display name 2
message changeuseremailrequest string user id 1 string new email 2
message deactivateuserrequest string user id 1 string reason 2
meta charset utf 8
meta name viewport content width device width initial scale 1 user scalable no
meter progress min max axis optional consequence red threshold mark
method length on its own
method length on its own and the bare presence of a comment stay non findings no matter what the catalog calls them
method runs inside whatever transaction the caller established with
method whose comment lists exceptions to handle
methods but neither consumer is coupled to anything it doesn t use
methods on the class readonly fields
methods on the struct that own the invariant
methods orders is unaffected because it never asked for them
methods that enforce invariants private fields
metric label big numeric value unit suffix optional rail progress strip optional target line
mfaenabled bool db mfa enabled json mfaenabled
mfasecret string db mfa secret json
mid iteration so a page can repeat a row or skip one
mid refactor you just moved a range check from the controller into the repository layer
middlewares is first do this then that then the next thing the thing
migration knowledge belongs in one module
migration or seed script updates a snapshot clears a cache edits
migrations are an interface between code versions
migrations run in deployment order
mind that design is a gateway interface with two implementations and
mint from items the learner missed this session at a pause or the close so each card is anchored to a real memory
minutesuntilthetrialquietlyends names the moment the user finds out
mismatches throw classcastexception at runtime
miss all hit the database and the system briefly tips over
missing coverage new behavior or a regression path with no useful test including a bug fix that ships without a regression test
missing one of the three means it isn t a criterion so ask or drop it
missing precision a field or argument with no units bounds or null meaning where those aren t obvious
mission ops gives natural reasons to use the components toby artifact ships status pills stable watch consequence worked examples premise derivation result decision rows approve hold reject stat grids with units c ms dbm
mission ops is the default for fake or placeholder content
mixed levels of abstraction in one function diff visible a function s body swings between calling well named subordinate operations and doing raw low level work for the same conceptual step forcing the reader to switch altitude mid block
mixing optimistic and pessimistic access to the same row reopens the
mobile complexity arrives from places web doesn t see the network drops
mobile interfaces have three idiosyncrasies the comment test exposes
mobile shares most of the frontend principles but adds three native concerns
mock external dependencies at the system boundary
mock of an internal collaborator verifies the call sequence which says nothing about what the system produces
mock only at system boundaries external apis payment email time randomness file systems and specific failure modes that are hard to trigger otherwise
mocking internal collaborators couples tests to call sequences and current structure
mocking the other side s internals couples the test to its structure
models p95 latency under burst load passes
module structure a public api a cross module decision an extension
module structure decides who owns which knowledge and how much callers must know to do their job
modules can each independently get the key wrong
modules feedscreen and logoutflow
modules leakage and parsing code ends up duplicated
money path nothing else in the system is allowed to move funds
mono 2px bottom border for press affordance
mono for values ids coordinates
mono for values ids coordinates 312 ms r 04 2 34 05 n
mono jetbrains mono 400 500
mono values sans labels every number id timestamp or coordinate is jetbrains mono with tabular numerals
monospace with steady width digits for data a humanist sans for prose
mood comes from one source one light in 3d one accent in 2d
more caller facing documentation only hides the shallowness
more channels are certain so build the dispatch now
more right where it uses it
more smaller modules is the wrong reflex
more than one is leakage which means the boundary is wrong
more than that means you re reviewing for taste
most cache libraries offer
most callers call this to make sure a session is gone often after a partial
most code in any system is written by extending what s already there
most fake findings die here
most interfaces don t earn a full design loop
most modules don t need one
most slides are paper
motion disclosure 180ms panel reveal
motion fast 100ms hover focus press
motion overlay 220ms sheet dialog entrance
motion quick 120ms state changes
move cache into the data layer invalidation is internal
move it off the critical path render then fill
move that knowledge so it lives in exactly one module
move the answer onto the type
move the code toward that structure
move the decision into the events module
move the entry under a trailing retired heading with the date and why in the diff that deletes the test
move the network error handling down to the api layer where it belongs
move to compound components with children or a map from variant to a style object
move up a rung only when the case bodies carry weight
moved the range check into the repository because the controller has three callers now and two of them skipped it
moving code between modules changing a signature or altering a public return type is behavior changing work
mu sync mutex unexported only methods touch it
much cheaper than serializing one record 500 times
multi slice work lists each slice by name done or not done with its evidence
multiple in parallel when they re independent
multiple kv pairs stat grids sparkline in table cells evidence rows
multiplied across thirty services and fifty endpoints this pattern leaks
must be exposed amount it s a method that names what the
must callers use both halves and pass state between them
must handle redis is down as a failure mode for what looks like a
mutable state is complexity like any other
mutates a posted entry is a bug
mutation can in principle touch anything
mutex locked at every call site
n 240 sample count
name a misconception as something most people carry in
name each as a follow up and stop
name each slice for what a person can do once it lands in their words invite shows up as pending search matches a full sku
name reused for two purposes the classic source of silent bugs
name the blocker on the same line
name the boundary the misconception trips on anchored to an instance that sits right on that line
name the closest thing you found at path line and why it doesn t fit
name the command you want and why and say in the report which findings rest on reading alone
name the concrete near future use or it s speculative
name the entry the file line and the skill listed then leave the code alone
name the flag how to turn it on and the slice that deletes it
name the phases a readable string deploy work flee drives sim and camera
name the principle by its source so the explanation matches the rule being applied
name the result that would mean the criterion is unmet a check with no failing result passed before you wrote it
name the rule that changed the output when the user asks
name the skip in its words run the criterion by hand and quote the output
name the smell the file line and the routing skill in the final response
name the specific mechanism then steal its real texture actual form numbers the euphemisms it hides behind not medically necessary for quality and training purposes the fax only window that opens after it closes
name the specific thing it can t explain and hand it back that works for the first half
name the task that now routes somewhere else
name the tokens toby
name the wiring at path and line the registered route the render site the caller of the exported symbol the cli subcommand the event subscription
name toby artifact style
name toby code review
name toby feature dev
name toby simplify code
name toby swd clarity
name toby swd complexity
name toby swd docs
name toby swd environment
name toby swd experiment
name toby swd interfaces
name toby swd modules
name toby swd strategy
name toby swd testing
name what stays unverified
name what the code does then what follows from it the cost the count the date the failure it will cause
name whichever ones you couldn t recover
named one task group each
named operations changeemail deactivate preserve invariants
named preset components over a deep core
names comments and consistency compound across every future read every debug session every onboarding every agent that touches this code next
names that map to one primitive
naming a field sensitive in a comment is worth more than the value in a log line
naming the misconception normalizes it since most people carry it in
naming the plan at that stop is what gets one
narrate sparingly a dry wire service ticker news 311 memo reports the world neutral in tone and quietly on the underdog s side
narrative density silent toy through full press corps
narrow category nest interceptors and express middleware fit
narrow the blast radius run it with an empty list in your head walk me to line three and let them hit the wall
native bridge interfaces navigation prop contracts storage module interfaces
native bridges are not usually the place where someone wrote the api
native browser spinners hidden
native module throws string codes
native modules the bridge interface should hide the platform s
native modules wrap the platform s protocol
navigating screen s state and the staleness model
navigation navigate home user
navigation navigate productdetail product recommendations user
navigation navigate productdetail productid product id
navigationmenu like menubar with rich panels
nearest conventions the repo already has billing for money adjacent modules and the jobs daily aggregate pattern used by jobs revenue rollup ts
necessarily longer there because the operation is truly
need not exist and refusing speculative optimization
needed for the operation they do not manage sessions or query syntax
needing placeholder data or demo copy for a slide or artifact references sample content md
needs to keep reads consistent
needs to know whether they are inside a transaction for example to avoid
nested ellipses a small center dot
nested one doesn t and encoding it as base64 only delays the wall
network auth unexpected errors never reach here
network blips and the timeout abort
network failures during the
network layer is mostly about keeping the failure modes bounded
network retried inside the api client
network topology graph mesh crosshair or grid lattice
network unavailable only if all attempts fail
networkerror is usually transient
never a brand accent
never a default series color
never a fixed brand
never a hidden coin flip on top of a decision
never a whole file rewrite
never backfill behavior this change doesn t touch
never enters the failure path aggregate the rest into a small typed set
never leave that debt unlabeled because the next person cannot see it to pay it down
never manufacture a finding to show effort and never soften an empty result with you might consider an empty result is a result
never roll the outcome and then animate toward it
never seen that error string
never ship a diff to look productive which is the churn this skill exists to prevent
never stale line above flips
never use marketing verbs in toby artifact artifacts submit get started continue learn more
never use the same overall composition twice in a session if the last artifact was dense reference this one is spacious argument or diagram led
never write a criterion to license one of these which is what the catalog exists to stop
never write a criterion yourself to license one of them which is what the catalog exists to stop
never write assuming yes proceeding past a stop which leaves this skill with no checkpoints at all
new author either duplicates the cache machinery or much worse forgets to
new behavior with a settled contract once the design pass identifies the abstraction and its interface test first pins down the contract and drives the implementation toward it
new caches in the codebase ask
new cases ship in code you do not control
new requirement deleted users render as deleted
new requirement deleted users should
new states change one method
next request that side effect is named in the operation s purpose
next session read the file quiz the due and shaky items cold before any new material update the tags and markers hand it back
next such value means editing the whole chain again
no 700 in product ui
no added complexity loses
no asset folder so synthesize audio with a little webaudio a tone for a blip a noise burst for a hit
no callers must anywhere
no callers must the hook owns the
no code or equation is up yet
no content cards no kpis no tables no callouts
no contractions ste s reader is a non native technician
no field is set or unset
no fills except where a marker is intentionally a solid dot anchor body at focus periapsis marker
no first person we i our
no give aways no shared root length cue or grammar tell in the prompt
no idea ends it no penalty
no imagery by default
no imported assets no rainbow no boxed toolbar over the scene
no left accent colored cards as a general decoration a known ai slop tell
no load on miss
no lock is held and nobody waits which is cheap when conflicts are rare
no metaphor nothing a reader would have to decode
no method by that name
no one can forget to extend it
no op if it already does not
no p1 p2 labels
no pattern no class
no phrasal verbs that deletes roll back spin up back up tear down check out and time out and swaps plain anglo saxon for latinate
no plan file for a one slice tactical change
no prop combinations to memorize
no reader state assertions
no reading yet drop to noticing skip interpreting for a second
no recent failure signal so you re paying complexity for nothing
no red as brand accent
no relevant harness exists the change is documentation only the edit is formatting or metadata or the only useful verification needs unsafe external state
no resizing strategy they oom the device
no second person you your in most reference contexts
no separate is simpler
no settled fact to card so retain the structure
no shared mutable state between tests no ordering dependencies deterministic and self validating
no signals a broken invariant so a bare retry hides a bug
no single line is
no strategic trigger because the route is new the boundary isn t and api orders ts is the sibling at path line
no timeouts on downstream calls
no title slide start directly with the first claim
no toc here because the cover index slide handles that
no ttl choices no eviction logic no stampede protection just a get and
no verbing technical nouns software runs on cache log mock flag ship diff seed patch and branch
no virtualization library needed
no warm up no narrating what you are about to do no sign off
no words describing call order or protocol first then after you must call x before y
nobody knows whether to change the ttl where to find all the places
nobody understands well enough to set correctly
non obvious design constraints business product regulatory or external constraints that force non obvious decisions
none of that complexity in their interface comment
none of the three needs a runtime consequence
none of these clear the countable win bar so leave them
nonobvious code behavior not graspable on a quick read
not clearing auth on logout the ios size limit is documentation a
not every deck starts with title glossary
not every slide needs to be dense
not for i o
not mention is not
not the one you used last time
not visible in any signature
note also that this screen is reachable via deep link in which case
note any existing user domain objects after upgrade will
note the readme references the interface comments in gateway py and lets the
note what is absent no function signatures no algorithm descriptions
notfounderror is usually a real this resource doesn t exist for
nothing accounts for it a file in the diff tracing to no criterion and no reported cleanup a user quoted behavior with no record entry a record entry no test quotes or an assumption stated mid run and missing from the handoff
nothing below depends on those skills being loaded so this file stands alone
nothing below it names an environment variable or picks an implementation
nothing else in the repo names it so removing it touches no signature and no caller pure line reduction
nothing found means quoting the search you ran the pattern and all
nothing gets deleted outright because the sentence that stopped being true is the only surviving record of what somebody once wanted
nothing imports it and the one caller that did was removed in 4f8c2a
nothing in this feature reads it via props drilled from the page
nothing in this repo references it and it is exported so something outside might
nothing is deployed yet
nothing is ever fully still quiet the motion when the player asks for reduced motion
nothing missing means the reply opened on a warm up and closed on an offer
nothing nearby resembles this no metering module no counter storage no billing surface
nothing on the banned list fires
nothing reads process env
nothing the player needs under a busy thumb on a phone the live controls live in the two bottom corners clear of the home indicator and the must see action renders in the band above them
nothing worth simplifying is a complete answer
notifications share nothing except a tendency to live globally
now a real world momentary network drop never reaches the screen because
now it s a measured problem
now new states are one place callers are untouched
now there are two retry policies that drift
now there is one place that knows what derivations exist
number field mono digits with tabular numerals stacked on the right
number of days from now
number scattered across the codebase
number should i put here stops coming up
numbers carry units a bare number is a defect
numerals always tabular font variant numeric tabular nums
numerals tabular tnum everywhere
numeric columns are always right aligned mono with tabular numerals
numeric ticks or direct annotations tabular numerals tnum
object prop memoization is for stabilizing references passed to
observability cost did this request hit the cache
observable a post to orders id cancel on an order the caller owns returns the order with status cancelled
observable a query matching a name and a different product s sku returns the name match first
observable a seat added and removed inside one day counts once for that day
observable a workspace with no activity returns a zero row no missing row
observable an admin opening the usage page for a workspace sees the seat count as of the last completed day
observable cancelling an order already shipped returns 409 and leaves the order untouched
observable cancelling someone else s order returns 404
observable given a starting state when an action happens the user or caller sees a result at the seam they touch an expired token gets a 401 and the retry after refresh returns the account the seam is where something outside the changed module arrives an http request a cli invocation a screen someone opens a queue message an exported symbol another module calls
observable opening a used or expired link shows an expired state and creates no account
observable searching a full sku returns that product first
observable searching a product name returns what it returned before in the same order
observable state for rendering
observable submitting an email on the members screen shows that address in a pending list without a reload
observable the invited address receives one invite email carrying a link that opens the accept screen
obvious it isn t
obviousness lives in the reader s head and self assessment is unreliable
of caller side tuning will save you
of existence applied to latency
of interface and gets whatever the implementation does behind it
of synthesis the design it twice step is supposed to produce
of the contract so callers don t write defensive code and the cache
of the downstream s healthy behavior
of users request it sends every miss to the store at once
offer it with its cost and benefit and keep it out of an unrelated change
offer re testable material for what is worth keeping
offer that refactor when it fits the task
offer the narrower alternative the specific test file the package s tests a focused type check
offer them because the owner wants them
offer to create or update it with the module facts learned during the experiment
offline another says no connection a third just shows error
offline online transitions native module errors list virtualization image memory
offset is zero based and
offset limit pagination drifts when rows are inserted or deleted
often the answer is basically what s there plus the new bit so proceed
often the whole fix
ok false reason user cancelled
ok this is officially cursed yeah
ok true signature string
on a backend service references backend apis md works this through
on a backend that
on a mission to dedicated to passionate about committed to devoted to
on a new architecture app it s a reasonable default for
on a published service splitting updateuser into four rpcs is a
on a real knowledge boundary
on a retry the stream is already consumed and the second attempt sends an empty body
on cost benefit by the third component
on existing code read the current design before choosing a new one
on first mount before the user context resolves treat undefined as
on ink slides color the footer text with text on dark muted a8a39a
on ink track paper thumb
on ios faceid requires the nsfaceidusagedescription info plist entry or
on logout becomes a deliberate policy
on the data hook same pattern as web md example 2
on top of that pricingservice quote is called inside the render loop rather than once above it
once a deck has been asked for apply artifact copy rules to its copy
once in one place
once long order dependent leaks internals
once or twice a session on the one decision where a wrong guess is plausible ask the learner to call it before you reveal where does this go and why then explain the gap between their guess and the result
once per dense point never twice running
once the user chooses the behavior add or update tests for the durable contract if a reliable harness can protect it
oncleanup window removeeventlistener resize onresize
onconfirm if the user proceeds calls onclose in all other paths
one behavioral concept per test and multiple asserts are fine when they prove one behavior
one centred claim in 64 80px inter 500
one clause on what that feature got right tells the user which pattern they are about to have two of
one constant declared once replaces the guesswork and the repetition
one criterion per cycle test first watch it fail make it pass run it
one decorative mark per panel maximum
one design one check no second candidate
one edit undoes it so it ships marked assumed reading taken is name first reading dropped is sku first and what changes if they wanted the other is the ordering clause in rankresults
one entry per claim
one fact per card
one file or one call site a ticket a product ask a half built feature an endpoint
one find query with a value object
one finding at most at file line naming toby swd strategy
one game is one html file one style one script opens by double click no build step no assets
one general method covers every existing finder and the next ten that haven t
one handler at the top replaces a handler at every call site
one handler forgets to lock
one handler replaces dozens
one heading one behavior stated the way someone outside the code sees it
one hook one operation one piece of state to render against
one input is never allowed two outputs and that is the entire rule
one line in chat
one message at most three questions both readings side by side your recommendation
one method per caller variation a finder handler query method per combination of conditions growing without bound
one module reads the environment process env and import meta env appear in exactly one file
one name one purpose never that name for a second purpose
one name per thing
one name per thing held across the whole artifact a series called p95 latency in the chart is p95 latency in the legend the caption and the summary slide
one narrow interface per consumer
one narrow interface per consumer
one narrow interface per consumer idiomatic
one of each per meaningful module
one of them is a duplicate or the two sentences differ in a way somebody has to settle
one of those libraries is usually cheaper than writing your own
one paragraph context beneath in 16 17px color text on dark muted
one place the inter module string passing interface disappears and callers
one primitive per problem multiple names for it
one query for orders addresses joinedload one query for all
one question at a time when each answer changes the next question
one reactivity caveat the three handle differently total is a derived
one reading the water tracks the narrator s own mood flat right after the argument with her sister stormy again three pages later when she lies to her mother
one reason to change and one body of knowledge are the same test
one red dot that was not there last frame is the whole scare
one retry policy one place that knows what s transient jittered backoff
one retrygrpc helper retryability decision in one place
one rpc per intent field mask form only when the bulk case is real
one self contained html file
one sentence covers it
one sentence trigger plus observable result written at the public interface toby swd testing tests through
one short lowercase inter 600 word naming the topic
one slice per field
one slice tactical work keeps the criteria list in chat
one state object everything in one lowercase g with a single reset that re seeds and rebuilds
one statement one round trip milliseconds
one test at the top detects all special cases and the common path then runs with no further branching
one thing re measure and revert anything with no measurable effect unless it
one transaction one round trip each input maps to a typed outcome
one type bound to
one usememo on the one expensive computation
one warm or cool light low fill underneath warm dots for windows and fire
one wire caveat a query string has a practical length ceiling because proxies
one withretry wrapper idempotent transaction bodies
only a measured critical path justifies complexity that good design would otherwise reject and even then the off path code stays tidy
only do it when the call sites are three distinct
only explains what a developer needs to use the feature correctly
only flag it with the specific file line and a quote of why the diff makes it false
only for derived computed values and behind a clear function memoize style
only for irreversible actions
only forwards its props is a pass through method
only if the field is present in the protobuf wire format clients must
only now write the bodies
only signatures can be expressed in code
only the public surface
only then show the correction and close with so the rule is so they state it
only when humans need orientation
only when the request names a behavior
only when the server signals throttling with backoff
only where the next slice depends on the answer
only with a fresh larger deadline
only worth the complexity when
onmount window addeventlistener resize onresize
onscanned nativemodules barcodescanner onscanned
onselect fires when the user confirms a choice via enter or click
onselect item t void
op add column users sa column phone number sa string 20
op drop column users phone number
open a transaction when you need it commit release
open it with the preamble in examples md so the next reader knows what they hold before the first heading
open open onclose close
open open onclose close
open state uses paper 2 2px info underline
open the members screen invite an address see it in pending with no reload
open void close void reset void
open with what the user can now do that they couldn t this morning in the slice s name then what proved it then what the next slice does and wait
opening a used or expired link shows an expired state and creates no account
opening line one line change long description because the next person will want to know why one line was enough
opening line this deletes 400 lines and adds 60
opening pattern pick from the options below
opens with the mode the one line problem the criteria in their pre code wording and what s out of scope then one task group per slice in the order they ship each carrying its slice s name and ending in its verification block
operation has a side effect on a peer module the side effect probably
operational contract json serialization key namespacing versioning
operations are atomic and return after durable commit
operations are atomic reads may be
operations that maintain its invariants
optimistic a version column the row carries a version and the update
option against the cart s contents some items require specific shipping
option b a small core plus thin presets
option b wins because the
optional bool is active 4
optional caption meta or caveat mono 12px muted pinned bottom
optional kv block beneath
optional role role 5
optional set role roles
optional set role roles
optional small dot indicator row showing position max 20 dots before switching to numeric counter
optional str which is the same value as an explicitly null phone
optional string display name 3
optional string email 2
optional string locale 6
optional user findbyemail string email
optional user findbyemail string email
optional user findbyemailandactive string email boolean active
optional user findbyid uuid id
optional user findbyid uuid id
optionally a distributed lock for cross process coordination so the
optionrenderer p productoption product p
options as a thunk so the query tracks id v6 makes the thunk mandatory
or a design decision made silently because asking felt slow
or a test that has only ever passed
or after a failed fetch
or an external symbol called with no installed source opened and no existing call site cited
or coupon changes and svelte s derived store does the same
or createdbefore to skip that filter
or flashlist shopify s higher performance alternative
or framework specific patterns
order as row for order in orders
order by order created at desc
order by which bites hardest and let the consequence sentence carry the weight
order findings by which bites hardest
order first x then y internal exception names
order id foreign key should have an index on order id if you ever
order id ord 123
order is asc or desc
order line items lazy load
order matters because the index supports customer id queries
order means has leaked into every caller
order repo list for display customer id
order s query order filter order id order id one
orders data build orders view orders
orders filter o o customerlower includes debouncedfilter tolowercase
orders order repo recent for customer customer id limit 50
orders order repo recent for customer customer id limit 50
orders s query order options joinedload order line items filter by user id user id all
orders session query order filter by user id user id all
orgid restricts results to that
original code and structures
original code illustrating the procedure
orm lazy load whatever i touch is what produces the n 1
other keys are ignored
other processes wait briefly and read
otherwise delete and reintroduce the bug
otherwise drop one pointer at the end cleanup candidate file line no severity no argument
otherwise note it as a follow up
otherwise offer that update as the next local step
otherwise persists the order captures the
otherwise report it as follow up
otherwise tell mechanical filler one answer steps a learner who just asked you to hurry
otherwise the ball snaps into a save it visibly cleared and every tester calls it random
otherwise the half and half state is worse than either
otherwise the seed races the schema change
otp field n mono slots default 6 with optional groupat separator
our powerful new dashboard delivers blazing fast insights at scale empowering you to unlock the full potential of your data
out of memory is rare and there is nothing useful to do so checking it
out of scope bulk invites invite revocation role selection at invite time
outer padding 64 px top bottom 80 px left right
output argument diff visible a function s result comes back through a mutated parameter a field set on a passed in object a value pushed into a passed in array
output is how acquisition happens and over correction stops it
outside the retried block
outside those a few lines of plain stdlib stay because saving a line or two isn t worth an import
over built code where simple does the same job
over rl counts userid limit
over specific is also a defect
overexposure callers must understand rarely used features to use common ones
overrides variant s default
overwriting any unsaved changes
owed and have collected
own error pages in sveltekit
own retry or not check gets it slightly wrong misses the deadline
own the clock clamp the frame delta so a backgrounded tab does not detonate the sim step outcomes at a fixed rate so replays match and give the player a speed knob
own the meshes build each once
owner gn c sayo
owner mission ops sayo
owns a soap client a client cert a different retry policy a nightly batch file
owns flow state and the order of steps delegates
owns how orders are stored and queried
owns how users are stored and queried
owns it and the rules for touching it live in one place
owns its key schema and migration
owns load from secure storage on mount refresh sign in sign out
owns open closed state highlighted index keyboard nav aria props
owns the design pass and the near future variants and it routes the
owns the response type
owns the stripe sdk client the api key a retry policy an idempotency cache
owns what step the user is on and what
owns what we believe we are
p style font size 22px color var text secondary max width 720px margin 48px 0 0 line height 1 55
p95 is still acceptable
p99 latency is bad even though no single query
pace it an ambient wire line every twenty five to forty seconds an event line the instant its trigger fires and then rate limited the gaps between left alone
padding 12px 16px border bottom 1px solid var border strong
padding 16px 20px border bottom 1px solid var border hairline
padding 16px 20px border radius var radius base position relative
padding 4px 10px border radius var radius sm font size 11px
padding 8px 0 color inherit
padding inside slide clamp 20px 4vw 64px outer 1rem inside cards
padding left 12px border left 2px solid currentcolor
page for the timeout duration
page size defaults to 50 maximum
page user find userquery q
paginated result with opaque page token
pagination 32px mono cells tabular numerals ellipses kept when range overflows
paidat whenever they set status to paid must validate amount 0
pair it with a small uppercase label naming it
pair point estimates with intervals 0 73 0 08 or 0 65 0 81
pair with a dark code block in a side column
pair with a label for the band
pair with rejected alternative alerts in a side column
panel content card with eyebrow title actions
panel gets a 2px info top border
panels can overlap on screen but one surface takes the live verb at a time so a tap never lands in two places
paper 3 against paper 3 is invisible
paper 3 against paper 3 is invisible so never use it as a selection state
paper carries work toby paper fdfaf1 is the default surface
paper default fdfaf1 background
paper is the background
paper is the working mode
paper panels should feel like interruptions a table that needs to be read a diagram that needs white space
parameter off every method
parameters the cache can choose better than the caller stay inside the cache
parent authors must check every subclass before changing instance variables or non final methods
parent reference a context that doesn t change every render list keys
parent then to a context then to a global store but the store grows by
park a deferred lesson with its reference so picking it up stays cheap
parse and validate at startup that module turns raw strings into a typed frozen object with zod envalid or a hand written parser
parseconfig reads the file on every call
parses behind a single request receive socket
parsing a large blob computing a derived structure over many items
partial indexes for sparse conditions if open orders are 5 of
partial work reported whole a multi slice feature handed over as done with the remainder demoted to a closing note that begins the rest is just wiring
pass an empty set for roles to
pass cache layer cut p95 from 312 ms to 184 ms
pass children via props with rigid slots
pass length is shrinking by 0 2 min per day
pass median p95 latency dropped from 312 ms to 184 ms after the cache layer landed commit 4f8c2a
pass null for orgid createdafter
pass the current items and an id
pass the method runs in o n log n for n 10 the negation is a real claim someone could dispute
pass this dashboard surfaces alerts where p95 exceeds 200 ms substitute stapler breaks immediately meaningful
pass through method an entry point that only forwards to another with a near identical signature
pass through method diff visible a method whose body is a single call to another method forwarding the same arguments with no added logic
pass through method does almost nothing but forward arguments to another method with a near identical signature
pass through method forwards arguments adds no functionality
pass through variable a value threaded through a chain of methods that don t use it frontend prop drilling mobile param threading across navigation stacks across a message boundary a field relayed through an intermediate hop that doesn t read it
pass through variable threaded through methods that don t use it prop drilling nav param threading
pass window 7 min
passwordhashalg string db password hash alg json
past a handful suspicions slipped through
past three words the reader has to guess which word attaches to which
patch a live game with unique anchor replacements on a working copy one match per anchor count 0 means the code moved count 2 means the anchor is ambiguous re read either way escape free anchor text syntax checked after and confirm the patch printed success before trusting a measurement
path d m 2 4 l 23 8 l 44 12 l 65 12 l 86 15 l 118 19
paths that update the data without going through the caching layer
pattern for a state tinted panel background soft border tint left strip 4px un soft hue text text
pattern forced onto the problem a visitor factory observer or strategy applied for its own sake when the problem does not have the structure the pattern solves
pattern forced onto the problem a visitor factory observer or strategy interface adopted on the belief that patterns are good while the problem does not have the structure the pattern solves
patterns banned from anything toby writes whoever wrote them first
patterns earn their place by removing complexity
patterns earn their place by removing complexity
patterns either contain or leak that knowledge
pause for the learner s work or a check only when it earns the interruption a real fork or a nod along on a dense point
payload is supplied the result includes a signature bound to a
payment and returns the persisted order with an id
payment are retried internally up to 3 attempts and surface as
payment declined tax unavailable
payment gateway z enum stripe legacy
payment to the payments service and address validation to the address package
paymentmethod pm shippingoption ship
paymentmethodnotfoundexception if not found
paymentservice process was called once with amount 42 is a call log
pending row rejecting an address already invited and unexpired
per call site retry logic is where this goes wrong
per device key created lazily on first use
per policy dying is covered
per route per widget boundaries root is last resort
per screen network error handling
per scroll gesture so animations driven by scroll position barely update
per toby swd modules a long method that s one coherent deep abstraction with a simple signature is fine too
performance changes made without a baseline
performance defenses become invisible to callers
performance design at the
performance is part of the layout cap the pixel ratio scale particle and shadow budgets down on a phone and shed detail before frame rate when it is tight
performance limit on deep pagination is caller facing
performance pitfalls to know without measuring
period at the end
period at the end
periodically tests the downstream to see if it s healthy again
permanent errors bubble up where the
permission re evaluation across the
permission set camera subscribe start stop on unmount and every screen
permissions may be undefined for an instant
permissions move from boolean per action to presence of the action in
persisted order in rare cases the reconciliation job handles these
pessimistic lock the row take the row s write lock for the rest of the
philosophy adds a near convergent axis formal validity
phone held sideways no bottom sheet controls in the side gutters the camera pulled back so the short dimension still frames the play
phrase each standing assumption so the user can settle it in a word and name the questions this run could not resolve
physics is the verdict on what s on screen animate real motion and let geometry decide at the moment of truth
pick an input count the outputs it produces that single count is the whole test
pick b even though its insides wrapping the
pick candidates discard any that risk behavior drift
pick from references geometry md
pick one before discovery say which in a line and read the table for what each gate costs at that size
pick one candidate change
pick one per artifact
pick one strategy per contended resource and hold
pick the closest never a one off
pick the cluster that fits the artifact s subject then reuse its specific terms
pick the few words that capture what matters and omit the rest
pick the lowest tier that solves the measured problem
pick the mark for the structure of the subject
pick the model that keeps the surprise alive then say what you faked in a comment the way the games do physics is vibes based and labeled as such
pick the one with the simplest caller side interface even when its insides are harder
pick the repo s own identifier and hold it
pick the shape for the structure of the subject examples
pickshipping option shippingoption void recomputes total
pie charts use bar
piece of stateful complexity that
pill before content width 6px height 6px border radius 50
pill display inline flex align items center gap 6px padding 4px 12px
pill watch background var toby watch soft border color var toby watch tint color var toby watch text
pill watch before background var toby watch
pin down what the system does so callers can rely on it
pin label background var toby info soft
pin line width 24px height 1px background var toby info
pin num width 22px height 22px border radius 50
pin one version and one cdn or the duplicate module bug finds you
pin position absolute display flex align items center gap 8px
pkg orders admin checkout go
pkg orders checkout go
pkg orders checkout go
pkg orders interfaces go
pkg users user go
place the game on a few axes propose one combination with a reason then let the creator turn any of them
place the label beneath or beside the mark never inside it
place the series name at the right end of the line in the line s own color
place which spares every call site from re deriving the categorization
placed and payment captured or neither happens
places a new order from the user s cart validating the request and
plain literal english only
plain react 19 without
plain specific literal no decoration
plan ceremony a plan file written for a four line change
plant a throwaway early and pay it off transformed at the finale
platform branch platform os ios checks scattered through components are the copied decision case
player relationship observer funder or twitch actor
plus a distributed lock for cross process coordination
plus an up next rail at the side
pnpm test pnpm lint full pre commit hooks codegen scripts the giant validation script
point them at the file this repo keeps and the directories its tests live in then quote the command you ran
polymorphism when each case owns behavior private state or its own dependencies
pool batches before concluding because a compounding ladder swings 6 to 12 on identical code at n 150
pool timeout 2 fail fast if no connection available within 2s
popover click anchored click outside dismisses
pose a question that gates the next sentence
position 0 is the loaded product or null when loading
position 1 is the last error or null
position 3 is a refetch
position 4 is a mutation function that optimistically updates the local
position their open dialog
position uncertainty without committing to a direction
post api orders import
post api users search body identical contract different transport
postgres now handles 1000 connections for what should be a single query
powerful intuitive seamless beautifully simple elegant game changing delightful polished refined robust modern sleek
praise when it s real stands on its own line
pre class code code span class code ln 1 span span class code kw function span retrywithbackoff span class code num 3 span br span class code com no clue why removing this fixes the bug but it does span br span class code kw return span span class code str ok span br code button class code copy copy button pre
pre existing problems go in residual risk only when they bear on the change
pre lowercase the customer name once when orders are loaded so each
pre sort pre index at load debounce input
precise does the claim say something a rival claim couldn t also say
precise vocabulary why questions an anticipated edge case a correction of you raise difficulty shorter scaffolds hand over more
precision lower level function of comments the half most often skipped
precision lower level on fields arguments return values add what the name and type cannot say units inclusive or exclusive bounds what null or empty means ownership and invariants
precomputed cache warming for example
predict the next handler s name
prefer a computed value or a narrower operation over a configurable one
prefer a small surface that shows the values under test and the result they produce
prefer computing a value internally over exporting a configuration parameter or throwing back to the caller
prefer computing a value internally over exporting a configuration parameter or throwing to the caller
prefer direct labels at series endpoints tufte style
prefer final sealed parents with one or two override points and separate parent managed state from subclass managed state
prefer one coarse call over many fine grained ones across that boundary
prefer whichever states the contract more directly
preferences use this when displaying the user s settings
prefers that over five shallow ones
premature convergence is the failure mode
prescribes call order pass primary for confirmations
presentation is whatever the consumer writes
presents the payment sheet and waits for the user
preserve caller visible errors timing logs metrics and status codes unless the user approves a behavior change
presets are thin wrappers
press states step the bg darker
pressure drops by orders of magnitude
prev next buttons at the bottom
priceincludingthefeewedontshowuntilcheckout named for what it actually holds
primaryaction label delete onclick dodelete loading deleting
primitive obsession a domain concept money a phone number a date range a role code represented as a bare string int or float with parsing or validation for that concept duplicated at each use site
principle is why they re structured that way
print li product name another n 1
print li product name another n 1
print o shipping address city one query per order n 1
print o shipping address city one query per order n 1
print the per round distribution against the curve you meant since a ladder total hides one round eating half the runs
private async evictderivedfrom p product
private final authzguard authz requirerole moved here
private final orderservice orders
private final responsebuilder respond ok error moved here
private listingkey category string return product listing category
private productkey id string return product id
proactive spend trying a second design choosing names well writing the
probability distribution uncertainty bell outline or spread cone
probably no virtualization yet so measure the
process concurrent waiters share the result
process concurrent waiters share the result
process is 200 lines and does nine separate things
processes a new order
processes and the ask list
processes charges and maintains the ledger of what has been collected
producer defined interface with 6 methods
produces inconsistent ui state
producing a slide deck html or pptx references decks md
product catalog changes are infrequent and not user visible immediately
product error loading refetch mutate are the caller s choice
product is the product to display
product names the repo provides
product product repo find product id
product product repo update product id changes
product self update product id changes
product the ttl 600 seconds the serialization format and the
productdetail product product recommendations product user user
productid merges quantities item with a different size variant does not
production charts in artifacts observable plot vega lite or d3
production for language the goal is use
production language the learner wants to say write or speak the target language or hands you target language text
production runs at info so these failures are invisible
productsstore must not bypass this service or cached data goes stale
productview product query data
profile that tempts thin wrappers and persistent storage interfaces are
profile what it is doing
profiler session is for after
profiles are a hot data type after all but the access pattern doesn t
progress 4px linear bar
progress and the previous value is still in the cache
projected p95 with cache 50ms miss path 5ms hit path
prompt opts reason string payload string promise authresult
prompt reason confirm payment and switch on the result
pronouns that carry a noun
proof sources are local guidance files nearby code the repo s own skill md files and its conventions and config files
prop combinations the caller must assemble correctly each time
propagation double retries on aborted
proportionality a real one liner doesn t need an architecture review
protected responseentity error string code int status
protected responseentity ok object body
protected void requirerole string role
protobuf forces an explicit interface and the comment test is unusually
protocol the way example 1 does regardless of which side you re writing
prove behavior is preserved then change
proved by test invite appears pending fails if a duplicate
proved by the migration running against a scratch database and
provider classes with injectable
provides access to users
public abstract class basecontroller
public api concepts gotchas
public api reference if not self evident from the code
public bigdecimal amount return amount a named query method
public bigdecimal getamount return amount
public boolean issettled return status orderstatus paid
public class ordercontroller extends basecontroller
public instant getpaidat return paidat
public order processneworder neworderrequest req user user cart cart
public ordercontroller orderservice orders authzguard authz responsebuilder respond
public orderresult placeorder placeordercommand cmd
public orderstatus getstatus return status
public page userdto listusers
public record placeordercommand uuid userid uuid cartid uuid paymentmethodid
public responseentity getorder pathvariable string id
public responseentity getorder pathvariable string id principal p
public sealed interface orderresult
public static order place bigdecimal amount
public userquery this empty empty empty empty empty 50 empty
public userquery withactive boolean a
public userquery withorgid uuid id
public void cancel string reason
public void markpaid instant when enforces transition
public void setamount bigdecimal a this amount a
public void setpaidat instant t this paidat t
public void setstatus orderstatus s this status s
pull the logic out and run it headless
pulling everything down is a god module
pulling unrelated complexity down is leakage with a new hat
punch up at the system never down at the person
purpose built read model if the list view always needs the same
push them up to that caller
push toward producing the target word because recognizing it only feels like knowing
put the explanation against the thing it describes the line the equation the quoted passage
put the hard part where it costs the fewest callers
put the state and its lock in one type expose methods and keep the lock hidden
put unavoidable complexity inside the module when it is related to that module s job
putting a content scale or decorative geometric mark on a slide or panel references geometry md
qty 1 coupon valid for the current items and recompute total
qu viste the past tense is modeled in a real reply and the conversation keeps going
queries but not bare status queries
queries to render a list
query construction syntax every caller writes sqlalchemy filters
query method that returns all matching rows
queryfn api products get id
queryfn api products get props id
querykey product props id
question because fine grained reactivity updates only what changed
question how the row is guarded so the conflict is detected at all
question is where in the system each kind of failure gets absorbed and at
questions the repo answers questions about anything one edit undoes and questions the user already answered earn no stop
queue age 88 s
quick inline charts recharts chart js or hand rolled svg
quickly navigation params get serialized so the interface to a screen is
quiet decorative shape on the right opacity 0 16
quiz them every line and they stop watching
r 04 2 reference deck identifier
r json promise reject r status
r json promise reject r statustext
r xx x reference deck identifier for example r 04 2
race each was meant to close
radio radiogroup built the same way as checkbox
radius base 12px inputs cards panels
radius full 999px pills status dots
radius lg 18px outer slide panel dialog
radius md 10px buttons
radius sm 8px compact controls chips
radius small radii keep the system serious
radius xs 4px kbd day cells
rainbow or viridis as categorical use positional palette
raise keyerror sid callers now wrap every call in try except
raise missing handling only when a real caller needs the signal
ran npm install to fix a missing module could be a typo the wrong directory or a lockfile mismatch
rare knobs are in
rate limit and tenant context become explicit dependencies of the operations
raw card numbers are never handled here
re cut to a seam a user reaches
re evaluates and nobody notices
re explaining it there gives you two copies that drift apart
re read a file before editing it a second time when anything else happened in between
re read the diff and confirm the caller now reads closer to intent
re renders too often the right fix is usually higher up the tree a stable
re slice around the knowledge each module owns
re slice by knowledge
re slice by knowledge the decompose by knowledge check
re slice so each module owns a body of knowledge end to end
reach for a library only when it replaces a reimplementation in a hazard prone class timezones unicode retry and backoff deep clone or equality schema validation
reach for the design since better wording won t fix a leaky abstraction
reach for this vocabulary by default and deviate only when the actual content is from a different domain
react compiler v1 0 opt in memoizes at build time so where it runs the
react fetch in component
react headless behavior hook presentational component
react higher order component for needs window size
react hook form tanstack form
react jsx artifact with prev next pagination and keyboard navigation
react navigation params are serialized so passing a complex user object
react virtual or tanstack virtual
react with tanstack query
react with zustand the grab bag store
react zustand slices per domain
reactive spend fixing a design flaw the moment you hit it while you have the
read a test you can t classify until you understand it
read before shipping prose
read committed the common
read it end to end route handler model test doc
read it there before flagging
read models versus write models become natural splits when one query
read sentence one alone does it carry the answer the number or the decision
read state can collapse
read the audit trail toby feature dev leaves because a review of the diff alone reads half the change
read the behavior record for the area you re touching
read the config value pick the implementation from a map inject it
read the derivation open the worked example run the benchmark never get started never start your journey
read the lengths here not the rhythm these run from one word to a full paragraph on purpose
read the non transitioning panels into a css variable and re measure on resize or a hardcoded dock height parks a button on top of the controls
read the repo guidance and scoped instructions that apply to the changed files
read the request again hunting for a second defensible reading
read the rows compute and write inside fn so every attempt
read the seven before writing the plan s out of scope line or before the first edit where there is no plan
read the stack file for the interface you are designing and open a subject file only when that subject is the contract
read the stack file matching the code in front of you and open a subject file only when that subject is the change
read the stack file matching the code you are placing and open a subject file only when that subject is the change
read their level per concept from the chat and weight their produced work over their stated confidence
read them for the range not for a rhythm to match
reader announcements home end navigation and so on and the caller pays for
reader who needs orgid only reads that one comment
reading files git diff git status ls cat grep tree
reading it as prose misses all of that
reading list table with each entry cite block type one line description
reading pace is deliberate
reading several objects is the design
readme and agents setup steps migration notes
readme md files are sparse by design
readonly coupon coupon null
readonly shipping shippingoption null
reads may be served from cache
reads the grpc status code and applies the table above
reads useauth and a useproduct productid query
real cross cut and the rest move into named modules
real domain objects in memory databases real validators
real environments have a database with someone s actual data in it
real environments have a half finished branch nobody wants force pushed
real environments have a pnpm dev already running on port 3000 with state the user cares about
real failures after 3 retries still failing
real implementation when it s fast enough and stays in process
real interface is unknowable from its file
real motion natural physics comes from real kinematics velocity gravity a little drag weight in the follow through
reason it once climb the ladder
recast cloze the word the learner must produce
recast the design fails if this assumption is wrong
recommendations is the related products list to render below the main
recommendations on mount via the products repository renders a guest
recommendations right now immediately is better than holding the user s
recommendations will be empty and user will be the deep link guest user
recomputed on tier change even if items did not change omitting it was
reconcile py matches processor settlement reports against the ledger
reconcilecart or why those three dependencies
record a baseline change one thing re measure
record candidate values and what the user observed
record criteria 1 and 2 quote a person so both get entries
record failed orderfailurereason reason string details implements orderresult
record filekind makes a missing entry a compile error so the set stays complete
record placed order order implements orderresult
record the question verbatim and stop where the irreversible part starts and never mark a question blocked before asking it
record the result in the chat
rect x 2 y 7 width 116 height 9 fill var toby paper 2 opacity 0 7
recursive or nested structure concentric squares or nested brackets
red is consequence toby accent c44e3f for risk breach finality threshold violation
red toby accent c44e3f is reserved for material consequence a threshold breach a failure a decision point
redefine the semantics from delete it fail if absent to
redefining the semantics is right only when the condition is a non event
redesign as typed accessors per domain
redesign by moving the cache into the data layer
redesign interface expresses intent slice enforces invariants
redesign with a typed result
redesign with ambient transaction context
redesign with intent methods
redesign with the actually needed method only
redesigned interface designed by knowledge whether this client may proceed
redesigning the interface to migrations past this one migration helps
redis delete f product listing product category and this one
redis delete f product product id don t forget
redis it should own caching for this app
redis setex key 600 product to json
reduce complexity in recently changed code while preserving behavior and tests
reduce the number of places that must carry the extra logic
refactors go in their own commit unless the refactor is what makes the behavior fit
reference decks teaching a concept stay sober when the concept needs sobriety
reference interface comments and let them carry the behavior
references architecture md the one file skeleton and the engineering spine
references backend apis md http error categorization retries as masking timeouts as design circuit breakers
references backend apis md java spring go typescript backends
references backend apis md java spring go typescript backends
references behavior record md carries both the entry rules in full and the two branch case
references behavior record md holds what writes no entry and where an inferred behavior goes
references behavior record md what the record skips the entry and binding rules in full the grep retiring and the two branch case
references caching md cache as a deep module invalidation stampedes
references caching md cache get or load contract invalidation methods
references caching md when a cache earns its complexity measurement driven adoption stampede as performance problem
references calibration and testing md measures it random and skilled input must land in different places
references calibration and testing md prove it
references checks md holds the seven things that get built without anyone asking for them
references checks md the seven don t build entries and the eight red flags each under the name a handoff cites
references comedy and narrative md the voice and the story
references copy md carries all four
references cross device md live on desktop and phone
references databases md n 1 patterns transaction retry on deadlock bulk vs row by row index decisions
references databases md repository interface query interface transaction interface
references databases md repository pattern schema as interface orms transactions
references examples artifacts md commits pr descriptions doc headings identifiers error messages
references examples banned writing patterns md overused patterns each paired with the move that replaces it
references examples chat md replies to a person answers frustration pushback status i don t know
references examples code md findings on code architecture naming tests performance
references examples md carries a task group written at that detail
references examples md foundational backend and frontend cases
references examples md foundational cases rate limiter usercard file upload
references examples md python backend and react web
references examples md runs a greenfield cut through all three
references examples md worked explanations across subjects a cold concept with nothing on the page yet a decision inside code already underway an interpretation call with no single right answer
references examples md worked slice cuts for four kinds of feature greenfield and brownfield a plan with steps at approval detail and a filled behavior record with its bindings and a retired entry
references gameplay md how it feels to play
references interpretation md divergent subjects with no single right answer judging a reading on support and precision claim evidence warrant the sharpening moves and the close that retains an argument
references mobile md react native
references mobile md react native
references mobile md react native navigation state native modules async storage
references replace the conditional md carries the reasoning per rung and a worked example of each
references replace the conditional md the five rung ladder with a worked example per rung then react react native and backend forms
references retention md covers it
references retention md high volume recall and language the spaced retrieval loop card quality recasts and comprehensible input the receptive to productive ladder and the across session artifact
references runtime config md has the detail
references runtime config md where deploy config enters validation at boot injecting typed values the composition root
references ste floor md the clarity floor from asd ste100 with before and after pairs and the surface budget
references toby md holds the rules
references toby md the rules
references visual identity md the look
references web md example 4 shows it in full
references web md react solid svelte
references web md react solid svelte
references web md react solid svelte
refetch triggers a fresh load mutate updates locally and reconciles
refill and accounting are internal callers need no notion of
refill cadence and the clock moved inside
reflexive agreement sounds like you re absolutely right fired before checking whether they are
refresh reloads the user from the database
refund amount number promise result void
refund amount number promise success boolean error error
refund chargeid string promise refundresult
refunds require a charge id from a settled charge
refused bequest a subclass that uses only a slice of what it inherits doesn t get an entry
registry when new cases must be addable without editing a central file
regression tests for a fix and the refusal to make a failing test green by
regressions a changed public contract ui state api contract persistence permissions or workflow
reject block the action
reject requests without it
rejected alternatives stack as state tinted alerts
release reservationid string promise void
release reservationid string tx transaction promise void
reload this skill with references toby md and references ste floor md on a bare voice toby voice check the voice voice pass or voice standards
relocating a special case is second best to eliminating it
remains hidden and the operation warm is the named contract
remembered to call multiremove
remove id set s items s items filter i i id id
remove id string items update xs xs filter i i id id
remove id string setcart items xs xs filter i i id id
remove itemid string void
remove temp names flags debug routes scratch files and notes that no longer serve the final behavior
remove the branch the type answers for itself or the common path takes the edge input
remove the retry on 400s it was four guaranteed failures with a delay between them
removefromcart id set s cart s cart filter i i id id
removes the cached value for the key if any
removing it reintroduces double
rename a formatting pass a comment edit
rename it for what it holds
rename temp to renewaldeadline which is what it has held since 2021
rename to fileblock and diskblock
renaming a concept mid lesson is the expensive error
render 200 list items
render 2d canvas 3d or dom
render a not found view which is the accurate state for this case
render return component this props this state
renderitem defined inline as a new function each render child
renderitem item feedcard item item
renders a destructive action confirmation when open is true
renders a modal dialog when isopen is true
renders an application error screen
reopening the design is not
reordering for taste a rename that doesn t fix a misleading name swapping one construct for another of equal length and clarity splitting or merging expressions with no debugging gain formatting a tool owns
repair artifact copy review findings comments docs commit messages and plan wording
repeat for info stable cons unknown with the matching family
repeat only after feedback
repeat stat for each cell 3 or 4 columns total
repeated in every page that loads data each slightly different about
repeated in ordersscreen profilescreen notificationsscreen
repeated setup or branches that can be named once two or more real occurrences where naming them once removes lines
repeated type switch diff visible a switch or if else if chain branches on a type tag kind type field enum role or status into arms that each run distinct behavior and the diff adds an arm to it or adds a second chain over the same tag
repeating the question back is a stall
repetition nontrivial code repeated so factor it to one place
replace it with a map
replace the subject with an unrelated noun
replace what is the smallest edit that does what i need with
replace with a number a range or omit
replace with operations that name intent reserve markpaid and enforce invariants
replica is the repository s choice
replying to an external caller maps it to a category and a safe message first
repo guidance here means a file in the user s repo an agents md a readme a contributing guide
repo guidance is usually written for humans who have judgment about when to skip it
repo guidance overrode caution an agents md instruction is not a license to skip judgment
repo pattern reuse the helper s defaults match the inline code timeouts retries logging caching what it throws
report issues the change introduces
report skipped checks during cleanup when the throwaway work is deleted or folded into the project
report that criterion met unproven and name the run that never happened
report the changed value and expected observation
report the real risks in a change the user asked about and stop
report what you found
reports whether biometric auth can be used on this device and prompts
repository comments are now about the operations only
repository computes it from other fields
repository method has the same tx argument and the same caveat
repository method means adding tx to every call site that ever touches it
repository method that returns orm managed objects
req tenant req allowed and writes a new field
req user is gone and session is a value with a real type
request because the data changes constantly or the cache key is
request body is expected and per request so it rides the aggregation path to
request is whatever the union of all those fields adds up to and no module
request let people cancel an order from the account page
request the search should match on sku as well as product name
request users should be able to invite a teammate and see the invite pending
request we need usage metering so we can bill by seat next quarter
requestparam defaultvalue 0 int offset
requestparam defaultvalue 50 int limit
requestparam optional boolean active
requestparam optional boolean hasmfa
requestparam optional instant createdafter
requestparam optional instant createdbefore
requestparam optional integer hasloggedinindays
requestparam optional list role role
requestparam optional string order
requestparam optional string sort
requestparam optional uuid orgid
requestpermission nativemodules barcodescanner requestpermission
requestreader reads bytes off
require session session action string resource resource void
required for the split to be worth it
required parameter is missing
required silent data corruption that took months to find
requires more implementation but produces the strongest intuition for spatial concepts
reserve full object equality and snapshot matching for cases where the entire structure is the contract config files public response schemas serialization formats
reserve full restructuring proposals for modules that are exported have multiple callers cross a service boundary or are costly to change later
reserve it for a surface that is open to code you do not control
reserve it for moments that earn the weight
reserve items item promise void
reserve items item tx transaction promise void
reserve the full loop for consequential ones
reserve those for code that is exported crossed by several callers or costly to change later
reshape with best of n a checkpoint a story flavored mulligan then tune
residual is no longer random extend the model with a non linear panel angle term
resist any urge to fill the gap by picking a direction yourself
resist exposing internal mechanics config knobs or special cases just because they re the shortest path from where you are
resolution when it s ready
resolve then replay a pure function decides the outcome
resolves the promise instead react never runs it and the camera leaks
resolveuserbyemail email string users user error
resolveuserbyemail is for guest checkout
respond the same way to a request for a rewrite banned phrasing help tone repair or wording help
respond to the weak axis
response can submit the same write twice so a write needs an idempotency
restarted a server to reset state whatever was in there is also gone
restating material that reads itself
result after retries failed and offline isn t the reason
result err fn ctx
result gateway charge paymentrequest
result never throws under normal use
result so callers can surface failures without try catch
result t is one type every caller handles success failure offline
resulting decisions to toby swd modules and toby swd interfaces
results are paginated the response includes the next page
results are paginated with opaque page tokens the next page
retention a named count these 40 words a list or table or paradigm or drill memorize quiz me review success is recalling them later after the session
retire a verb and re grep its call sites
retire an entry in the diff that deletes its test and edit a falsified sentence in the diff that falsifies it
retired 2026 05 02 per merchant cancellation policies replaced the fixed window
retrieve don t re show
retry back to example 2
retry inside the api client rung
retry interval ms as a configuration parameter and let operators set it
retry on a fresh connection
retry rate queue age partner errors throttle ingress ingress 60 two sample rule consequence band entry
retry the whole transaction with backoff after re reading state
retry timeout and test questions raised inside a spike
retry transient at api layer offline banner global
retry volume sits 18 above baseline
retry with backoff only when the request is safe to repeat
retrygrpc is one function
retrying a non idempotent post after a 5xx or a timeout
retrying mutations that weren t idempotent repeat across the codebase
retrying network failures during payment
return a named type and comment the fields data structure member comments
return a purpose built type if the use case is list orders with
return authcontext provider value user signin signout children authcontext provider
return await downstreamclient getrecommendations userid
return await downstreamclient getrecommendations userid timeoutms 200
return await fetchwithtimeout path 10 000
return await this repo insertmany orders
return c stub getorder ctx orderspb getorderrequest id id
return cache getorload product id 600 store find id
return cancelled true teardown
return channel deliver notification user
return class extends react component p windowsize
return classifypaymenterror e owns the ios android difference retries network
return data err l caller does result 0 result 2
return db transaction async tx
return e instanceof networkerror
return email client send user email notification body
return errors is err errdeadlock
return f self first self last
return f self first self last
return f user first user last
return f user first user last
return filtered map p productcard key p id product p
return get width return width get height return height
return handler handle message
return orders confirm order id
return placeorder req body tx
return plain domain objects commit before return
return preview file file
return product from json cached
return productview product product
return productview product product
return render user data orders data
return respond ok orders find id
return retrygrpc ctx 3 func ctx context context order error
return rl counts userid rl limit
return self charge via legacy payment
return self charge via stripe payment
return sms client send user phone notification body
return status completed transactionid native transactionid
return tabs user user navigation navigation
return text start end as characters not bytes
return the revised output first
return this cache getorload product id 600
return this cache getorload product id 600 this inner find id
return this cache getorload product id 600 this store find id
return this cache getorload product listing category 60
return this cache getorload this productkey id 600 load
return this orders find req params id session tenantid
return true if a request from client id may proceed now and
return unknownpreview file file
return uow run async
return uow run async uow injects an ambient tx
return user display label
return user display label the user owns the answer to
return view product q product
return window removeeventlistener resize onresize
return window removeeventlistener resize onresize
returning that is a real need
returning the diff unchanged is a valid result
returns a 5 tuple
returns a flat orderdisplay row per order with all fields needed
returns combobox state and handlers
returns detached domain order
returns early if it expires during backoff
returns either the placed order or a
returns notfound if no such user exists returns
returns object with status field
returns signals for open highlighted filtered accessor props
returns stale while revalidate if a fresh load is already in
returns the cached product or loads it via the supplied function and
returns the cached value for the key or loads it via the supplied
returns the cached value or loads it via the supplied function
returns the current query state for the product with this id
returns user tenant or throws
returns users matching all filters
returns users matching the filters
returns users matching the query
reuse exact names already established for a concept
reuse the vocabulary already in the schema the routes and the ui
reusing a key from a failed
reveals because writing permissions may be undefined for an instant is the
reverse only may lose data
reversibility here means software reversibility
reversible changes exposing state for inspection deferring durable tests
revert the revert the first version was right and the test was wrong
review a change to a skill md an operating guide a hook or an agent config for what it does to every later run
review pr review diff review commit review working tree review or findings on
rewards 30 45 seconds of attention per panel
rewrite it to protect the same useful behavior through a better interface
rewrite prose into toby voice
rewrite that sizes cells automatically and the v1 chore of estimating item
rewriting it into another idiom is churn
rg finds the file
richer than a tooltip lighter than a popover
riders are where hedges foils and softeners live and they always sit at the end
right answer for most components so resist the urge to add a layer
right answer subjects math code chemistry anatomy facts state the result and walk the reasoning that reaches it
right column text still has to read true and complete with the tone stripped
right reasoning thin or absent don t take it yet
right with the reasoning confirm then raise the next ask what would break this
ring of decreasing density around a point
riverpod provider play the context role
rn screen passing a user object through route params
role can be repeated to match any of several
roles set role limit integer
rollback rehearsed 21 may 09 18 owner on record
rolling back is a theory
rolling forward over a downgrade re runs the upgrade against existing
root never per folder and check the existing file for accuracy before
rotation orbit cycle partial orbit arc a closed circle reads as generic decoration
route level boundary aggregates everything that bubbled past per widget
route on the goal and the error
route path dashboard element
route path orders id element
route to toby swd clarity
route to toby swd clarity
route to toby swd clarity
route to toby swd interfaces
route to toby swd interfaces
route to toby swd interfaces
route to toby swd interfaces
route to toby swd interfaces
route to toby swd interfaces
route to toby swd interfaces
route to toby swd interfaces
route to toby swd interfaces
route to toby swd modules
route to toby swd modules
route to toby swd modules
route to toby swd modules
route to toby swd modules
route to toby swd modules
route to toby swd modules
route to toby swd modules
routes carry only what identifies the destination an orderid a tab
routine interfaces take the cheap path in step 4
routine private one caller changed easily in one place thin helper
rowmeta rowchrome five components each a handful of lines each only
rpc mutation that updates any subset of these fields
rule or human facing module usage
run a discovery loop when the behavior is not decided yet
run both over the recent output fix what breaks and keep the rules in front for the rest of the session
run every check against three tests before you ask it
run freely and run it first on any task
run it against today s code and quote the output
run it and quote it
run pnpm test api invites screens members
run pnpm test members list before the first edit and quote it
run scripts rule inventory py against both sides or the repo s equivalent and report any unmatched deletion at its old path line
run t work promise t promise t
run the candidate first in the cheapest faithful proxy available and treat acting on the real system as a state change that needs approval
run the change the way a user would and quote what came back
run the changed code before writing findings as a hunt for what you have missed
run the comment test
run the full test suite before every commit is well meant and expensive on a three line diff
run the hit test in the coordinates you render on this frame s drawn position so what the eye sees is what the math checked
run the same test on that object s contract
run them in this order
run these before writing any slide
run these on the draft
run this checklist before writing any code or markup
run this list against the diff before calling a boundary decision done
run this list against the finished interface before calling it done
run unasked what toby swd environment calls safe inspection or narrow verification the changed function in a repl a scratch script the one test file covering it
run when it matches the task narrowest form
rune based factory or composable
rung 3 again at the render layer
rung 5 when handlers ship independently
rungs 4 and 5 need two conditions at once a case set that visibly grows and case bodies that own state or dependencies
rungs above the client s retry the interceptor the boundary and never
running a migration the user did
running that census means opening and reading an unrelated file end to end
runs whenever the cart contents the applied coupon or the user s tier
s cache delete fmt sprintf daily count s ev date
s cache delete fmt sprintf daily uniques s ev date
s cache delete fmt sprintf org events s ev orgid
s cache delete fmt sprintf user events s ev userid
s cache delete fmt sprintf weekly count s ev week
s cache delete global today
safety relevant findings destructive command warnings
same as b plus large file streaming
same but without using a key
same call applies for solid solid virtual and svelte svelte virtual
same comment test discipline applies
same defect as web prop drilling the different layer check pass through variable with
same form every criterion every slice with the command named beside it
same key written by two unrelated modules
same module different audience
same names which makes it the canonical shallow module
same pattern across all stacks when the comment swells with ordering
same reasoning as the retry interval example in examples md example 2
same result even if the call is retried
same treatment fewer arms because the handling moved
same treatment in solid createstore and svelte writable module
same treatment in solid errorboundary is built in and svelte boundary
sample claim formats with units and intervals
sampled at 1 hz
sampled at 1 hz
sans inter 400 500 600
save it up into a separate cleanup phase
save items feeditem promise void
save p product promise void
save p product promise void
save t tokens promise void
save the depth for the call that carries weight
say in one line that this is experiment mode and what would move it to durable
say it plainly and don t ship a rewrite to have something to show
say means editing every mutating method that could affect that
say none applies when none does
say so when it isn t
say that plainly and move on
say that plainly when you offer it
say which case applies and how you verified instead
say which relation it is
say why you re starting it before you do
saying it is done would be a lie
saying so replaces the evidence with a claim about the evidence
saying this needed approval with no command named is a skipped step
scale specificity to scope
scale to the session a toy needs only state and a reseed a bounded run earns a diagnostic ending a campaign earns a save
scatter of dots within a bounded region
schema decisions outlive everything else in the codebase and orm
schema shaped package private
scope decision features checkout is a feature module one agents md at its root
scope decision services payments is a meaningful module that owns a real body of knowledge so it gets one agents md at its root
scope discipline is part of design so do not wander into cleanup outside the code you touched
scope down to what you can do well inside this change
scope the wrapper to safe methods or require
scoped to the endpoints a currentusercontext provided near app and read in
screen for a single product
screen job or flag or a bug fix whose repair is new behavior
screens call feedcache load and don t know what key was used
screens in the app crashes with an oom
screens that don t use user still take it because nested screens below
script does the former and offers no story for the latter
scripts check single html mjs file runs the rote pass
scrolleventthrottle left at default on ios sends roughly one event
scrollview renders every child upfront
scrollview with many children
search the repo for the operation before calling the code novel
searchbyemail email string user error
searchscreen tsx different shape same call different error handling
seasonal diurnal or threshold banded phases
seat member and active user mean the same thing today and one of them is about to appear on an invoice
second gateway for one specific customer
second guardrail do the reads inside fn
second the interface enumerates every method
secondary for destructive actions to prevent accidental confirmation
secondaryaction label cancel onclick close
seconds longer due to clock skew across cache nodes
section data screen label 01 title
section dividers summaries glossaries formula slides decision slides earn ink
section labels and meta t textmuted on paper t textondarkmuted on ink
section title inter 38 pt 600 t textondark centered horizontally vertically positioned at 38 from top
security auth authorization injection secrets exposure
security auth authorization injection secrets path traversal unsafe parsing csrf ssrf xss data exposure
see callout philosophy below
see checkout context shape in agents md
see references comedy and narrative md
see references components md for full paste ready html
see references components md for paste ready html
see references components md for the components referenced above
see references decks md
see references decks md
see references decks md
see references decks md for deck patterns
see references examples md for backend and frontend examples of both files
see references examples md for side by side tactical vs strategic versions adding a special case a new module s design pass modifying existing code under a deadline
see references examples md for worked backend and frontend cases
see references geometry md
see references geometry md for the 20 named geometric marks and their purposes
see references geometry md for the named marks
see references interpretation md
see references retention md
see settlement ordering in agents md
see the sparkline in a table snippet in references components md for the paste ready pattern
seed the randomness one seedable generator the seed stored in the result so a run reproduces and you can replay a bug
segues repeats the same defect
select qty from inventory where id 1 for update concurrent writers block here
selected current in lists trees menus toby paper 2 background a 3px toby info inset on the left
selected day fills ink
selected items in lists use paper 2 3px info inset
selected row uses paper 2 3px info inset
selected row uses toby paper 2 3px toby info left inset never paper 3
selectedrenderer p productchip product p
selecting the bare method s total or calling
selectinload by product ids
selectinload order line items selectinload lineitem product
selection state aria semantics popover open close and the presentation
selector that calls it usecart s s total so the subscription
self documenting code assumption shipping non trivial code with no interface or field comments
self documenting code reasoning would stop here because the
self evict for product one place that knows what to drop
self validating means the test asserts and does not print for a human to read
semantic kinds match state hues indeterminate available
semantic tokens for status pills only
send it the surviving signatures and interface comments only with no implementations and no hint of your preference
send the comment test predicate too and this rubric in priority order common case caller burden generality efficiency then depth without over hiding step 7
send them to the source could be
sending the signature to the server is meaningful only if the server
sensor s 1 sensor s 2 panel temperature solar angle thermal residual model v3 1 r 0 88 drift trend 0 005 c sample first order thermal model
sentence case for headings and body
sentence case for headings and body
sentence ceilings and the ban on buying shortness by dropping words
sentence into the test docstring run the record check
separate answers mean separate code
separate answers mean separate code and merging them couples decisions that should move apart
separate db every service is touched
separate general from special remove special cases
separate the operations that are conceptually distinct
separately the same server calls malloc equivalent allocation deep in request
separator horizontal 1px hairline or vertical 1px column
serializable makes the database detect the interleaving
serializable without object juggling
serialization and invalidation policy live inside productsrepository or in
server change its internal offset strategy without telling clients so
server dedupes on or the operation is naturally idempotent
server side image processing or a cdn with resize or to an on the fly
serves criterion 1 standing correct on its own
serves many callers and the right author for the hard parts is the module
serves them faster and is smaller
service interfaces rest grpc contracts
service method with dryrun skipx usey flags
service never owns it
service unavailable and lets permanent true bubble up where the
service unavailable which is recoverable
services payments util does not get its own and that content pushes up here
session length endless toy through bounded run with a graded ending
session necessary if you want to return a user object beyond the
session owns the secure storage detail session save writes to
session returns the current sqlalchemy session
session s lifetime otherwise accessing relationships afterwards will
sessions knowing those are derived facts that shouldn t be the caller s
sessions work how to write sqlalchemy filters and how to manage commit
sessionstore expires it after an hour so the caller needs a refresh
set it has degraded into a pass through wrapper
set it to 16 for 60fps
set key string value string promise void
set nx ex around the load
set role roles int limit
set s items s items item
set stores the value with the given ttl default
set t key string value t ttlseconds number promise void
set whichever subset of these fields the caller asked for the intent
setcameraindex nativemodules barcodescanner setcameraindex
setcart c set cart c
setcart items xs xs item
setcoupon c set coupon c
setcoupon coupon coupon null void
setcoupon replaces the coupon callers must verify the coupon is
seterror e as error
seterror request timed out
seterror something went wrong
seterror you appear to be offline
sethighlightedindex i number void
setinputvalue v string void
setisopen v boolean void
setitems items cartitem void
setitems replaces the items array callers must
sets internalopenindex to 1
setselecteditem item t null void
setshipping replaces the shipping option callers must compute
setshipping shipping shippingoption null void
settheme t set theme t
setthemepreference v light dark system promise void
settings screen with 8 sections each built differently
settlement ordering the ledger is the source of truth the processor
setup including the two steps everyone forgets
setuser u set user u
seven middleware pipeline collapses to two or three when each represents a
seven sentences references internal behavior controls the icon color
seven was the connection pool
several candidate changes in one turn
severity is a claim about impact
shadow drops on bars or markers
shallow module interface nearly as complex as the implementation
shallow module interface nearly as complex as the implementation
shallow module the interface costs almost as much to learn as the implementation behind it would cost to inline at the call site
shape as a filler noun sounds like the shape of the response a bug of this shape the interface has the same shape
shape is for geometry and for a typed shape field nowhere else
shaped like its contract list users by criteria with one criteria
shared behavior across many handlers
shared mutable state and ordering between concurrent contexts is complexity like any other
shared signatures are not leakage when each participant adds distinct functionality
shared step state goes on the context
sharpen by constraint and affirm the direction before tightening the aim so it reads as forward motion
sharper test write the module s interface comment
sheet covers drawer and bottomsheet side right left bottom
ship it marked assumed when a wrong guess costs one edit to undo
ship the reversible half marked blocked when nobody is available to answer
shipping at read time
shipping carry invariants the store doesn t enforce
shippingoption shipping string idempotencykey
short scoped transactions don t hold a database connection for
short scoped transactions pool timeout to fail fast
shotgun surgery diff visible one conceptual change forces small edits scattered across many files or classes the diff visible symptom of information leakage running in reverse
should be doing more of the work
should coordinate misses themselves if loading is expensive
should have a caching implementation reach for the lower layer first
should i add a cache
should i add a retry here no
should i add a retry here no
should live in one module
should never carry it
shouldn t accept tx
show the common case first
show the formula the inputs and the computed output updating live
show the name alone with no declaration documentation or surrounding code
show the outcome forming as the player drags locking on release
show the thumbnail first swap in the full
show the tradeoff with concrete behavior or an example the explanation sitting against the thing it describes
show the whole slide at once
show them and keep moving
show them and wait
shows watch consequence boundaries beside a value
shrinking the code below the design while implementing means the design was wrong so go fix the design and say you did
side effect on dependency change
sidebar grouped vertical nav
sign errors unit mismatches and totals that disagree with what got stored surface here and appear in no catalog
signal that the design is wrong
signals all point one way
signals here mean a thrown exception an error return a status code a result an option
silence is a fine choice made on purpose
similar abstractions across adjacent layers usually surfaces as
similar things done the same way dissimilar things done differently
simpleprompt nativemodules rnbiometrics simpleprompt
simpler while the work moved to where it belongs
simplifies every caller so pulling it down is correct here
simulation with run pause the learner presses run to advance a simulation in real time and pause to inspect state
since a downstream body can carry stack traces or query text rung 3
single anchored ellipse with a body at one focus
single copy there stay authoritative
single flight at most one in flight load per key per process
single flight if a load is in flight for this key await it
single flight in process is the cheap default locking and stale while revalidate cost more
single flight per process at most one in flight load per key per
single flight stale while revalidate but they re additional
single getorload key ttl load plus rare warm
single huge errorboundary at the root
single index to add
sites variant for free
six cache keys that derive from it
six confident looking findings that turn out fake waste more time than the bug would have
six operations expressing intent but the implementation owns dedup
six sentences describes invariants about combinations of fields treat x
six stops on a six slice feature is the skill running for its own benefit
size gameplay critical things to be seen and hit at arm s length
size the report to the change
size up mid run when a trigger you did not see fires and say so in one line when you do
sized via clamp 24px 3vw 38px
sizes display 34 h1 28 h2 22 section 18 body 14 body sm 13 meta 12 eyebrow 11 mono 13
sizes md 16 lg 22
sizes sm 22 md 28 lg 40
sketch at least two structural approaches
skew the ratio of interface cost to implementation benefit heavily toward the implementation side
skill md owns when an entry fires and the check that runs before the handoff
skill md owns when each fires and what the report has to say
skip are you sure hand them the next slice on that exact point good you write the guard for the empty case producing it can t be faked
skip how it works
skip it for a one
skip it for a pure code edit with nothing to
skip it for a throwaway parameter
skip it for a throwaway spike
skip it for code comments and docstrings which
skip it for module documentation files which toby swd docs owns
skip it for work
skip it when none of those artifacts exist
skip it when the change moves no behavior a
skip it when the question is what one signature exposes
skip it when the question is where
skip it when the user asked for findings and no
skip it when the user asked for the code to be changed
skip test first when the design is still being worked out
skip the ask for mechanical filler and one answer steps
skip the cache where it isn t
skip the grid entirely on small charts sparklines tile sized small multiples
skip the legend when direct labels exist
skip this only for changes trivial enough that there was no real design decision
skip when only one series
skip when the parameter is categorical or when the interesting thing isn t the gradient and use a toggle or step through instead
slice 2 of 3 names the counting
slice by real knowledge boundary auth cart theme
slice one depends on slice two to be worth anything
slice one ends on a boundary carrying a real question
slice one of the invite flow
slice s method surface are read by every consumer
slice two at jobs index ts 17 the invite created subscription
slice two s last checkbox deletes it
slices one an open order can be cancelled from the account page
slices one search matches a full sku
slices two named for what each one lets someone do invite shows up as pending then the invite email opens the accept screen
slices two the cli reports a day s seat count bin usage show workspace x date y then the usage page shows it
slide aspect ratio 16 9 enforced via css aspect ratio 16 9
slide count grows with the material but keep the deck to the smallest count that preserves the concept
slide id at bottom right
slide width fills the artifact viewport
slider continuous parameter a scalar value governs a visual or numerical outcome
slider mono value displayed right of the label tabular numerals
sliders are one option among many
sliders are one option among many
slightly longer code that makes a state change visible
slope 0 005 c sample
slow is a feeling and numbers are a baseline
slow non critical dependency on critical path
slow query in production
sm for 28px height
small caller facing interface
small eyebrow above the claim in toby accent one of the few places consequence red is allowed without indicating risk because it marks the deck closing
small uppercase eyebrows mono values
smooth band around a trace
snapshot blob large auto updated string nobody reviews on change
snapshot blobs that nobody reads are not tests
snapshot tests have to be careful about ttls
so a design smell that needs a boundary fix walks past silently because fixing it isn t this skill s job and nothing tells it to say so
so both classes know the
so invites form defaults off slice one is observed with it on and the flag flips for real when slice two s send lands
so retries don t synchronize
so stop grading the conclusion and pressure test the path to it
so take the tactical path deliberately and label it
so the highest yield point in software is whether the module boundary holds when the next change arrives
so the transient handling above never sees it
so the two cannot be confused at a glance and better still give them distinct
soft pale bg tint mid border text saturated dark fg
solid and svelte sidestep the
solid and svelte versions work the same way signals or stores wrapping the
solid composable returning signals
solid equivalent shape with createstore or signals a module
solid forms tanstack form
solid same idea built on signals
solid tracks it on read cart total in jsx re runs when items
solid with tanstack solid query same shape different reactivity
some are ink forward with paper used for contrast
some asks arrive with no code equation or passage already on the page because the user is asking about the concept itself
some can be minutes stale product catalog
some costs show up only in aggregate
some failures can t be defined away masked or aggregated and stopping would make things worse
some findings have a second fact that makes the point on its own
some generality is the target
some have no ink at all
some messages carry one
some surfaces hold that line harder than others
somebody who has never opened this repo can still tell whether the software does this
someone says we should
something elsewhere might describe this isn t checkable
something wrong and sustained 5xx real outage
sometimes a written artifact helps a labeled list of options a sketch of the space a map of the set boundary
sometimes it reveals the current design no longer fits
sometimes prose is enough
somewhat general purpose framing applies
somewhat is the operative word
sort a b b total a total
sort email order asc limit 50 offset 0
sort field email order asc
sort must be one of email createdat lastloginat
sort once when orders are loaded so every render reads the sorted list
sort one allocation per pass
source api orders ts 31 the ownership check every other order route runs
source billing invoice ts 52 which throws on a gap
source model note below the chart meta size 12 px text muted
source of slowness postgres query joins product variants 400ms
source prod traces 2026 04 12 to 2026 05 10
source the ambiguity pass on match on sku as well shipped assumed
source the existing contract
source the existing contract
source the existing search ranking suite which is the contract
source the user sentence or ticket line it came from quoted or the repo fact that forces it cited at path line
source to a repo fact only where the request is silent on that behavior
source uri thumbnailurl user profilepictureurl 80 80px thumbnail
source user cancel an order
source user invite a teammate
source user no cancelling once it s out the door backed by api orders ts 88 the shipped state guard refunds already run
source user on the follow up about resent links once i resend the first link should be scrap
source user see the invite pending
source user so we can bill by seat
source user someone joining and leaving shouldn t double bill them
source user the search should match on sku as well
sourced to toby swd strategy
sources at once network render user input and performance pressure
sources production traces last 30 days n 2 6m requests
space 2xl 32px dialog padding
space 3xl 40px empty state padding
space 4xl 48px major separation
space 5xl 64px section gap
space base 16px row padding card body gap
space lg 20px cell padding dialog body gap
space md 12px tight control padding
space sm 8px inline gap badge padding
space xl 24px section padding
space xs 4px gap between tightly coupled siblings
spacious argument one concept per section
span class pill pill watch watch 18 span
span class pin label periapsis closest approach span
span class pin line span
span class pin num 1 span
span markers tagged with a value band
span style font family var font sans font size 24px font weight 600 letter spacing 0 012em toby span
sparkline in table embed an 80 120px trace svg in a table cell
sparkline inline trace svg 80 120px wide
special case code sits off the path structured for simplicity since it no longer runs on the hot path and gains nothing from being fast
special general mixture caller specific conditions inside a general mechanism
special purpose conditions that exist for one caller don t belong inside a general mechanism
speculative complexity added for performance with no evidence it matters
speculative generality a parameter hook or extension point added for a future that has no named caller yet
spend effort in proportion to how expensive the interface is to get wrong judged from signals already in front of you
spend it all at once on a redesign and you have left the
spend it anywhere else and it stops meaning anything
spike a proof of concept a parameter sweep a throwaway debug surface a
spike and any change whose structure the surrounding code already fixes
spike proof of concept or throwaway which belongs to toby swd experiment
spikes 1 s may be lost
spinner 1 5px ring 720ms linear no glow
spinning past the ceiling burns quota without converging
split a two part answer
split into operations encode policy on the entities themselves
split only when the resulting pieces are independently understandable and each interface is simpler than the original
sqlalchemy concepts session query expunge
sqlalchemy query for users
stable watch consequence unknown drift residual breach threshold sample pass two sample rule watch band consequence band named rollback owner controlled release defer escalate throttle
stablesoft e3ecd5 stabletint bcc89e stabletext 2d4818
stale comment beside changed code a comment sitting right next to code the diff just changed still describes what the code used to do
stale comment beside changed code diff visible a comment sitting right next to code the diff just changed still describes the old behavior type or condition
stale is encoded in null
stale listings then appear in production
stale profiles to push hit rate up
stale while revalidate because it sounds clever
stale while revalidate continue serving the stale value to most
stale while revalidate is a third documented choice
staleness and signed in vs guest are owned by the screen itself which
staleness budget 5 minutes acceptable
stampede protection stale while revalidate single flight per process and
stampede risk a popular key expiring at the same moment thousands
standard caveats that fit toby artifact s caveat next to the claim rule
start a beginner with five to seven items and add more as they stick
start with the decision
starting stopping or restarting servers workers databases containers queues tunnels watchers
starts from what the database currently holds
starts or stops a process takes a port installs a dependency runs a
startscanner nativemodules barcodescanner startscanner
stat before content position absolute top 0 left 0 right 0
stat delta down color var toby accent
stat delta flat color var text muted
stat delta font family var font mono font size 12px
stat delta up color var toby stable
stat grid 3 grid template columns repeat 3 1fr
stat grid 4 grid template columns repeat 4 1fr
stat grid display grid border 1px solid var border hairline
stat grid up top
stat label font size 11px text transform uppercase letter spacing 0 12em
stat nth child 4n 1 stat accent var toby info
stat nth child 4n 2 stat accent var toby stable
stat nth child 4n 3 stat accent var toby watch
stat nth child 4n 4 stat accent var toby slate blue
stat padding 20px border right 1px solid var border hairline
stat source font family var font mono font size 11px
stat unit font family var font mono font size 12px
stat value font size 28px font weight 600 font feature settings tnum
state and exposes it read only for rendering
state assumptions explicitly use known unknown baseline assumes labels when the assumption is doing work
state each cross module decision once here and point to it from affected code
state hidden from the user while asking for feedback
state in the handler s own comment when and why it is invoked since the call site isn t visible from the handler
state it as one
state that second fact plainly and leave it there
state the change in one sentence including the obvious near future variants today it s one provider tomorrow there will be three
state the one design decision or piece of knowledge each module encapsulates
state the responsibility each file owns
state tinted components always read as one color family soft bg tint border text fg un soft left strip
state what changed and what should be observable
state width window innerwidth height window innerheight
statgrid kpi grid 3 or 4 columns hairline dividers 2px top accent strip cycling through the chart palette
status completed transactionid string
status discriminator names the three legal states in the loaded state
status error error error
status error product null error error
status failed reason card declined
status loaded order order actions orderactions
status loading product null error null
status locked retryafter date
status returns the current capability prompt
status stale product product error error
status success product product error null
status to component record status fc over a switch status in render
status unavailable reason unsupported
statuspill dot label fully tinted by hue
stay in the widening posture until the topic settles which means the user picks a direction says they have what they need or starts asking for implementation
staying alive required prior authorization
stays working the members list still renders for an org with no pending invites
stays working the order list still returns shipped and open orders unchanged
ste exists because a technician holding a wrench has to get the sentence right the first time in their second language at 3 a m
ste rule 5 5 notes give information never instructions
steelman the opposite interpretation
steelman this and say the warrant are worth naming as moves because the learner needs the verb to grab
step through stepper a button advances a process one step at a time
steps do not know about each other
steps one component per step each renders none owns flow control
still stuck name the category of the error
still stuck tell them then circle back to the same idea a slice later in other clothes
stop 1 the criteria
stop 2 the plan
stop 3 a slice boundary
stop as soon as one candidate passes for a routine interface two pass for a consequential one or you reach three candidates total
stop at the first rung that fits
stop in the safest reachable state for errors no caller can act on resource exhaustion unrecoverable i o a violated internal invariant which means a bug record what diagnostics you can and bring the unit to its safest available stopping point behind one checked wrapper so call sites don t each repeat the check
stop inflation a checkpoint at every slice boundary regardless of whether a decision is waiting there
stop only what you started
stop when the next change doesn t clear the countable win bar
stops criteria only one line keep moving
stops criteria then a plan file since this spans two slices
stops criteria then the design pass then the plan
stopscanner nativemodules barcodescanner stopscanner
storage and bridges to platform apis
storage row format can be changed without touching anything outside
store pop sid none
store slices repositories controllers native modules screens or data
stored key the key is keyed to the device so signatures are not portable
stores and retrieves products
strategic sketch two approaches first
strategic when any of these fires a new module or boundary a public api event or persisted format a migration auth permissions billing money or privacy a behavior three or more call sites depend on a ui workflow crossing more than one screen greenfield
strategic work fills all four before designing and keeps reading while any is short
strategic work runs it against today s code first and quotes the output at stop 1 because that starting run is what makes the criterion provable later
strategies each keeping the discipline in one place
strategy references examples md reaches this same design for the two gateway task
string fields the empty string is treated as clear this field if and
string keys constructed at every call site
string user id 1
stripe key z string optional
stripekey parsed data stripe key
strong asymmetry toward internal means deep and rough parity means shallow
strong modifier uses border strong
strong on all three confirm it holds then raise the counter it hasn t met
structural choices stay wide dense reference card spacious single claim ink heavy authority piece paper first reading surface animated process static evidence panel three slide lesson or twenty slide reference deck
structure one lock so there s no ordering left to get wrong
style nits a formatter or linter catches
style tokens first then layout style
subclass authors must read the parent to know what they inherited
subdivision cost the split merge check classitis five interfaces to learn five files to
submitting an email on the members screen shows that address in a pending list
substitute breaks its contract an override or implementation contradicts what the base type s callers rely on it throws where the base returns refuses input the base accepts drops a documented side effect or return value or ignores a parameter the base honors
substitution you said the ending is powerful if i swap in sad or memorable is your sentence still true
subtle background grid for diagram alignment
subtle bugs forgetting the retry retrying the wrong errors
succeed and then time out our connection
suite a type check on the affected package
summarize it say why the repo recommends it ask unless it is narrow and cheap
summarize the command in one line
supported but vague sharpen it below
supported is there evidence behind the claim or is it asserted
surface card fffefa alias of paper 3 for card backgrounds
surface it as a real result the caller acts on
surface it as a typed result so the component can
surface it as part of the contract instead a result
surface than a bare row limit and buy correctness under concurrent load
surface the boundary what s in the set what s out what s adjacent
surface the gamble before it is taken as a number and a green amber red read on the thing about to happen
surface which toby swd interfaces owns
suspended users but includes pending email verification users from 2023
svelte 5 a factory returning runes actions
svelte 5 rune or with stores a writable
svelte 5 with tanstack svelte query
svelte all three frameworks ship an integration
svelte forms tanstack form
svg width 120 height 22 viewbox 0 0 120 22
svg width 44 height 44 viewbox 0 0 64 64 fill none
swap to the existing pattern only when two or more independent call sites already use it
switch alias of toggle
switch when the learner names a count hands you a list or says drill memorize quiz me or review
switch when the learner wants to say write or speak the target language
switching orms is one module s problem
system runs fast without per query heroics
t 04 12 utc t 18 t 42 mission elapsed time
t accent red for consequence callouts only
t ink2 background no border 24px padding white on dark text
tab screen name feed component feed initialparams user
tab screen name profile component profile initialparams user
tab screen name settings component settings initialparams user
tabs info soft tinted active tab with 3px toby info underline
tactical interface the one to reject
tactical otherwise which is most work
tactical tornado large volume of working code fast each piece adding a special case or dependency
tactical work has one slice and owes only the four ship conditions below so read those and move on
tactical work names the command and runs it after
tactical work owes the first and the third
tactical wrap the call site in a for loop with a sleep
tagged anchor at a precise x y
tagged span from 0 to a labelled magnitude
take a reviewer s report of confusion over your own read of the code however clear it looks to you
take each significant design decision or implementation detail a file format a wire protocol a storage layout a policy
take it always for lists that might grow
take the reading this repo already follows and carry the reading taken the reading dropped and what changes if the user wanted the other one
take the second only if most callers need just one of the results
take the tactical path when
take them on every list you write
tanstack query for solid tanstack query or tanstack svelte query for
target a design that survives the next change
task a dialog component used in form confirmations destructive action
task a module to rate limit api requests per client
task a usercard used in a list a profile header and a search result
task add retry logic to an http client call
task an interface for a client to upload a file to storage
task due tomorrow support a
taste calls need labels a note reading smells wrong and it resembles the cache coherence bug from m 03 is a valid signal in an investigation artifact
tax cannot be shown until an address is validated legal requirement in two
tax display is blocked until address validation completes
tax service unless skiptaxvalidation is true used for tax exempt orgs
tbl num tbl th num text align right font family var font mono font feature settings tnum
tbl td padding 14px 16px border bottom 1px solid var border hairline vertical align middle
tbl th text align left font weight 600 color var text muted
tbl tr last child td border bottom none
tbl width 100 border collapse collapse font size 13px
td class num 0 54 td
td class num delta down 5 3 td
td drift posterior td
teach in depth when the idea pays off past this one spot and the learner is at a seam a step done a problem solved a section opening
teach one verb in one line no tutorial wall one line in the game s own words drag to aim release to shoot and the rest is discovery
teach or prepare the next slice at the right rung
teach the decision make them do the reaching and keep the task moving
teaching artifacts reference modules anything paired with personalized teaching skill
teaching prose is a first read surface
teaching prose mid explanation
team can decide whether to invest in the deeper abstraction or accept the
team finds the bug at 1 000 items and panics
teardown sub remove stopnative
tell the learner where the checkpoints are
temp fix has 40 call sites and the oldest is from 2022
temporal decomposition structure follows execution order so one body of knowledge ends up scattered across stages
temporal decomposition structure follows execution order when it should follow the knowledge each module owns
ten items means ten round trips to a service whose p95 is 240 ms
term uppercase eyebrow mono tracked left value mono tabular numerals right
terse and two sentences long reads as a voice the first four times and as a tic by the tenth
terse sure fine after a check engagement is dropping so back off and move the work
test complexity tests now need to handle cached vs uncached
test deleted to make the suite green almost always the wrong move classify first
test every name by guessability
test fails on a correct refactor coupled to internals
test invite appears pending fails if the address needs a reload
test it live on a phone both orientations with a touch player
test name describes a call calls x with y names a mock log
test the comedy too every counter the ending reads is one the run tracked every thread advances the feed never goes silent
tested at n 10 events sec
testimonials social proof counts and as seen in rows are out unless the artifact s purpose is to surface those sources
tests at the boundary survive refactors
tests inject a fake implementing only that method
tests need cache reset between cases
tests not run because the local setup didn t allow it safely
tests that reach into internals do not
tests written for behavior still being discovered
text in the list
text is annotation and label
text on dark fdfaf1
text on dark muted a8a39a
text transform uppercase font size 11px letter spacing 0 08em white space nowrap
text transform uppercase letter spacing 0 12em font weight 600
textarea multi line input optional maxlength shows mono char count bottom right
tgrp btn on fills with ink white
th 12 sample trace th
th class num last th
th class num th
than the slow choice use originals when the api supports it
that already owns the data
that comment is six sentences
that connection is held for 500ms
that covers commits pr descriptions doc headings and first lines variable and function names and error messages
that covers toby swd experiment user feedback a proof of concept a spike parameter tuning design exploration and manual observation
that dependency has had two cves
that discipline is the gap between an agent that s useful in a real codebase and one that s only useful in a sandbox
that distinguishes loaded empty from failed even though that adds to the
that exemption marks the constraint as one for procedures
that exists read only investigation or explanation and anything the user framed as a
that failure is what toby swd modules exists to prevent
that gives up get s caching and idempotent by method semantics so keep the
that guarantee goes in the comment
that import is the hidden dependency rule 1 removes and it defeats testing
that is a design signal
that is a design signal so split or rethink it
that is correct here because this is the decorator case
that is experiment mode
that is most of it
that is rung 2
that is the bar every session
that is the copied conditional smell with an environment variable as the tag
that is the floor
that is the gap
that is the interface most caches
that is the same leakage pattern as example 3
that is the signature plus the informal contract only comments can carry behavior side effects ordering constraints errors
that is the test failing on three counts at
that is what earns the line
that is why card data is exchanged for a
that knowledge is a single design decision living in two
that knowledge what an
that means a promise you cannot keep or a parameter you did not need
that modify state are atomic and return after durable commit
that name is the plan s group heading the stop line and the row in the final report so the user reads one phrase from approval through handoff
that named flaw seeds the next candidate a structurally different decomposition that removes that specific problem
that occasionally throws on bad data shouldn t take out the page
that output moves the design backwards at speed
that produces a hidden per call performance interface
that runtime cost sits on top of the cognitive cost
that s 1 second of network latency before any actual work
that s a deliberate split per the split merge check and most apps don t need
that s a full file read this skill doesn t do during a cleanup pass so flag the override half under the existing red flag and stop there
that s a race condition and the eight passing runs are why it s still here
that s a real
that s a shallow interface because it has no asymmetry
that s an implicit caller obligation that doesn t appear in any
that s manual qa again slower and written in yaml
that s the definition of a
that s the trade
that s their job
that scans repeats it
that ships both ends up with a wide and shallow interface
that the shortcut is now visible bounded and carries a stated exit
that tries to be every combobox
that ttlseconds is enforced approximately values may live up to a few
that upgrades the reader s map and leaves the reader out of the sentence
that used the same pattern
that velocity degrades design so do not count it as progress
that web react doesn t have navigation state across screens persistent
that work is the code in front of you or the concept just asked about
the 1 2 envelope of a fit
the 12 factor rule is to read it from the environment
the 200ms timeout is a bound on the worst case
the 24 hours came from one merchant s support queue in march
the 27b 6 joke works because 27b 6 sounds like a real form
the 400 well clear of the crash path
the 400 were three copies of the same loop
the abstraction is ready for a variety that never arrived
the accent shows up as the section number the bottom rule strip under the heading and small cell id pills
the actions object a button that exists when permitted is impossible
the actions return a result object
the active definition stops being encoded at call sites
the active flag cannot be skipped pass true or
the adjacent feature and the adjacent bug the thing the request implies and the defect you found on the way to it
the advance has no ceiling
the alternative interface which surfaces outages explicitly is also
the animation plays it back and changes nothing
the announcement is the sign you might not be
the answer determines density and opening structure
the answer determines surface and density
the app pulls in a dependency to check whether a number is even
the appeal is granted
the appeals committee is gerald
the application the row by row code can stay it s clearer but at the
the approved word dictionary ste s own explanatory prose is exempt from ste
the artifact asserts more than it explains
the artifact reads as paper first but the ink anchors give it authority at specific moments
the audit log write is the risky part
the author reads a p1 and answers this is a nit get over yourself and they are the one who can tell
the author s mental model usememo prevents the re render
the author was already inside this question
the avatar is now 4kb
the banned lists cannot catch this because it is made of legal words
the banned lists catch that
the banner is one place that knows about offline state
the bar for splitting is higher than instinct suggests
the base is several focused
the baseline run matters more than usual here because criterion 2 is a claim about what search ranking printed before the first edit
the batch importer and the admin tool call insert and get correct
the behavior is one deep module the hook composable factory and the
the behavior record at the bottom is what these four left behind which is why it holds two of them and one retirement
the behavior side effects units invariants and reasons a reader needs live in the designer s head and have no representation in the code itself
the bodies of these middlewares belong to
the body explains qualifies or shows the work
the body shows the work
the both sides dodge sounds like there s no one size fits all answer every situation is different the move take the position the evidence supports and name the one condition that would flip it
the boundary owns the check
the branch still exists
the browser handles this without breaking a
the build fails before it gets there on an unrelated type error in billing types ts
the bulk update hides both
the bulk version is no more
the burden sits on the edit so the default is to leave the code alone
the burden sits on the thing you want to say so the default verdict is silence
the cache adds 2ms per request and saves time on 5 of requests
the cache adds 5 lines of code at the call site and 50 lines of
the cache calls live who owns invalidation and whether the cache or the
the cache decision is a complexity decision
the cache earns its complexity
the cache feels right
the cache interface is unchanged
the cache invalidate key string interface accepts any string
the cache key schema ttl
the cache never returns stale data if it cannot serve a
the cache owns single flight per process and
the cache s interface determines how much of the caching mechanism leaks
the cache s interface has not changed
the cache s interface offers
the cache should be deciding load through is the default
the cache version adds memoization and invalidation that
the cache wasn t going to help much
the cached path adds a redis round trip 1ms to every read
the call is not idempotent so a retry double charges
the call sites that would notice the change
the caller currently orchestrates
the caller must compose with platform quirks layered on top
the caller must fetch a
the caller must know about
the caller spreads the prop bundles onto their elements and
the caller writes one line
the camera tracks the moving actor so focus does not teleport on a phase flip
the canonical starting cases
the catch block turns the error into null and hands it back
the category most often missed on frontend state
the cause of bug 2293
the ceiling caps the sentence
the change re solves something the repo solves elsewhere a helper util base class hook decorator error type config accessor
the change sits inside a reached path so the fourth slice condition is met by the existing call site at search query ts 60 cited
the change takes effect after verification
the cheap checks apply on every edit naming comment presence obvious on read
the cheap design move first no special case
the cheap design time
the cheap layer and they prevent the death by a thousand cuts case
the check belongs where every path has to cross it
the check has to execute the new code path so a pair carrying only an after proves the harness runs
the check rides in the where clause so every writer enforces it identically
the check that never entered the file a criterion reported met by a command that never reached the changed lines
the checks above are cheap and apply on every edit
the choice is now informed
the choice is part of the interface
the chosen verb fills with its semantic color
the chunking logic lives inside the bulk method which keeps
the clarity fix here is also a
the class decides whether to run run narrowly or ask first
the cli is what keeps slice one from being a layer cut nobody can open
the client times out at 3
the closing slide is one sentence and a frame
the code is illustrative
the code is illustrative and the reasoning transfers
the code says what
the code should live which toby swd modules owns and skip it when
the code under it does not
the codebase names handlers handlesubmit handlechange handlerowclick
the combobox that grew
the comment carries it no functions no live references no cycles
the comment has to describe the
the comment is long because the interface is setx for each field but
the comment on ordersrepository insert
the comment on this interface
the comment requires a sentence about transaction threading
the comment test applies to each
the comment test applies to parameter objects too
the comment test fails on these in predictable
the common case stays in the main signature required and simple
the common case then specifies nothing
the common state is true but mushy and the gain is making it cut
the compiler re renders as it always has
the complete contract is two sentences and names none of its internals
the complexity concentrates in one
the complexity is real a background refresh
the compliment sandwich sounds like a real criticism buried between two pieces of praise so it can t be found
the component owns rendering filtering
the composition over inheritance check applies here
the composition replacement looks nearly identical across the three
the composition root is the one place that knows both the config values and the concrete classes
the composition root main a provider or factory and a framework sanctioned owner such as a data hook that owns its client are where wiring belongs
the concept deleted has leaked into name formatting
the concept governs the interaction type
the concept governs the interaction type
the concrete instance technique above serves an expert and a beginner the same way so a wrong read costs less than leading with an abstract definition would
the config module is deep the interface is a handful of typed fields
the connector is where novice arguments collapse because the writer thinks it is obvious
the consequence is that the change and the agreement disagree and the reviewer can quote both
the constraints protect quality and structure stays free to vary
the consumer pays for one method s worth
the contract should look like one operation and absorb the seven validations
the control does not move
the control does not move
the copy inside is not promotional
the core is deep
the cost is bounded
the cost is real one interface to keep stable one class per case a selection site
the cost of getting it wrong shows up months later
the cost over the tactical version is a dict and an
the count ceiling below applies to each pass on its own
the count that would break this does the color ever shift with no emotional beat nearby
the crash still happens two lines later in the caller
the credential flex sounds like in my fifteen years building systems at scale
the criteria before the first edit show the list under the heading what done means for task then the slice cut by name
the cross cutting concern lives in one place where
the cross module note about context contents lives in agents md
the cure is bulk apis at the boundary that crosses the network
the data interface productsstore has the same comment as before
the data layer fails the slice bar twice its only demo is a passing test suite and the thing the user asked for is three diffs out
the data layer has a few stock interfaces repositories query builders
the data layer is one of the highest yield places to get module boundaries
the data layer is where naturally efficient design and death by a
the day active gets redefined excludes
the day you change one say you want errors to
the deadline is gone and it won t sleep past it
the deadlock retry logic lives at every call
the debounce eliminates work the user can t perceive
the decision making artifact for a should we cache this thread should
the decision the rest hangs on and why it wins here
the decision what key holds the feed
the decisions how do
the decoded bitmaps are what exhaust memory
the deep interface accepts a typed identity
the deep link case has
the deep module is a query library tanstack query swr for react
the deep module is the
the deeper interface expresses intent
the deeper interface inverts the contract
the deeper move don t fetch the
the default and most of this file
the default behavior of taking their words literally is the failure mode here
the default constructor and the builder methods make the common case easy
the default get set delete surface looks
the default move on any state change is to inspect report and ask that covers killing processes taking ports restarting servers running migrations installing packages broad linting full test suites snapshot updates and anything that touches credentials or external systems
the default of let the
the default output is conversation
the default output is conversation
the defect the reader cannot know where the request ends without parsing the
the deleted concept has leaked into name formatting
the deliverable is a system whose design is at least as good after your change as before it
the dependency nobody approved that is a decision about their lockfile their build and their security review
the depth gain each intent method enforces invariants the bindings layer
the depth gain is substantial
the design pass cost a minute and a near future variant there will be more
the design pass scales to the size of the decision
the design question is where that read happens and what form the values take after it
the design time choice eager load hints purpose built read models
the design time fix a bulk path through the service down to a single
the design time move lands before any measurement
the design time moves are cheap and apply on every edit
the detail in each step is what makes it reviewable
the device backgrounds the user has 8mb of ram left and a 6 year old
the device supports biometric auth
the diagram question above is the only check that does
the dialog traps focus and restores it to
the diff adds a general purpose constant or helper inside a specific unrelated class and nothing about that class s own job explains why it owns this
the diff adds or changes a function that writes into a parameter it received as its way of producing a result where a return value would say the same thing
the diff s stated intent is one change but the edits inside a single file or class serve visibly unrelated purposes a pricing rule tweak and a display copy tweak in the same class
the diff touches many files with small similar shaped edits a field added to n structs a case added to n switch statements that all encode the same underlying decision
the diff touches two or more modules to express what is really one decision
the discipline before adding any cache
the discriminator eliminates the what does undefined mean problem
the discriminator removes the what does null
the dispatcher applies it
the dispatcher never names them
the don t build catalog runs while scope is still being fixed
the downgrade loses any phone numbers stored after upgrade
the downstream is called frequently so the breaker has signal to act
the downstream is fail fast so a quick error from an unhealthy
the downstream s failure mode is slow so calls hang and burn the full
the downstream service is slow today 30 seconds against a
the dread register at the dark end the machine inverts
the drop rule above still governs three lines you can fill from the file or there s no finding
the dropped subject is the giveaway
the edge per class
the empty qualifier sounds like a named audit the actual result a given function the specific problem
the ending convicts a verdict sitting outside the graded rank scale used elsewhere
the ending reads the run back track three to five things during play what they optimized what they sacrificed their tell
the endpoint is now
the error case is gone every call site loses its handler and the function is
the error cases collapse into the
the error is never set on 404 and null while loading or failed facts cannot
the error ladder applies directly
the error ladder failing at rung 3 aggregation
the error ladder isn t applied so everything propagates uniformly
the error ladder rung 2
the exact same block repeated in orderpage customerpage invoicepage
the example games ship an older token prefix so rename it on copy
the examples in references examples were rewritten to spread across lengths and openings on purpose
the exception forces a try except
the exception is a record that exists deliberately as plain data with the behavior over it owned by another module
the exceptions matter as much as the criteria
the existing examples md covers two foundational web cases prop drilling
the expensive ones are converting every instance of a convention and adding a design notes file
the failure it heads off is a bare undefined surfacing three layers down under load
the failure mode for any design system is convergence every artifact starting to look like every other artifact
the failure mode is the same
the failure this skill prevents is the nine file diff that arrives finished every decision already made inside it
the faux humble disclaimer sounds like i m no expert but just my two cents ahead of a confident lecture
the feed flashes a line every frame fire on a real change paced references comedy and narrative md
the field mask requirement once and accept the trade
the fields aren t relations
the file describes implementation mechanics so it will rot
the file still has to be opened
the file that owns the behavior and its test
the file that owns this behavior today
the files processes ports databases credentials browser state terminals background jobs and dev servers belong to the user
the filter object s schema versioned documented separately from this
the filter sort runs on every keystroke
the fingerprint sensor s
the first gains a branch every time the domain gains a case
the first question slow how
the first takes a new arm every time the domain gains a case
the first tells them a mood
the first three are pass tests
the first word doesn t have to applaud
the fix for unavoidable surprise is a comment where the reader will hit it
the fix is a batch api at the bridge
the fix is a better decomposition
the fix is to extract a smaller module with a real interface then test through it
the flag is named enable new checkout v2 final
the flag is set in one
the flat read rule binds everywhere
the floor is the same in any genre
the floor warm paper over near black ink and exactly one red that means consequence a denial a death the one dangerous button
the format knowledge is now in
the fourth is a cut test applied afterward
the freshness guarantee is a
the functionality of a module reflects your current needs but the interface should support more than one
the fundamental fix is at the design level
the further this content sits from the code the longer it stays accurate
the game has no skill in it and that is a structural hole the tuning pass cannot reach
the game s own copy is a separate register you set funnier and warmer and louder
the gap is the butt never the person
the gates below say what each one is
the global voice rules carry into artifact copy
the glove meets the ball where the player sees them meet
the grab bag form made them reach
the great perfect sure transition tic sounds like every paragraph opening with an enthusiasm word as glue
the guardrail check is anything the caller needs
the handler catches exception and logs at debug
the handler now invokes three named operations each owning its knowledge
the heading is the identifier so the check is one fixed string grep per entry and there is nothing to allocate increment or collide on
the heading is the identifier so there is nothing to allocate increment or collide on
the hedging stack sounds like this might possibly be a potential issue in certain cases the move this breaks when the list is empty three hedges in a row mean you haven t checked yet
the herd though the system is far slower than the uncached version because
the hidden contract is that callers of the underlying
the hook does not own the state
the hook returns five named fields and
the hot path goes from cached fast to slower than uncached
the http status codes already carry a category signal so use it
the human can t see your design reasoning in a diff
the human grade bot models human slack release windows reaction frames or it reports a number no human will see
the idempotency key must be unique per attempt
the idempotencykey is part of that contract too
the implementation behind it belongs in the code
the implementation in users is a concrete struct
the implementation is the benefit
the implementation runs to a few hundred
the implicit transaction held by session lives for the entire request
the index on orders created at is unused
the ing ban gerunds are the field s nouns caching logging polling batching
the instinct has to stay visibly flawed a lazy pass a snatched shot or skilled play has nothing to beat
the instinct to free a port by killing the process is the single most common way to nuke a user s dev server mid task
the instinct when adding redis to a slow endpoint
the instructions are too long one verb in one line references gameplay md
the intent s invariants
the interface a screen consumes for its data is a recurring case where the
the interface add remove total
the interface comment complete
the interface comment for productdetail
the interface comment for this is a paragraph and a caller rendering the common
the interface comment in full
the interface comment isn t usually written because alembic scripts are
the interface comment now
the interface cost is the point
the interface is a list of fields with extra syntax
the interface is everything a caller must know to use a module correctly
the interface is everything callers must know
the interface is flags somefeature
the interface is get set delete clear but the
the interface is leaking the structure of the
the interface is now getorload key ttl load
the interface is seven separate apis
the interface is single purpose and the comment naturally
the interface is the cost the implementation is the benefit
the interface is the cost the module imposes on the rest of the system
the interface of order looks like a normal object
the interface of the store
the interface productsstore is identical to the
the interface segregation principle is the floor here no caller should depend on parts of the interface it does not use
the interface shrank while the
the interface sits at the level of wraps
the interface to fetching is a
the interface to handle a
the intermediates lose the prop entirely
the invited address receives one invite email carrying a link that opens the
the iou and state the exit
the is this worth the complexity answer for each step
the item count is rarely the cause
the java bean default
the job is the next one in that range wherever you point
the job is to make your reasoning legible as the work goes by
the joke and the mechanic are one thing
the jokes repeat serialize escalate draw without replacement references comedy and narrative md
the js to native bridge in react native is asynchronous and serialized
the json format can evolve without coupled migrations
the judgment doesn t drift between write time and review time
the key is what
the key scheme is now a private detail of productscache
the keying convention does not appear anywhere callers can
the kit changes with the substrate
the label is mandatory because unlabeled geometry is decoration and out
the ladder collapses into one
the ladder reads differently per subject recognition to cued recall to free production in a language recognize to cloze to free recall for facts
the ladder runs lightest first
the ladders apply and the framework idioms decide the move
the ladders still apply
the last one is the expensive one so search the diff for the
the layout and the controls differ between a wide screen and a phone while the game stays live on both
the learner explores at their own depth
the learner is building a reading or an argument and you have no result to check against
the learner is spending their working memory on the concept so none of it should go to parsing the sentence
the learner sees the difference by switching
the ledger is append only
the legacymfasecret field is visible
the lesson sticks when the learner produces something a guess a worked step the reason in their own words
the library cost is real benefit isn t
the library must already be a direct dependency so confirm it in the manifest
the line is whether a real use can be named
the list looks like a precondition that every writer must remember
the list never changes
the load is so expensive that even one per process is too many
the load succeeds the cache repopulates things stabilize
the lock is held for three statements and none of them do i o
the lockfile version is what runs
the locking discipline lives at the call sites
the logic for formatting currency across locales that is a distinct body of
the logo primitive must be chosen for the subject s structure see logos section
the loop is safe here because get is idempotent so repeating it changes
the loop should make each result attributable to one change
the loop stops retrying once
the machine running this code is not yours
the machine safety stops in the operating guide and toby swd environment stay in force at either size
the main skill only credits a change with a literal countable reduction fewer lines branches or duplicated blocks
the map sits above the component or in a sibling module
the mark is annotation and the claim is content
the measurement driven move comes later
the measurement harness baseline discipline and critical path rebuild engage only for a stated performance requirement or a measured problem never speculation
the mechanism explanation comes after
the mega prop form crammed 20 props into one black box
the members list still renders for an org with no pending invites
the merged module is deeper than either original
the method is what survived contact with a real repo
the migration has an up and no down
the migration has no down step so if it goes wrong the way back is a restore from backup
the migration s contract is more complex than the script suggests
the mirror sounds like you re asking how x works
the misconception most people bring to it
the miss path also writes to redis 1ms additional
the missing information has no place in code so
the mistake adding a circuit breaker because we should produces a
the mistake in the other direction is speculative generality parameters or extension points for futures that never arrive
the mistake to avoid is treating named intents over a core
the mode is a structural commitment
the module has a public api that humans call directly
the module s purpose is non obvious from its name and structure
the module that owns the contract validates that input
the most fundamental fact the deck just taught the sentence that if remembered alone still does work
the most reliable way to handle a slow
the motivational sign off sounds like happy coding now go build something great you ve got this the move end on the last real instruction
the move a real number with a source or i haven t measured it invented precision is worse than no number
the move agree when it s true and say why
the move be clear
the move cut the flag
the move delete the adjective
the move make the argument
the move name the thing the return type the interface the record layout this kind of bug
the move say it and own it or don t say it
the move start with the content
the move state the problem in the open
the move three real points and nothing added to reach a rounder number
the move words carry the meaning
the moves are what transfer
the multi step checkout flow
the multi step checkout flow cart address payment review confirmation
the mutation methods enforce invariants no duplicate variants
the naive cache treats keys as strings
the name and signature cannot answer the questions a caller has is
the name covers none of them
the names look reasonably close so nobody questions
the naming and the cut matter once there are two
the naming call goes to the user
the narrower public surface forces callers
the native module throws strings as codes that callers translate by hand
the naturally efficient choice use thumbnails costs no more complexity
the naturally efficient version
the near future variant is stated in the first sentence today email and sms
the nearest shipped feature built the same way on the same layer and the same data path
the new approach is enough better to justify converting every existing use
the new code follows its structure unless you name a reason to differ
the new endpoint appending optimistically
the new rpcs ship alongside the old one which stays and is marked
the next case edits shared code or several files at once
the next derived view a product by brand brand cache
the next reader audits it against their own guess and files the difference as a bug
the next special case banned users system accounts
the next task starts over
the next user state banned system unverified adds another arm here or a second copy elsewhere
the not x but y construction adds drama and no information
the often skipped question what happens when the cache itself is down
the old currentuser was reachable from anywhere
the old form forced an edit in every writer
the old form made every handler remember to call
the old version of this file held fourteen examples that were all two sentences and nineteen words and a corpus that uniform teaches a template
the one caller config option a setting hook or extension point exactly one call site reads
the one exception is toby swd strategy s reactive pass a small local cleanup inside a file a criterion already named reported on its own line
the only assertion in the test is commented out
the opaque pagetoken encodes a stable cursor so paging stays correct under
the opening identity slide
the operating guide carries the full rule
the operating guide carries the one banned word list with the replacement move for each entry
the operating guide owns the format toby s plan for task task groups checkboxes a verification block ending each group
the operating guide s route starts it and nothing has to restart it
the operating guide s standing routes fire here on their own conditions and this file never narrows them
the operation s purpose
the order is paid is a behavior
the orders package depends on a one method interface it defined for its own
the orders table but most queries are about open orders a partial index
the orm session lifecycle every caller knows with session scope
the orm the session the eager lazy loading strategy the transaction
the override when the user explicitly asks for a different register write this as a formal letter keep it strictly clinical follow that register
the page handles only the cases that are its concern
the page loads in a bounded time even when downstream
the page runs one query for the cart then one per line item to fetch the product
the page runs one query to list the users then one more per user for their name
the pagination contract is opaque tokens which lets the
the palette is paper fdfaf1 ink 1a1c1f the one red c44e3f and a dark field 0e0f12 for canvas
the parameter count by itself proves nothing
the partial failure of payment capture reality is hidden
the passive voice escape sounds like mistakes were made the record got deleted the move name who did what yourself included
the path and the two globs are placeholders
the paths are invented
the pattern is consistent
the pattern most apps grow into in every framework
the pattern repeated in countless spring codebases
the pattern that grows
the payment method against the user s saved methods throws
the pending row shows the address and whether it also shows the inviter and the expiry decides what the second slice does
the per row re sort is redundant work on a
the phone fetches 50 4mb 200mb over the wire then decodes each jpeg to
the phone s memory
the phrase match on sku as well holds a second reading where sku matches outrank names
the phrase we should cache this usually means the repository serving this
the physics looks wrong the hit doesn t match the screen collide on what s drawn real motion references gameplay md
the picture cannot contradict the result and the same function tests headless
the placeholder in a production path a todo a hardcoded return standing in for real work an unimplemented branch a swallowed error a fixture the runtime reads
the plan after the design pass and before the first edit a written file reviewed and approved before execution
the plan is the execution record check items off as they land and when execution diverges from an approved step edit the plan and say what moved before continuing
the plan tells you whether the optimizer is using the index you expected
the pptx skill handles file mechanics
the pptx skill handles file mechanics python pptx
the pre sort and pre lowercase happen once at load and every render reads
the premature checkmark sounds like done everything works perfectly the move report what actually ran
the previous reply is the nearest and strongest example of what a reply looks like so it gets copied
the previous value is still displayed
the previously focused element on close
the principle is the same
the principles are the same and the idiom is what changes
the principles are the same in every column
the prior authorization desk is a target
the procedure is place an order from a cart so
the products endpoint is slow
the profound tricolon sounds like it s fast
the progressive matters too because the build is running and the build runs are different claims about the machine
the prose register and banned words rules apply to everything you write that is not chat
the proto3 presence problem disappears because
the proto3 presence problem leaked into the contract an
the proverb as content sounds like at the end of the day it s a marathon not a sprint the move say the specific thought the proverb stands in for
the query add an index denormalize fix the n 1
the question is the p99 latency improvement worth
the questions should reveal scope
the questions to check before doing this the somewhat general purpose
the quote is the words that were used the user s message or the ticket line with the date they were said
the raccoon has also reported you
the reader can see whether the writing holds up
the reader labors on paper reading comparing parsing
the reader sees a machine filling slots
the reader still can t tell what the system is supposed to do
the readme stays on what a
the real interface schema data code coordination across deploys the
the real operations the cart supports add an item apply a coupon pick
the reason there are seven
the reasoning is what transfers
the reasoning is what transfers
the record is the file that says what the software was asked to do in sentences a reader can check against a test
the red flag list runs against finished work before the handoff
the redesign a query object on the wire the implementation owns the
the redesign is usually
the redesign treats keys as typed values that the cache layer issues
the redesign was at the composition
the references bend it where the domain demands so load the matching one as soon as the mode is clear
the references map it
the reflex to validate before checking is the giveaway
the registration is indirection a reader has to trace and load order becomes something you can get wrong
the relationship picks the controls
the render loop calls out to pricing on every item
the repo already does this at file line this is a four line change this needs one decision from you before any code nobody has ever been annoyed to hear the feature already exists
the repos read internally
the repository already owns the data contract caching is part of that contract
the repository is more callers than authors so let it absorb the
the repository is shaped around mechanism when it should be shaped around the knowledge it owns
the repository is the natural home for read through
the repository returns list orderdisplay a flat dataclass with
the request is silent on this
the request is wrong the first time and unchanged on the next three
the resolve then replay seam means the outcome function needs no renderer so it runs ten thousand times in a loop
the response includes a next page link but offset 10000 is rejected
the response returns a named status the caller can branch on when that
the rest is detail
the rest is for games that resolve something
the rest of the definition needs a full census the parent class s public methods against everything the subclass actually calls
the retry count is configurable and the timeout is configurable
the retry count is configurable while the url it wraps is hardcoded to staging
the retry key threading
the retry mechanism assumes running this again
the retry policy already handles the failure mode adequately
the retry wraps the whole request including the body read
the retryability decision lives in one place isretryablegrpc which
the return type decides that contract
the reversal state an official fact flat then let the second clause detonate it
the reversal stops detonating and starts dawning
the review file holds per item the prompt side the answer side the hard direction usually production a last result tag and a next due marker
the reviewer has the diff without the roadmap the incident history or what ships thursday
the right count is the few that serve your real query patterns
the right default for any list of unknown growth potential is flatlist
the right move depends on the actual goal
the right move depends on which errors matter where
the right way done right in a beautiful way that just works made simple reimagined
the route is now pure identity what to show
the route was never registered
the rule the team wants
the same applies to the more substantial cases data fetching above
the same applies to theme prefs auth
the same as a bad backend api
the same decisions base url auth header error semantics retry offline
the same error handled at many call sites when one handler would do
the same locking dance now has to live at every cache call site that risks
the same move removes null checks
the same orbit shape on every artifact is a failure
the same principle holds so cut the number of crossings
the same soft tint text triple drives status pills badges toasts and worked example rows
the same subject moves between models by intent
the same subsystem asked for differently a quick poc of the metering dashboard so i can see if the layout works never reaches this section
the same treatment applies to other bridges location push notifications
the screen for longer than a few minutes the screen does not refetch
the screen now owns the protocol request
the screen now uses identity in its route and queries or context for
the screen only sees
the screen picks where they go
the screens that use these modules
the second clause arrives a beat too late and is not funny the building is fully evacuated
the second instance is what tells you the pattern
the second is the same branch decision copied across call sites
the second is the same branch decision copied across call sites
the second version is longer and tells the reader what happened
the sentence is the identifier and a sentence needing an id to be findable was too vague to audit
the server dedupes on
the service code returns to one line operations
the service goes back to calling products find id and
the service has an interface a base class and one implementation
the service s interface getproduct updateproduct is fine on its
the session cache expires it after an hour so the credential holder needs a refresh
the session orm query syntax commit policy and
the set method does not validate value size on
the set of codes is documented in the readme
the shallow interface accepted a string
the shared behavior is a function the component calls and composes
the shipping cost before setting the total is derived from items coupon
the side effects move from buried in prose to named in
the signal is about the operation as a whole
the significance signpost sounds like it s important to note interestingly notably
the sim blows up or drifts after a while integrate so it cannot detonate assert finite references architecture md
the sizing table says which of them you owe
the skill is domain neutral
the skill s rule applies gate
the skills that fired name the active skills line
the skiptaxvalidation flag is gone
the slice boundary where the next slice depends on the answer a decision surfaced a criterion that turned out wrong a strategic trigger discovery missed
the slide title is the claim
the smell is uneven altitude within one function
the snapshot is 4 000 lines and gets re recorded the moment it fails
the socket default is no timeout so those two paths can hang forever
the socket into a string and requestparser parses the string into a
the sound composition pattern that follows from example 1
the sound refactor would change an interface other teams or callers depend on and coordinating that is out of scope
the sound version requires information you don t have and can t get
the spike proves behavior and decides no structure
the spine below runs in every mode
the spine still runs predict before reveal respond to the answer the ladder the retrieval close but it runs over many small items on a schedule or over production
the staleness window becomes part of the
the stop is the correct handling for an error nobody can act on
the store is now deep
the store s interface and its implementation have the
the strategic version follows the structure the code
the streaker the rain the bribe earn their place by touching wobble or reach or quality
the strongest counter they would still need to answer
the structural choice you made and the main alternative you rejected and why
the structure and the scope decisions are what transfer
the structure carries the joke so the prose never winks
the suite takes 40 minutes so the team runs it once a week
the switch is a single dispatch point and the compiler holds it complete
the system ends up resisting change more than it enables it
the system was tuned for evidence led reference work
the table says which of them you owe
the tactical version wraps every get param call
the target is roughly 10 20 more effort than the tactical path spent
the teaching is in what you do with their answer so branch on it
the teaching mode depends on what the work is
the team renders 2000 items
the team renders 50 000 items
the team s first instinct is usememo on
the team s instinct lazy load
the tell if the decision changed again would all n have to change again the same way
the tell is a value nobody could explain on sight or repetition where a change to one should change all
the tell is a value with no obvious meaning or repetition across sites where a change to one should change all
the tell is whether the edits are driven by unrelated business reasons
the temptation is to define a convenient
the test covering the behavior about to change or none searched what you searched
the test description quotes that sentence verbatim character for character
the test description quotes the sentence verbatim a docstring an it string a t run name whatever the harness reads
the test is named test1
the test is reading something off your machine that won t exist anywhere else
the test name is the spec
the test name the behavior it protected why that behavior is obsolete and what coverage replaces it
the test passes today a refactor breaks fifteen tests tomorrow the team starts deleting tests to keep ci green
the thesis in one sentence sharp enough to disagree with
the thing being shared is a small piece of logic packaged as
the thing that s leaked
the third copies a pattern i do not fully understand so read that one closely
the third hour criterion the one that got smaller while nobody was rereading it
the third one is in a different speaker s mouth which flips it
the three line form below one per behavior
the three tests still hold generation discrimination a pause
the three word cap on noun stacks named relations and conditions before instructions
the ticker knows things it should not
the toby swd interfaces red flags carry the replacement and the plain data exception
the toby swd skills own what good engineering looks like at each step
the top primaryaction and secondaryaction render as buttons in the footer
the ttl is a contract about staleness
the ttl is a product decision so set it from the staleness budget
the ttl is now a magic
the turn comes last the second clause runs shorter and every word the joke survives without is cut
the two typefaces alternating is part of the visual signature
the ui cannot show a retry state and silently lies to
the unbounded version waits
the underlying driver is leaking hide it
the uninvited migration a migration rename or reorganization no criterion asked for
the unit cannot be retargeted or tested without editing it
the unitofwork run contract handles the transactional
the unvisited call site a signature changed and its callers were never swept
the url they both wrap is hardcoded to staging
the usememo is now real because the filter is the only expensive thing left
the user asks to build add implement wire up or finish a behavior spanning more than
the user asks what a function is in mathematics
the user asks why the narrator keeps mentioning the color of the water
the user can accept it or throw it away
the user can observe its result without reading the diff a request they can send a screen they can open a command they can run
the user cancelled is not really an error convention
the user explicitly asks for a pptx file asks to download a deck or asks for a deck for offline use
the user intends to keep
the user is part of the instrument panel so leave the controls where the user put them
the user loses their form data their scroll
the user prefers answering a question over watching you redo the work
the user proposes caching the result
the user s page load takes 30 seconds
the user sees something immediately and
the user when needed
the users related operations checkout needs
the using one means the other relation must run both ways
the value objects in between
the variable is named temp
the version that earns its complexity
the warm dots drain the idle motion stops the camera holds and will not cut
the warrant blank is the highest value exercise in the set
the way of the common use
the web spa stack has its own pattern vocabulary hooks react composables
the web stack has its own complexity patterns
the what to do while another caller is loading decision is made at the
the whole component reads as one coherent color family
the whole operation is several seconds for what should be
the whole panel reads as one mood
the why usertier note is the non obvious dependency a future editor would
the widening is the work
the win has to be earned a smooth ramp to a trivial answer is a dead game because the player or the optimizer finds it once and never looks again
the win is a supported precise claim so judging and sharpening replace right and wrong
the wire contract is leaking the implementation s
the word configuration covers two things
the word that carries it your whole reading rests on one word
the word throwaway outranks every noun after it so this owns the
the work is getting many items to stick which takes repeated retrieval over time
the work is in how you interpret the user s input wider more skeptical of their literal phrasing and in proposing options that stand on their own for them to pick from
the worked example component does this and other reasoning panels can borrow the pattern
the wrapper over one instance a new module layer or adapter standing in front of a single implementation
the wrong answer is process env thing scattered through the code
the wrong default no
the wrong fix is to leave the lazy interface in place and tell callers to be
the wrong move is to keep fetch calls scattered through screens
them when the surface justifies it and build the lightweight repository when
theme is read from context
theme prefs and onboarding deliberately survive logout
theme theme dense compactonmobile
theme threaded through here only to reach a child
themepreference promise light dark system
themselves must enforce the legal status transitions
then call biometrickeysexist to see
then do not split
then drop back to normal collaboration
then expand the set with instances they didn t name neighbors less obvious members the same pattern in different clothes
then in order dropping any section that s empty
then j setitems j items setloading false
then j setresults j items
then layer by reference and calibrate with bots
then p if cancelled setproduct p
then paste what came back into the consequence line
then present what you ve found for them to react to
then push is added then slack then a webhook
then r r json
then r r ok
then r r ok
then recommend a review pass
then refactor first and add the change on top with no workaround
then screens use it directly
then the operating guide s list anything incomplete or risky tests deleted or weakened heavy commands skipped processes left running assumptions still waiting
then the word isn t working yet what does the ending actually do
then two on the whole reply
then validates tax via the
then validates the shipping
there are meaningful decisions about how to use it correctly initialization order required configuration known gotchas that the api doesn t protect against
there is nothing to delete before one exists
there s a usermanager a userhandler and a userservice
there s no formal exit
there s no step by step checklist to run through
there s no story it doesn t flow a run is an arc that plants and pays off references comedy and narrative md
there the band is the parameter range that stays alive and the spot where the surprise reliably shows
there the data is the contract and depth lives in the module that owns the behavior
there the data is the contract and the depth lives in the module that owns the behavior
these all sit between the cover and the end slide
these are known patterns where the naturally efficient version is no more
these are the design time moves
these are the moves that give an toby artifact artifact its character
these examples are single use
these examples are single use
these examples show the same checks
these examples walk the comment test on the contracts you ll touch most
these files say why who this is for and how to think about it
these fire on what the change does to a contract someone else depends on
these write nothing at all refactors renames performance work internal helpers error paths and diagnostic or developer facing output
they also belong together when they fall under one simple higher level category or when one cannot be understood without the other
they appear at the top of the
they are cheap so run all of them
they are ci noise generators with version control
they are in the middle of their own work
they are not bundled with the skill
they are not merge
they are split is safe
they are useful internally
they belong to is leaked across all seven modules
they carry the units invariants and reasons that have no home in the code
they didn t give you one because they don t have one yet
they grate from any author
they have different audiences and different jobs
they hold the keyboard you coach
they must differ in where complexity lives which module owns the hard part what the interface exposes what callers must manage
they provide the deep module so you don t have to build it from scratch
they re handing you a fragment and the job is to find the rest of it with them
they re how a lateral rewrite disguises itself
they re persisted across reloads used by deep links and read by
they share state are always used together and none
they start by drafting the hook
they take no flow props
thin in caller facing code a few decisions and add real value by encoding
things get slower on low end devices but the
thinking about js performance so the js side often needs to ask for it
thirteen reading slides values carry units caveats sit beside the figures they qualify
this applies identically across the stack
this asks for two placements of the work so it does not conflict with the single candidate rule in toby swd interfaces which governs one signature once the placement is settled
this authz require session order read id req params id
this available for this user a new channel is one class and one entry
this bar is checkable by reading
this belongs inside the cache pull complexity down
this cache invalidate product p id
this check applies hardest in java kotlin c swift and python oop heavy code
this check is the single responsibility principle
this costs nothing and prevents the death by a thousand cuts case where ignoring performance entirely yields a system 5 10x slow with no single fix available
this covers today s known queries
this creates a hidden two way coupling
this file adds none and repeats none
this file adds patterns specific to building real applications
this file closes the gap
this file controls evidence units claim structure and artifact copy discipline
this file covers one question about caching should you add a cache at all
this file is for a developer calling the payments service from outside
this file is for the ones that keep growing
this file owns that order and how much of it a change owes
this file owns the entries and their names so a handoff can cite one in three words and a reader can come look it up
this file owns the rest what writes no entry what an entry holds the grep retiring and what happens when two branches write the same sentence
this file owns when the plan gets written and what a step carries for someone to approve it
this file shows the rules that references toby md states
this file shows them calibrates against them and carries the procedures for running them
this file would survive a full rewrite of the internals unchanged
this fits a plugin api or a set of adapters loaded by config at boot
this getorload wrap loadingcache
this handles 1000 concurrent in process requests with one downstream call
this inner find id
this inner listing category
this is a cache
this is a legal
this is a legitimate quick fix situation hard
this is a pass through variable the
this is a pass through variable the different layer check
this is a pass through wrapper the different layer and depth checks
this is a performance problem and the results stay correct
this is a recurring pattern
this is a tool for depth one interface many implementations a storage interface implemented by disk s3 memory
this is aggregation at the wrong level
this is aggregation rung 3 at the api client or response interceptor
this is classitis at the store level
this is composition exactly the pattern the composition over inheritance check recommends
this is design time naturally efficient work
this is don t hand the fix in a language
this is faster alone is not on the list
this is heavier than what most teams need on day one
this is identical to the database n 1 problem the same cause
this is implementation inheritance leakage in non class clothing
this is information leakage at the data layer
this is information leakage in slow motion
this is overexposure the depth check a split merge split candidate
this is rung 4 of the ladder in toby swd modules an interface with one
this is speculative performance complexity added without measurement
this is temporal decomposition
this is temporal decomposition the decompose by knowledge check
this is the brownfield rule the
this is the canonical example of pulling complexity downward at the
this is the canonical place to pull complexity down
this is the cheapest moment in the system s life to improve it and it doesn t come back
this is the death by a thousand cuts case from the skill because no profiler
this is the depth
this is the design smell catalog toby code review checks a diff against
this is the design time naturally
this is the error ladder applied at the bridge layer mask transient
this is the error ladder applied at the render layer
this is the foundational pattern
this is the frontend form of passing config down from the composition root
this is the generic container failure positional unlabeled meaning obscured
this is the just crash case one checked
this is the kind of
this is the kind of complexity that lives in the
this is the liskov substitution test
this is the loop s maintenance mode
this is the path almost every interface takes and it adds almost no cost beyond the comment you owed anyway
this is the prior
this is the risky part
this is the same design it twice pattern as the usercard case in
this is the same move as rung 1 of the conditional ladder in toby swd modules the branch that is not written cannot rot
this is the same point as examples md example 3
this is the same point as examples md example 3
this is the same point as the depth check
this is the same principle as the depth check in a different syntax
this is the same problem as examples md example 1 define the error out
this is the same problem as the mobile bridge example n 1 over a network
this is the same redesign pattern as databases md example 1 orm
this is the source of most cache bugs
this is what most findings look like
this is what speculative caching produces
this is why the review step blocks on address state even though the
this loadingmap delete key
this loadingmap set key promise
this masks information the caller needs because an empty list and a failed
this module s readme md is minimal because it serves developers already inside the codebase with none of the orientation a shared library owes outside callers
this one ships with a default
this order is a full table scan
this orders orders this authz authz this respond respond
this pattern is what radix headless ui and melt ui productize
this proves the test catches the bug proves the fix works and leaves a regression guard behind
this reference adds artifact constraints claim in the heading units on values citations beside claims and no marketing copy
this rule targets guessing which line is the bottleneck
this second move encodes allowed as presence of the action so a parallel
this service charges cards
this service now appears slow to its callers which back up which
this service s threads event loop hang on
this should weigh both columns
this skill adds none
this skill brings the taste a real system underneath jokes wired to the mechanics a paper and ink look a run that goes somewhere
this skill controls artifact structure visual tokens density copy tests and component patterns
this skill governs voice
this skill means the agents md inside the user s own module tree the file it teaches you to write
this status orderstatus paid
those belong in agents md or code comments
those files run the next agent so breaking one costs what breaking a lint rule costs
those live in interface comments in gateway py
those pass and ordinary state changing behavior is not a smuggled result
thousand cuts both live
thousand cuts case at scale when every query is slow and there s no
thread pool saturated on slow downstream add the breaker for that
threading it through intent extras or uinavigationcontroller
three bots set the band random input finishes near zero a human grade bot lands in the target range often 10 20 a superhuman ceiling sits above
three caller obligations load on miss coordination
three card three laws
three cases all named
three concerns is the same defect as one module owning three pieces of
three design time moves
three findings in the bugs pass leaves the compliance pass its own three
three habits leave a suite that passes and protects nothing mass updating snapshots deleting cases that break a lot and weakening assertions to land a pr
three handler buckets disappear from this file and from every other page
three in a row means the previous reply wrote this one
three index decisions that pay back consistently
three is a hard ceiling
three js for 3d raw canvas for 2d plain dom when the board is cards and panels
three levels an outer last resort boundary rare full reset per route
three live verbs in the system
three mitigations in order of complexity
three named verbs only
three names for one object in the first version session cache credential holder sessionstore
three of them only forward the call
three one line branches over a closed set do not qualify
three questions no other section asks
three real call sites are three named intents
three sets of prop combinations that happen to recur do not qualify
three stops on the running order above
three tone progression for narrative blocks when a panel walks through reasoning tint the rows setup info teal working watch amber conclusion stable green
three ways out in roughly increasing complexity
three wrappers deep wrapper hell and the component s
threshold lines 1px dashed toby accent when crossing means consequence otherwise 1px dashed text muted
throat clearing sounds like great let me dive in
throttled caller is over a limit
through 5 attempts and routine ones stay absorbed
through gateway py looks redundant it is required because the processor may
through them silently breaks deep links and state restoration
throw new error unhandled json stringify x
throw new error unreachable
throw new error unreachable
throw new httperror res status await res text
throw new httperror res status await res text permanent istransient res status
thumbnails at the right resolution lazy where possible
tick marks 4px same color as axis
tie difficulty to the player s behavior and clamp it
tight code tends to be fast because defined away special cases need no checks and deep modules cross fewer layers
time series pipeline queue horizontal rail with tick marks
timeout choice is a performance design decision
times and the operator is guessing
tinted state families when a panel expresses a state info stable watch consequence unknown give it the three step palette pale background mid saturation border dark saturated text
tiny diagrams or plain text flows when structure is relational a sentence for a sequence
title and description appear at
title as a claim p95 latency exceeds 200 ms after 14 00 utc passes
title big fact title slide then a single high contrast slide stating the central numerical fact measurement or result
title block anchored left
title bold body opacity 0 88
title claim led sentence case inter 600
title glossary title slide with logo primitive then a glossary slide defining the terms before the mechanism
title problem statement title slide then an ink slide stating the problem or question the deck answers
title slide cover index default for reference decks sparse title with the toby mark giant topic followed by a dense toc grid
title worked example first title slide then immediately a concrete worked example or case
title your game title
to also skip an attempt that
to be a domain repository
to be clear let me be honest sounds like announcing candor ahead of the actual claim
to be specially described because the contract was designed for the
to determine whether the wishlist button shows
to do work that belongs inside
to implement getorload but as public methods they invite the
to render in the wrong state
to start at a high level it s worth noting that the move delete it and lead with the finding
to the new address
toast bottom right stack auto dismiss 4s semantic left border soft tinted bg
toby accent c44e3f consequence red risk breach finality only
toby artifact controls colors fonts sizes padding and slide structure
toby artifact controls visuals
toby artifact mark wordmark in the top left
toby artifact mark wordmark in the top left using text on dark for the strokes red dot stays accent red
toby artifact mark wordmark top left
toby artifact s visual system pairs with a specific voice
toby artifact style is a constraint set
toby blue steel 2f6e8a
toby code review s references smells md owns the full entry for every name below the definition the fires when criterion you quote against the code and the exception that makes it a non finding
toby cons soft f6e2dc toby cons tint e9b7ad toby cons text 7a2d22
toby field olive 7d7e3a
toby info 2e6a78 teal
toby info soft e2e9ea toby info tint bfcdce toby info text 2c3a3a
toby ink 1a1c1f dark panels for decisions evidence
toby ink 2 2a2d33 lifted dark surface
toby muted violet 7a4e85
toby paper 2 ede8d6 recessed
toby paper 3 fffefa lifted card near white
toby paper fdfaf1 primary bright off white slight warmth
toby s operating guide is a different file already loaded by the host tool and these rules never mean it
toby s prose rules borrow from asd ste100 simplified technical english the controlled language the aerospace industry writes maintenance manuals in
toby s reader is a developer and don t ship that reads the same as do not ship that to them
toby sage steel 607a5f
toby simplify code owns behavior preserving cleanup the user asked for
toby slate blue 344b6e
toby squall is a posture
toby squall is ambient
toby squall is for keeping that decision in their hands
toby squall runs when the user is uncertain either about what they want or about how big what they re after really is
toby stable 4d7a30 leaf green
toby stable soft e3ecd5 toby stable tint bcc89e toby stable text 2d4818
toby swd clarity owns
toby swd experiment s finish phase deletes the throwaway code and what survives gets the greenfield or brownfield read on its own merits
toby swd interfaces one method per caller variation red flag argues for consolidating those into a single method with a value or query parameter
toby swd interfaces owns the four questions that apply this and the speculative generality limit
toby swd modules calls a decorator that adds little a shallow pass through in disguise worth questioning even when it satisfies a contract
toby swd modules defines the deep module and carries the checks that find a shallow one
toby swd modules has the ladder
toby swd strategy toby swd interfaces
toby swd testing owns when test first is skipped
toby unknown 88827a warm grey
toby unknown soft ebe8e3 toby unknown tint c4bfb6 toby unknown text 3f3d39
toby watch b07020 amber
toby watch soft f1e6d2 toby watch tint d6c19a toby watch text 5a431d
today email and sms
today it is passed
today it s an accident of which screens
together the view is sluggish
together they break the pattern that lets a reader
toggle 36 20 track thumb
toggle a panel with an explicit block or flex
toggle mode switch a binary or small n switch changes the view
toggletorch nativemodules barcodescanner toggletorch
token at the edge and only the token reaches this module
token card data is exchanged for a token at the edge before it
token if there are more results
token is returned in the response
token tok abc card token from the payments edge
tokens convey it implicitly because the token stops being valid past a limit
tokens copy constraints and visual rules are tight
tokens promise tokens null
tone warm through bleak to the dread register
too easy longer gaps climb to production widen the set add a confusable
too hard shorten the gap drop a rung give a first letter or category hint shrink the active set
too narrow saves nothing and costs added latency on the miss path
toolbar btn on fills with ink text on dark
toolbar inline mini controls strip
top comment names what s faked then state sim render wiring
topbar header with breadcrumb optional id stamp right aligned actions
total derived items coupon items coupon
total number computes from items coupon
touched it and logoutflow is destined to drift behind every new key
touchstones that show the range
track each item s next look as a rough count of items ahead so the gap scales with the session
track the command so you can stop the specific process later
tracks recent failure rate
traffic alone the cache restart is now a production incident
trajectory uncertainty after a perturbation
transaction otherwise it runs in its own transaction and commits
transactional partial failures roll back except for the payment capture
transient errors are the canonical case a network blip a database deadlock a rate limit response
transient request never completed
translating native error codes ios vs android into the typed reasons
transport classifying retryable errors are more work than a loop
treat 503 as a real service is down failure surface 500 to the user
treat a comment you can t write short and internals free as a bug report
treat a name that can be confused as a value that can be
treat comments as the mechanism by which abstraction is delivered
treat creating one as a design smell first
treat experiment output as learning material
treat explicit gates literally
treat handler code that can t be tested reliably with extra skepticism
treat the cache as a layer below the repository
treat the ceiling as a maximum
treat the machine as the user s
treat the name as an abstraction
treat the reader as a serious professional reviewing evidence
treat the verdict as evidence you weigh
treeview hairline rule down the left of each level
trigger n 1 by reading the wrong field because the field is just a column
trigger only when the creator explicitly invokes this skill by name or with the toby game slash command
trigger only when the user explicitly invokes the skill by name or with the toby squall slash command
trigger when the user asks for a visual including a visual to teach or explain something draw it show it make a diagram build a chart sketch a graphic
trips stops calling when failures cross a threshold
trivial fields with names and types that already say the whole contract can stay quiet
try catch in every useeffect effect
try return await api getorders
ttl is the freshness
ttl method that explicitly signals the intent
ttl staleness budget name it and document the reason
tunable performance config errors the caller must handle durability or visibility guarantees ordering the caller depends on
turbomodule calls help for the per call cost and batching helps for the
turn a feature request into the smallest change that satisfies it with evidence it
turns an authorized cart into a settled charge and a ledger entry
twelve services when each constructs raw sql or hardcodes a column alias
two blocks that share text while answering to different reasons to change are also fine however identical they look
two branches that wrote the same sentence wrote the same behavior and the merge conflict is the correct outcome
two branches writing the same sentence wrote the same behavior and that conflict is the correct outcome
two callers pass timeout none
two callers will read the positions
two cases the one decision loop gets wrong many items that must stick and language use
two checks fail at once
two competing marks read as noise
two concentric circles defining inner and outer radius
two design time choices that prevent this
two finders that differ in argument order do almost the same query
two fixes depending on where the responsibility belongs
two forms of conditional are a design smell
two formula cards side by side plus a worked examples table beneath
two i am sure about
two improvements at the contract level
two lists toby feature dev checks work against
two maps that switch on the same tag is the leak this rung removes
two modes and picking wrong is expensive in both directions
two of them call the third and one has no callers at all
two or three sentences for most explanations
two pieces belong together when they share knowledge or when using one almost always means using the other
two readings that produce different observable behavior mean it is ambiguous and the criteria list records the outcome either way
two rules hold under it
two sample rule not yet tripped
two sample rule not yet tripped div
two scales primary at content size with a small uppercase label or decorative small 80px and quiet opacity 0 25
two screens shrink to rendering code
two sections this file adds and both always appear
two sentences means two behaviors
two sentences means two behaviors so split them
two side effects session revocation async permission re evaluation are
two sides share one catalog an opponent reads the same state and buys from the same shelf the player does escalating on a schedule
two skills stating opposite rules is a finding whichever one is right
two strong slices hand them the harder call themselves
two things are tangled the behavior of a combobox keyboard navigation
two things here are cheap removing error conditions that
two transactions read an inventory row both see one unit left both sell it
two valid splits extract a general purpose subtask parent keeps its interface child stands alone or divide a method doing unrelated things into separate caller visible methods
two way coupling masquerading as reuse
two ways to make the interface s cost visible
tx parameter on every method
type composable rest endpoint grpc service repository cache interface
type is wildly different from the entity reporting dashboards
type user struct domain entity narrow
type userlookup interface 1 method defined by the consumer
type userservice interface declared next to implementation
typed cache module with private key constructors
typed failure with a structured reason
typed outcome per input created or rejected with a reason lets the caller
typed result discriminator wrapper classifies
typed result result t or sealed outcome
types so they cannot be interchanged at all
typing into a filter is
typos at the call site fail at compile time
ui could render without it
uiviewcontroller activity fragment same caution as the composition over inheritance check
ul use cb listaction
uncertain by amount pair value with interval
uncertainty cone or band
under a deadline write
under high load the connection pool
under low load fine
under peak load with a popular key expiring 1000
unexpected bubbled to the route s errorboundary
unexpected errors propagate to the route level error boundary
units px on web convert to pt for pptx
units what null means bounds ownership
unknown not measured open question
unknown or open say so
unknownsoft ebe8e3 unknowntint c4bfb6 unknowntext 3f3d39
unless you have hundreds of processes
unlock transform supercharge revolutionize harness empower elevate accelerate streamline
unmounts the whole tree
unsaved state in a browser tab connected to it is gone either way
unstable keys the array index as key on reorder insert or
untagged entries need a cross file search code the diff didn t touch or repo history to prove
until group 2 s send lands
until the cache warms
unverified is available when the run needs something out of reach an approval gated command a credential an external service a device
unverified users adds another branch here or worse another branched copy
uow run or in its own if not
up top it holds what to review first what is shaky and what is retired
update agents md whenever a structural change makes it wrong responsibility moves a file s job changes a cross module dependency is added or removed a constraint changes
update id string changes partial order promise order
update id string changes partial order tx transaction promise order
update inventory set qty qty 1 version version 1
update method that takes partial entity
update users set last seen now where id any
update when the public api changes when a new constraint is added or when a user reports confusion that a good readme md would have prevented
updated snapshots wholesale either the behavior changed update tests deliberately or the snapshots were noise delete them on purpose
updates the user with user id
updateuser u user error
upgrade downgrade and live with it
upload returns once durably stored and
uppercase eyebrows tracked 0 12em for section labels
use a lookup map when the case bodies are small an interface with implementations when each case owns state
use a section eyebrow and the logo primitive in the footer and skip the dedicated opening slide
use a stable id
use as a table cell kv value or stat card adornment
use cause and effect this holds because
use experiment mode for prompts like
use field mask if they want to clear a field since proto3 makes the
use for a quoted measurement a derived formula or a stated decision inside a paper slide
use for algorithms sort traversal packet routing hash collision resolution protocols handshake sequence state machine transitions or any process where the sequence is the lesson
use for formulas where plugging in numbers is the lesson friis equation shannon capacity nyquist rate
use for hierarchical reference material protocol layers taxonomy trees component breakdowns
use for protocol references parameter tables specification sheets side by side comparisons
use for queue dynamics signal propagation population models or any system with emergent behavior over time
use for spatial or geometric concepts moving a threshold line to see false positive negative tradeoff rotating a beam to show angle vs gain repositioning a node to show path length change
use for summary dashboards executive snapshots decision panels final state reference cards
use ink weight hairline or position for emphasis
use it across artifacts to maintain coherence
use it for a
use it for code
use it for naming
use it for non trivial work a feature a bug fix with
use it freely when the interface captures real shared structure
use it here only if the cached value is a service specific composition that no repository would own
use it on any task that runs a command
use it when a task
use it when a task changes
use it when a task touches error handling retries
use it when the request
use it when writing
use lucide where available
use names such as experiment poc spike scratch debug or temp
use object subset matching when only a few fields matter
use open ended prose questions when the answer space is unbounded
use platform select ios android at one module boundary or foo ios tsx and foo android tsx files where the bundler picks the file and no branch runs
use real or generated imagery when the task requires the product place object state gameplay or person to be inspectable
use rung 2 when the branches differ only in which function runs
use serious plot forms
use text on dark muted on ink surfaces text muted on paper
use the app s icon library for coded frontends when one exists
use the available user input tool when one exists and the question has discrete options they could pick from
use the bridge only when it holds because a forced one costs more than skipping it
use the cache here
use the toby artifact chart palette in positional order for any series
use the worked example component three rows info watch stable tinted
use these together whenever a component expresses a state alerts toasts status pills badges callouts worked example rows kpi accent stripes
use this for operations not covered by other methods
use this over rung 2 when each branch reads different fields
use this skill when the user wants something explained a decision inside work underway or a concept they asked about with nothing else on the page yet
use this skill when the user wants to learn the work by doing it
use this skill when the work is exploratory and the user expects fast learning before durable implementation
use toby swd environment before commands ports processes browsers broad checks dependency installs migrations generated files cache clearing or destructive work
use uncertainty cones for forecasts error bars for measured points confidence bands for fitted curves
use when the concept has two distinct states or representations that benefit from direct comparison time domain vs frequency domain serial vs parallel execution raw data vs normalized data
use when the concept is fundamentally spatial or relational network topologies signal flows state machines data distributions
use when the concept is singular and deep a derivation a worked example an analysis
use when the lesson is a trade off or comparison that must be perceived simultaneously two algorithms on the same data two antenna configurations in the same environment
use when the relationship between input and output is the lesson gain vs noise figure sample size vs confidence interval learning rate vs convergence
use whenever a diagram is to scale
use whitespace to expose structure blank lines between phases spacing within dense expressions
usecallback is for callbacks passed to memoized children that
usecart getstate total outside a selector doesn t subscribe and the
used when the user asks for a pptx file
usememo is for expensive computations whose dependencies are stable
usememo usecallback on everything
useorder returns a discriminated query result a status plus the data
user data build user view user
user editable settings must reflect changes quickly
user is the current user used
user plays three roles at once database row json dto and domain entity
user profile 60 seconds
user s query user filter by id current user id one
user s resources is async so the new role may not be in effect for the
user s resources which is async callers should not assume the new role
user save user u
user select none display inline block width 20px text align right
user session query user filter by id current user id one
user session token refresh failures stacks five words so it becomes failed token refreshes
user states drove it
user user null null while loading or after a failed fetch
user user variant list showemail true showavatar true
usercombobox tagcombobox is a few lines of presentational code that
userlistitem user user onselect fn
usermenu directly or give the wrapper a real responsibility
userprefs and onboarding deliberately survive logout
userquery has its own comment describing each filter
userquery is for users so don t
userrepository find new userquery orgid org should be simple to write
users a much smaller surface than every row column
users complain that their profile changes don t show up for a long time
users don t see results between keystrokes
users find new userquery withorgid org withactive true
users update profile id profilechanges name new and gets a typed
usersearchresult user user query q
usertier is included because tier based discounts must be
usually doesn t get an answer
usually it s lazy loading example 1
usually the abstraction boundary you re testing through is wrong or the behavior under test is tangled with something else
utils js is 1 800 lines
v4 2 v3 9 versions
vague name broad enough to refer to many things
vague phrasing i think a restated question copying without change smaller steps work more of it yourself
valid for systems where degraded reads need to be a deliberate decision
valid for the current items before setting or the displayed total will
validate input range and show a boundary error when the value falls outside it
validate the change report the real risks stop
validation recovery paths caching concurrency batching or a measured
validation total computation coupon validity async network calls during
validationfailed with field level reasons if any change is invalid
value and rolls back on server rejection
value it loads fresh
value mono 14 pt below
values must be strings serialize json yourself
variant controls the icon color and primary button color
variant prop that grew a button variant whose variant gains values every quarter is rung 2 wearing a prop
variants default lifted flush paper toned authority ink toned
variants ink default info stable watch cons
vary information density a lesson on a single formula can live on three spacious slides
vary ink allocation some artifacts are mostly paper with one or two ink evidence panels
vary length underneath it
vary the opening structure across decks
verbatim means character for character because the check is a fixed string grep and a helpfully reworded sentence reads to it as a missing test
verbs written as verbs
verify per the behavior drift rules above
verify state and results over interactions
version and cost no extra complexity
version bumps to the key scheme happen in one
version column optimistic or select for update pessimistic one per resource
version suffix v1 to v2 so old clients don t read corrupted data
version the saved blob and migrate or discard it on load inside a try catch or a later edit breaks old saves
very large bulk loads 10k rows
viability score build pass rate caveat freshness rollback rehearsed weighted lanes viability 0 73 0 08 watch band 0 6 0 85
view if empty the section is hidden
view if the user is not signed in
viewcontrollers and activities are thin and stateful only about their own ui
virtualization and an image caching library expo image the default in
virtualization here adds complexity for zero perf benefit
virtualized is exactly the speculative complexity anti pattern
virtualizing a 100 item list
visualizer requires transparent background so set paper color on a top level rect if a paper field is wanted
vocabulary recognize see the word give the meaning cued recall prompt in the other language produce the word free production use it in a sentence you build
void delete uuid id
volume and recall subjects vocabulary terminology pathways give each item one anchor and flag the few that trip everyone
volume or recall many items that must stick vocabulary terminology a paradigm table
wait for feedback before changing the next candidate
wait for the migration to finish
wait the timeout still exists just higher because wait forever is
walk a worked step
want me to rebase instead
warm up flattery sounds like great question that s a really sharp observation the move open with the answer
warnings and informational alerts
warrant reflex on any claim plus evidence with the link assumed ask for the link you ve got the claim and the quote
was a session bound proxy that explodes if you touch it after commit
watch for a different failure late in a session
watch for two way coupling any mechanism where a shared behavior provider and its consumers can each silently break the other
watch threshold breached at sample 8 two sample rule satisfied at sample 9
watching a correct solution build is how the model forms
watchsoft f1e6d2 watchtint d6c19a watchtext 5a431d
wave signal antenna oscillation arc or sinusoidal rail
ways and the redesigns are equally predictable
we cancel what the loading state holds what counts as an error are
we don t want a dependency loses
we may not store pan data pci
we should cache it
weaponized just sounds like just run the migration simply update the config the move name the actual steps and what can go wrong
webhook is advisory and may arrive out of order
weeks later a second call site needs retries and the loop is copy pasted with a
weigh what the caller must manage to use the module parameters preconditions ordering error cases against what it handles internally and invisibly
weigh what the repo asks for against what the change touched
weight what the user says about themselves over their phrasing alone because a precise question can come from someone precise about everything except this topic
weights 400 body 500 emphasis 600 headings
what a caller needs with no internals
what a test protects behavior at the public interface where callers notice it
what a wrong guess costs decides what happens next
what do you do with the line where she laughs
what follows is what arrives after the user has seen a layout they like and asks for the real one
what happens if start end
what hidden dependency did it create a decision in two modules an ordering a caller must respect a field layout two files agree on with nothing enforcing it
what in the text makes you say it
what in the text resists your reading
what is it doing now
what is the deck doing teaching a mechanism summarizing evidence building a reference walking a process
what is the simplest interface that covers all your current needs fewer methods each carrying broader semantics so the interface stays small as needs grow
what it surfaces 4xx caller did
what not to do add an index per column just in case indexes have
what s one word in here that seems off too strong or out of place
what s the hit ratio
what safest means belongs to the environment a pure computation aborts a process driving something physical costly or external reaches a defined safe stop before it dies a supervised isolated unit stops and is restarted to a known good state a long running loop contains the fault to the smallest unit and keeps running
what screens do handle the real i couldn t load this thing
what separates this from pure tactical programming is
what special case did this diff add and which caller now has to know about it
what stays unknown after you check
what structure would this code have if it had been designed from the start with this change already in mind
what survives is the container and it hardens the same four word opener the same two bolded sections the same closing caveat
what system are we modeling and what is the one surprising thing it should do when it runs
what the alternative would have cost
what the record does not hold no test list since the grep derives it and a list you never wrote can t go stale no rationale or alternatives which toby swd docs and toby swd strategy own no task list which is the plan s job no status beyond living above or below the retired heading
what the schema allows that you should never do
what they would revise now
what this absorbs rung 2 mask at lowest level transient 5xx 408 429
what this is one or two sentences what problem it solves and who it s for
what this module is and the problem it solves one or two sentences what it does and why it exists
what to clear on logout is a decision the orchestrating code now makes
what to load order repo recent for customer customer id limit 50
what toby refused and why
what would the author have to believe for that to be true
whatever you pick here becomes the pattern the next five features copy including the parts you picked because it was late
when a banned word has no plain replacement rewrite the sentence
when a behavior is hard to test through the public interface the abstraction is usually too coarse
when a boundary is also a serialization or process boundary crossing it costs a round trip and a serialize deserialize at runtime
when a bounded retry inside the module turns them into success the caller never had a failure to handle
when a check is wrong or the contributed work is broken don t paste the fix
when a naturally efficient option is no more complex than a slow one take it reach for a hash table when ordering isn t needed allocate one block where many would do
when a new case must be addable without editing any central file the selection site itself has to be open
when a reader would trip on it check the catalog s exception for whatever it resembles first
when a reported success would mask a real bug the error stays
when a request matches one of these patterns use it
when a result depends on a library or platform version record it so a later mismatch is visible
when an interface s natural
when changing the layout of stored data bump the key s
when does the complexity pay for itself
when each case owns behavior private state or its own dependencies a map of functions is the wrong container
when each field is missing
when exploratory work becomes durable code state the inputs seeds and versions the exploratory surface left implicit so the result reproduces outside the original session
when in doubt push the documentation up to the nearest meaningful module root
when it conflicts with these rules by saying always run x without asking for example pause and ask
when it is needed describe the smallest code that must run in the common case ignoring the current structure and rebuild toward that
when it matches the flag don t fix half of references smells md or a red flag in toby swd modules toby swd interfaces or toby swd complexity name the smell and the skill that owns it
when no reviewer is available simulate a specific developer who is unfamiliar with this code
when none of them survive rebuild them from the code that exists and the original request show them and confirm before continuing
when one handler would do
when one service consumes another the question is what the consumer side
when one ships anyway name the entry and the criterion that carried it
when pinch zoom is off give a live game its own way to see a board bigger than the screen
when placing code in an existing module inspect where the surrounding code already puts that knowledge
when scope is unclear ask before proposing
when something is slow measure then redesign the critical path never optimize on intuition because programmer intuition about what is slow is unreliable regardless of experience
when something uses ink the artifact has stopped to make a point
when that handler replies to an untrusted caller the surfaced error carries the category the caller needs and nothing about internals no stack traces internal identifiers or query text
when that needs an approval gated command name the exact command and ask for it
when the answer is no but the
when the answer is yes ship the cache
when the artifact s actual subject is from another domain use that domain s real vocabulary
when the branches read different fields the map does not fit
when the cache key expires a hundred concurrent requests all
when the cause is in the diff follow it to the callers it affects even where they sit outside the diff
when the change alters an existing callable surface run the caller sweep toby swd interfaces owns and carry its result into the handoff
when the change creates or clarifies a public contract in a meaningful module offer an agents md update if none exists nearby
when the code looks awkward because an outside requirement made it so the reason lives here
when the comment has to describe what the caller
when the comment refers to mechanism sessions queries transactions cursors
when the comment test exposes
when the contract crosses a serialization boundary what survives that crossing is part of the contract
when the evidence arrives an outage post mortem says
when the evidence settles it the reason is padding
when the experiment drives something physical costly or externally observable a reverted change does not undo what already happened
when the gate is met and an independent review tool is available use it as a critic
when the learner asks for a visual to carry the lesson a diagram an image a graphic a chart make it and apply toby artifact style for the look
when the obvious thing wins give it a wall to climb
when the operation must continue in a reduced or safer mode the handling is a deliberately designed degraded path the system was built to expect
when the output is inexact by nature assert it falls within a stated justified tolerance of the expected value
when the real dependency lives across a process sandbox or hardware boundary you can t run in process that boundary is the system boundary
when the repo has a settled convention for this exact construct visible in two or more sibling files it wins even over the textbook idiom
when the repo recommends a heavy command
when the request as worded and the problem as described disagree name the gap first
when the requirements change
when the system itself must reproduce the same outputs across runs or machines that determinism is part of the contract under test
when the team introduces a second category listing endpoint the
when the team later decides every error
when the team moves from sqlalchemy to sqlmodel or splits writes off to a
when the topic has a real trap worth naming a convention that trips everyone a textbook that introduces it backwards
when the urge to shrink a function fights a deep module the deep module wins
when the user asks for a visual to carry the point a diagram an image a graphic a chart make it and apply toby artifact style for the look
when the user asks to simplify tidy tighten refactor de duplicate or clarify code
when the user chooses a behavior retire the experiment
when the user invokes toby squall whatever they named is one instance of a bigger set
when the user picks a behavior at the end of an experiment come back here and write the criteria from what they picked
when there is a target name the specific mechanism and steal its real texture before writing a single joke
when they only need one method
when they re strong in a field next door bridge to a structure they already hold there
when they reach for the keyboard hand it over and switch to toby learning
when to escalate to flashlist large or image heavy lists where measured
when to make a slide interactive the concept has a parameter that governs a visual outcome a trade off between two effects or a process the learner should be able to step through
when to remove a cache
when to stay with scrollview known small known bounded lists with
when unsure drop it
when unsure leave it
when work is a proof of concept spike parameter experiment or user feedback loop use toby swd experiment
when work touches an existing meaningful module check whether the nearest module root already has an agents md
when you delete or weaken a test deliberately report four things
when you feel yourself reaching for the same layout you built last time stop and pick a different one
when you must inherit a framework demands it you override one method on a stable parent keep the surface narrow
when you need several of the same kind keep the common root and add a distinguishing prefix srcblock dstblock
when you switch from letting errors ride to a tightening pass say so in one line or the learner can t tell why errors are suddenly flagged
when you take the quick path for a real reason make the debt visible
whenever you can t write the comment short redesign
where a cache should live
where a criterion is a claim about what that command printed before the change run it first too and quote both
where a lighter touch has room
where a special case can be removed by redesigning semantics so it does not arise redesign it away and leave it out of the interface
where acceptance criteria a plan file or a behavior record exist for this work check three things and report what failed
where did 1 hour come from
where filter is a single structured value with one documented encoding
where id 1 and version 2
where it breaks or what would change the answer
where it fits have the learner predict the result before it runs
where stale data lives if the contract isn t clear
where stop 2 also fires say so here since the yes at this stop is what asks for the plan
where the plan lives and any step that ran differently from the approved wording
where the repo already keeps plans
where the stack will not compress name the relation with a preposition resistance at the light connection
where they can be tested once
where thumbnailurl either points to a pre generated thumbnail
whether a channel is available for a user
whether a key pair already exists for this app
whether the body asserts it is on you
which catches it once and produces the 400
which errors get which treatment
which is a separate transaction and may leave a captured payment with no
which line carries your claim and what happens to it if that line is read the other way
which of these was cheaper to fix in this diff than it will ever be again
which rung of the error ladder an edge case sits on and why
which staleness category does this fit the guessing question what
which toby simplify code owns
which toby swd experiment owns
which toby swd interfaces owns and skip it for an edit inside one
whichever choice you make name
while i was in there is how a two file diff becomes a nine file diff
who asked and the words they used
who knows what else just show generic error
why a file or module owns the behavior deep modules the knowledge each one owns
why does that quote mean what you say it means
why is the checkout flow slow
why it does what it does
why it hurts every caller that formats a name now silently depends on this
why it quietly hurts
why one design carries lower risk where complexity lives
why this is slower than you expect
wide desktop the scene with mouse and keyboard read state beside it
wide interface near implementation
widen the interface you already have per toby swd interfaces and wait for it
width and height are getters
will do with the return values the interface is too low level
will send the email twice when the retry succeeds
will show one expensive call
will this generalize without becoming a god interface generality with a clear single purpose is depth
window addeventlistener resize onresize
window addeventlistener resize onresize
wire breaking change because existing clients call an rpc that no longer exists
wire discriminated union a payload with a type tag is validated once at the edge then dispatched with rung 3 so the compiler tracks every case
wires keyboard events arrow keys move highlightedindex enter selects
wiring api routes ts 142 router post orders id cancel cancelorder
wiring slice one at screens members tsx 210 inviteform onsubmit createinvite
with 50 items fine
with 500 contacts that s 501 bridge crossings
with a 2ms db ping
with a sensible retry with backoff a network
with email only login
with it you have
with no local convention follow the language idiom
with no side effects outside the database no email sends no api calls
with nothing present propose docs behavior md and get a yes before creating it
with nothing present propose docs behavior md and get a yes before creating it the same gate the plan file gets
with nowhere obvious propose a path and get a yes the same gate the behavior record gets
with public methods and no one declares it implements anything explicitly
with session scope as s
with session scope as s
with storage clear it was a side effect that could
with usenativedriver skips the js thread round trip entirely
withdrawal reduces the account balance
withhold the answer and elicit only when the decision is a real fork with more than one defensible answer the learner has the context in front of them to reason about it and there is no rush in the room
within them vary everything
without it fetch in the background insert when it arrives this is a
without it the deadpan reads cold
without that line the handler is a well tested function nothing can reach
without that starting run the criterion is unprovable and that is what you report
without the field comments a caller cannot use this
without the index load all line items for
without this the handler is unreachable
without this you re carrying speculative complexity
withwindowsize and mycomponent are
word count arithmetic warning placards and illustration callouts signage and tallying
wordmark is lowercase toby in inter 600
work down this ladder and stop at the first rung that applies
work down this ladder and stop at the first rung that fits
work engages only on a measurement
work is the investment
worked body font size 13px line height 1 55 color inherit
worked border 1px solid var border hairline border radius var radius base
worked caveat font size 12px opacity 0 78 margin top 8px
worked example three rows premise info tinted derivation watch tinted result stable tinted
worked for something new do it yourself and say the reasoning as you go the decision the option you turned down the cost
worked math font family var font mono font size 14px
worked row derivation background var toby watch soft border bottom color var toby watch tint
worked row derivation worked step color var toby watch text
worked row display grid grid template columns 120px 1fr gap 16px
worked row last child border bottom none
worked row premise background var toby info soft border bottom color var toby info tint
worked row premise worked step color var toby info text
worked row result background var toby stable soft border bottom color var toby stable tint
worked row result worked step color var toby stable text
worked step font family var font mono font size 11px
worked think aloud through a close read or an argument naming the moves a skilled reader makes silently this word repeats three times so i ask what changes each time
worker a is this stale check
worker that invalidates based on database changes
workflow rules stay with the active task skill
working code falls out of that
works and the requirement written where a future reader can audit it
works for analysis diagnosis or decision support decks
works for concept heavy topics where undefined terms would block comprehension
works for evidence led decks where the conclusion is the anchor
works for multi section reference decks
works for short reference cards 6 slides or when the deck is one section of a longer session
works well for reference decks with multiple sections
works when intuition precedes formalism
worse than give up after 5 seconds
worse the updateproduct author maintains a list of cache keys to
worse the user experience is inconsistent one screen says appear to be
worth checking the two mid book mentions before this reading holds all the way through
worth it only with significant new information and a commit that converts every
worth it since a fourth caller is already planned for next sprint
worth it when staleness budget allows
would have had if designed with the change in mind
would this reply diagram the same as the last two same opening move same length same count of sections
wrap effect based logic the same way in every component
wrap the bridge with a typed result
wrap the checkout entry point with checkoutprovider
wrapper then surfaced as recommendations unavailable if it persists
wrapping a single unremarkable string in a type is speculative generality run in reverse
wrapping middleware at the router
wraps the native biometrics module
writable in a module
write cost every insert update updates every relevant index and storage
write each acceptance criterion as three short lines before any code plus one for what stays working the behavior beside this one the change has to leave alone
write each finding as three short lines
write interface comments before the implementation because they are a design tool
write one candidate signatures plus interface comments bodies empty describing what and not how
write one criterion per behavior the request names and stop there
write tests that survive a correct refactor
write the interface comment in full and it gets long
write the module s boundary down once per toby swd modules and toby swd interfaces
write the positive form
write the sentence that connects them or give the thesis and blank one topic sentence
write the test before the implementation when the behavior is well defined
write this file like you are briefing someone smart who is meeting this code cold and has no time for surprises
write today s code knowing every future change inherits its structure
write what the design called for
writing files installing packages codegen snapshot updates migrations seed scripts
writing headings paragraphs labels button text or any substantive copy references copy md
writing in the idiom whose cost class is already known allocation free naturally typed is ordinary design and stays welcome
writing the body first traps you in the structure of whatever you wrote
writing the comment first lets you reject a bad design while it s still text
written during the rolled back window
written when the user asks which includes the yes at stop 1
wrong at multiple levels
wrong mostly already fast low repeat access rate near zero staleness
wrong on the retry count
wrong read which wrong it is
wrote and name any entry that fired
wrote it ran the happy path once it works
x axis label with unit time utc requests n weight kg
x is a fascinating topic
xx nn current slide of total 1 indexed zero padded to 2 digits
xx nn r xx x label
y axis label with unit latency ms pressure kpa accuracy proportion
yes for cacheable responses
yes for most components profile interaction heavy ones
yes sometimes the caller
yet mycomponent cannot be understood without
you are now inside this code with full context
you bring the idea
you do not defend
you do not recommend
you end up with a feature shaped pile of code and no real abstraction
you have information that was not available when the convention was set
you have no memory between sessions so the file is the memory
you ll love you ll wonder how you ever you ll be amazed you ll find that
you need metrics for hit rate miss rate ttl
you present labeled options and let the user pick
you took the shortcut
you want that cost much smaller than that benefit meaning a simple interface over substantial functionality
you would be fast and wrong
your call but decide that before it ships and not after
your memory of a file you changed three steps ago is a guess with a confident tone
your own reading enters only as a challenge
z base 0 z rail 10 z dropdown 50 z nav panel 60
z index strictly layered never invent a value between these
z popover 70 z tooltip 80 z sheet 90 z modal 100 z toast 200
zero references to internal data structures algorithms or named internal steps
zustand redux toolkit slice
