## Per file

### content-t1.md

23 distracting sentences.

1. Task 1, setup sentence. "Relay consists of two packages." The sentence names a count and makes the reader wait two more sentences for the packages.
2. Task 1, restatement. "The `@relay/agents` package contains the runtime, which handles tool calling, memory, and pauses for human approval before a destructive action." The approval pause repeats "wait for a person to approve anything destructive" from two sentences earlier under a second name.
3. Task 1, one-word definition. "Each model provider is an engine, and engines are swappable." A model provider is defined by the single word "engine", which the reader has to translate back into a provider.
4. Task 1, connection. "Relay requires Node 20 or later, and npm installs both packages with one command." The "and" joins the Node requirement to the install command, and the two facts have no relation.
5. Task 1, one-word definition. "A new agent in Relay is a plugin." The sentence defines an agent by one bare word and reads as a slogan.
6. Task 1, what it does not do. "A plugin never imports an engine directly." A README overview tells the reader what a plugin does not do, and no reader assumed it did.
7. Task 2, connection. "The migration takes 6 weeks, and the service costs $400 a month." The "and" welds the schedule to the price with no relation between them.
8. Task 2, restatement. "6 of the 43 cron scripts failed silently last quarter" The slide 2 title repeats the first bullet of slide 1 word for word.
9. Task 2, bullet restates the title. "No alert reported any of the 6 failures." The title already says the failures were silent.
10. Task 2, restatement. "The invoice export was one of the 6 silent failures last quarter." The first bullet of slide 1 already said the invoice export was one of the 6.
11. Task 2, given fact. "On the queue service, the same failure would have paged the on-call engineer after the final retry." The task does not say the export would have failed all 3 retries, so the page is a guess.
12. Task 2, restatement. "The queue service retries a failed job 3 times and then pages the on-call engineer" The slide 4 title repeats the second bullet of slide 1.
13. Task 2, mirrored phrase. "Every job moved to the service gets the same retries and the same page." The phrase "the same retries and the same page" repeats a pattern for sound, a job does not receive a page, and the bullet restates the title.
14. Task 2, restatement. "The service costs $400 a month." The bullet repeats the third bullet of slide 1 and has no relation to the slide 4 title.
15. Task 3, one name. "The build rejects engine imports because they broke every plugin the last time a provider changed its SDK." The writer put the reason in a paragraph after the list, away from step 3, and "rejects" renames what step 3 called "fails the plugin".
16. Task 4, restatement. "Phase 1 of the queue migration moves the 8 highest-risk cron jobs to the queue service." The body repeats the subject line with "Phase 1 of the queue migration" added.
17. Task 4, padding. "- Delete the crontab entries for the 8 jobs, so cron no longer runs them." The "so" clause restates what deleting a crontab entry means.
18. Task 5, padding. "When `processEvent` throws, the handler logs a warning and still returns 200, so the payment provider stops retrying and never resends that event (`api/webhooks.ts:41`)." The words "never resends that event" repeat "stops retrying".
19. Task 5, actor. "The `catch` block is the only check, and it calls `logger.warn(err)` and then lets `res.status(200).send()` run." A catch block cannot permit anything, and "check" is a second name for the error handler.
20. Task 6, connection. "It writes nothing." Two short sentences with no connector read as a clipped slogan.
21. Task 6, literal. "Most of the novel is yours." The reply calls CLAUDE.md "the novel", which turns the user's joke into a second name for the file.
22. Task 6, connection. "Two hours is too long, and this whole failure comes from a hand edit in `~/.claude/skills/toby-voice/SKILL.md`." The "and" joins a judgment of the user's time to the cause, and "whole" is an intensifier.
23. Task 6, actor. "A reinstall with `--force` replaces the installed file, and the hand edit is lost." The passive "is lost" hides that the reinstall deletes the edit, which is the fact the user has to weigh.

### content-t2.md

23 distracting sentences.

1. Task 1, setup sentence. "It consists of two packages." The sentence names a count and makes the reader wait for the packages.
2. Task 1, padding. "Before any destructive action, the agent pauses and waits for a person to approve it." The pair "pauses and waits" says one thing twice, and "human approval" in the sentence before already stated the approval.
3. Task 1, condition after the instruction. "Install both packages with `npm install @relay/ui @relay/agents`, which requires Node 20 or later." The requirement comes after the command, and "which" attaches it to the command when Relay is what requires Node 20.
4. Task 1, what it does not do. "Model providers run as engines that Relay can swap, so a plugin never imports an engine directly." The overview states what a plugin does not do, "run as engines" is a figure, and "so" invents a cause the task did not give.
5. Task 2, bullet restates the title. "6 silent failures out of 43 scripts is 14% in one quarter." The bullet repeats the title with a percentage added.
6. Task 2, bullet restates the title. "No alert reached an engineer, so a customer found the gap." The title already says a customer noticed, and "the gap" is a new name for the missing invoice export.
7. Task 2, given fact. "A failure like the 9-day invoice gap would page the on-call engineer after the final retry." The task does not say the export would have failed all 3 retries.
8. Task 2, label with no sentence. "Unknown: how long the 3 retries take before the page goes out." A label and a colon stand where a subject and a verb belong.
9. Task 2, bullet restates the title. "Phase 1 moves the highest-risk jobs, so those jobs get retries and paging before the rest." The title already says "with the highest-risk jobs first", and "get retries and paging" uses nouns for the service's actions.
10. Task 2, bullet restates the title. "$400 a month is $4,800 a year." The bullet gives the title's cost again in another unit.
11. Task 2, label with no sentence. "Unknown: what the 9-day invoice gap cost." The line is a label and a noun clause with no main verb.
12. Task 3, connection. "The build fails the plugin with `E_ENGINE_IMPORT`, because engine imports broke every plugin the last time a provider changed its SDK." The "because" attaches the history to this plugin's failure, when the history explains why the rule exists.
13. Task 4, sentence about the document. "This commit is phase 1 of the 3-phase queue migration." The sentence describes the commit and delays what the commit does.
14. Task 4, connection. "The queue service retries a failed job 3 times and pages the on-call engineer on the final failure, and cron sent no alert when 6 scripts failed last quarter." The sentence runs 30 words and joins two facts with "and", so the reader supplies the contrast.
15. Task 4, restatement. "- Move the 8 highest-risk cron jobs, including the invoice export, to the queue service." The bullet repeats the subject line.
16. Task 5, over 25 words. "A webhook event that `processEvent` fails on is never processed, because the handler logs a warning and still returns 200, so the payment provider stops retrying it (`api/webhooks.ts:41`)." The sentence runs 28 words, chains "because" and "so" in reverse order, and "fails on" replaces the word "throws".
17. Task 5, actor. "The `catch` block is the only check, and it calls `logger.warn(err)` and lets execution continue to `res.status(200).send()`." A catch block cannot let anything happen, and "check" is a second name for the error handler.
18. Task 5, closing caveat. "This finding comes from reading the diff, and I did not run the handler." The writer put the caveat at the end of the finding as its own closing line.
19. Task 6, restatement. "Everything outside the markers stays as it is." The words "replaces only the text between the markers" in the sentence before already said it.
20. Task 6, literal. "About two thirds of the novel is yours." The reply calls CLAUDE.md "the novel", a second name taken from the user's joke.
21. Task 6, unverified claim. "The fix for this one is a single reinstall." Nothing in turn 4 shows a reinstall fixes the test, "this one" has no noun, and "single" is emphasis.
22. Task 6, connection. "`scripts/test-install.sh` passes after I reinstalled with `--force`, so the install is fixed, and the hand edit in `~/.claude/skills/toby-voice/SKILL.md` is gone." A third clause about the lost edit is welded on with "and" and gets no relation to the fix.
23. Task 6, literal. "Deleting the validator also deletes the output those gates read." Deleting the validator stops new output, and the output already written stays. The sentence also ends the reply as a verdict.

### content-t3.md

16 distracting sentences.

1. Task 1, noun doing a verb's job. "The runtime handles tool calling, memory, and a pause for a person's approval before any destructive action." The phrase "handles ... a pause" makes a noun do the work of "pauses".
2. Task 1, given fact. "Relay requires Node 20 or later, so check `node --version` before you install both packages with this command." The task gave no `node --version` step, and "this command" points at the document.
3. Task 1, what it does not do. "Model providers are swappable engines in Relay, so a plugin never imports an engine directly." The sentence defines providers by a figure, states what a plugin does not do, and "so" invents a cause.
4. Task 2, restatement. "Nothing reported any of the 6 failures when it happened." The word "silently" in the bullet above already says it, and "it" does not agree with "failures".
5. Task 2, mirrored bullets. "Phases 2 and 3 move the remaining scripts." The bullet copies the construction of "Phase 1 moves the highest-risk cron scripts."
6. Task 2, restatement. "After approval, phase 1 of the 6-week migration begins with the highest-risk cron scripts." The bullet repeats slide 4.
7. Task 2, mirrored bullets. "Without approval, the 43 cron scripts stay on the two unowned hosts, where 6 of them failed silently last quarter." The pair "After approval" and "Without approval" mirror each other, and the second half repeats slide 1.
8. Task 3, ambiguous parse. "The build fails any plugin that does with `E_ENGINE_IMPORT`." The words "does with" read as one phrase on the first pass, so the reader has to parse the sentence again.
9. Task 4, connection. "The queue service retries a failed job and pages the on-call engineer on the final failure." No connector ties the service to the invoice export in the sentence before, so the reader supplies why the commit mentions it.
10. Task 4, padding. "- Delete the crontab entries for the 8 jobs, so cron no longer runs them." The "so" clause restates what deleting a crontab entry means.
11. Task 5, given fact. "The only record of the event is the `logger.warn(err)` line." The line logs `err`, and nothing in the diff shows the event body reaches the log.
12. Task 5, padding. "`processEvent(req.body)` throws for any reason on a webhook call from the payment provider, which supplies `req.body`." The clause "which supplies `req.body`" tells the reader nothing the route does not already show.
13. Task 6, restatement. "Your CLAUDE.md already has the Toby marker block, so the installer replaces only the text between the markers, and your text outside them stays." The last clause repeats what "only" already said.
14. Task 6, literal. "Most of the novel is yours." The reply calls CLAUDE.md "the novel", a second name taken from the user's joke.
15. Task 6, pronoun with no noun. "Two hours is a long time to spend on this." The word "this" has no noun, and the line comments on the user's time without giving a fact.
16. Task 6, connection. "I did not run `evals/run.py gates`, and the gates are not verified." The relation is cause, and "and" hides it.

### content-t4.md

12 distracting sentences.

1. Task 1, noun doing a verb's job. "The runtime handles tool calling, memory, and the wait for a person's approval." The phrase "the wait for" turns the verb "waits" into a noun.
2. Task 1, connection. "Relay requires Node 20 or later, and you install both packages from npm." The "and" joins the Node requirement to the install step with no relation between them.
3. Task 1, what it does not do. "Model providers are swappable engines, so a plugin never imports an engine directly." The overview states what a plugin does not do, and "so" invents a cause the task did not give.
4. Task 2, bullet restates the title. "Neither host has an owner, so no team is responsible for a failed script." The title already says nobody owns the hosts, and "no team" brings in teams the task never mentioned.
5. Task 2, given fact. "Under the queue service, that failure would have paged the on-call engineer after 3 retries." The task does not say the export would have failed all 3 retries.
6. Task 2, bullet restates the title. "The lowest-risk cron scripts move in the last phase." The bullet states the title "highest-risk jobs first" in reverse.
7. Task 3, ambiguous parse. "The build fails any plugin that does with `E_ENGINE_IMPORT`." The words "does with" read as one phrase on the first pass.
8. Task 4, sentence about the document. "This change is phase 1 of the 3-phase queue migration, which moves the highest-risk jobs first." The sentence describes the commit and delays what the commit does.
9. Task 5, given fact. "The only record of the event is the `logger.warn` line." The line logs `err`, and nothing in the diff shows the event body reaches the log.
10. Task 6, unclear pronoun. "Anything you wrote inside that block gets replaced with it." The word "it" could mean the block or the installer's copy, so the reader has to guess.
11. Task 6, literal. "About two thirds of the novel is yours." The reply calls CLAUDE.md "the novel", a second name taken from the user's joke.
12. Task 6, literal. "After two hours, the cause is a hand edit in `~/.claude/skills/toby-voice/SKILL.md`." The cause did not begin after two hours, so the time phrase reads as false, and the sentence stands alone as a paragraph.

### content-t5.md

18 distracting sentences.

1. Task 1, connection. "Relay has two packages, and `@relay/ui` contains its shared generative UI components." The "and" joins the count to one package and holds the second package for the next sentence.
2. Task 1, restatement. "The `@relay/agents` package contains the runtime, which handles tool calling, memory, and pauses for human approval before a destructive action." The approval pause repeats "wait for a person to approve anything destructive" from the sentence before under a second name.
3. Task 1, what it does not do. "Relay treats model providers as swappable engines, and a plugin never imports an engine directly." The first clause is the comparison this reader already named as failing, and the second states what a plugin does not do.
4. Task 2, bullet restates the title. "The invoice export failed silently, so no alert went to anyone during those 9 days." The title and slide 1 already give both facts.
5. Task 2, bullet restates the title. "A customer found the failure before any engineer did." The title already says the export was missing before a customer noticed.
6. Task 2, bullet restates the title. "Every job that moves to the queue service gets the same retry and paging behavior." The title already states the retries and the page, and "behavior" is a vague noun.
7. Task 2, actor. "A failed cron script alerts nobody today, as the 6 silent failures last quarter showed." Failures cannot show anything, and the bullet repeats slides 1 and 2.
8. Task 2, mirrored bullets. "Phases 2 and 3 move the remaining jobs." The bullet copies the construction of "Phase 1 moves the highest-risk jobs."
9. Task 2, actor. "Approval starts phase 1 with the highest-risk jobs." Approval cannot start work, because a team starts phase 1 after the approval.
10. Task 3, ambiguous parse. "The build fails any plugin that does with `E_ENGINE_IMPORT`." The words "does with" read as one phrase on the first pass.
11. Task 4, padding. "- Delete the crontab entries for the 8 jobs, so cron no longer runs them." The "so" clause restates what deleting a crontab entry means.
12. Task 5, literal. "Nothing in the diff runs the failed event again." An event is processed or delivered again, and nothing runs an event.
13. Task 5, actor. "Any webhook request the payment provider sends to this route makes `processEvent` throw, for any reason." The sentence says the request causes the throw, and "any ... for any reason" states the scope twice.
14. Task 5, what it does not do. "The diff has no rethrow and no non-200 response, and a warning in the log does not make the provider retry." No reader thought a log warning makes the provider retry, and "no rethrow and no non-200 response" is a mirrored pair.
15. Task 5, closing caveat. "This finding comes from reading the diff, because nothing ran." The "because" makes the missing run the cause of the reading, and the caveat closes the finding.
16. Task 6, restatement. "The installer replaces only the text between the markers, so it keeps everything outside them." The "so" clause repeats what "only" already said.
17. Task 6, literal. "You wrote about two-thirds of the novel." The reply calls CLAUDE.md "the novel", a second name taken from the user's joke.
18. Task 6, given fact. "Stop searching the repo, because the cause is outside it." The user never said they were searching the repo.

## Across files

92 sentences failed in total.

1. Restatement of a title, subject line, earlier slide, or earlier sentence: 23
2. Connection, meaning unrelated clauses joined by "and", a missing connector, or a misattached "because": 11
3. Literal test, including "the novel" for CLAUDE.md: 8
4. Given fact: 7
5. What it does not do: 6
6. Actor test: 6
7. Padding, meaning a clause that does no work: 6
8. Mirrored bullets or phrases: 4
9. Ambiguous parse: 3
10. Setup sentence: 2
11. One-word definition: 2
12. Noun doing a verb's job: 2
13. Label with no sentence: 2
14. Sentence about the document: 2
15. Closing caveat: 2
16. Pronoun with no noun or an unclear antecedent: 2
17. One name: 1
18. Condition after the instruction: 1
19. Over 25 words: 1
20. Unverified claim: 1

## Passed

### Task 1

- content-t4.md. "Relay ships as two packages, `@relay/ui` for shared generative UI components and `@relay/agents` for the runtime." Borderline. "Ships" is the software sense of release, and the task used the word.
- content-t2.md. "`@relay/ui` contains the shared components for generative UI, and `@relay/agents` contains the runtime for tool calling, memory, and human approval." Borderline. The two clauses are parallel inside one sentence, and each names a different package with its contents.
- content-t3.md. "To add an agent, write a plugin." Borderline. The sentence is an instruction with its purpose first, and it avoids defining an agent by one word.
- content-t5.md. "Install both packages with npm on Node 20 or later." Borderline. The sentence is short but keeps its verb, object, and requirement.
- content-t1.md. "A plugin consists of a JSON manifest and handler functions, and Relay compiles the manifest at build time." Plain pass. Both clauses concern the manifest, and both facts were given.

### Task 2

- content-t1.md. "Scheduled jobs should move from cron scripts to a queue service" Borderline. Jobs as the subject of "move" is ordinary English, and "should" marks the title as the proposal.
- content-t4.md. "Each of last quarter's 6 silent failures would have either succeeded on a retry or paged the on-call engineer." Borderline. The bullet opens like "Each failed job gets 3 retries", and its either-or claim stays within the given facts.
- content-t3.md. "Approve the queue service at $400 a month" Borderline. The title is an imperative, and it states the request and the price plainly.
- content-t2.md. "Phases 2 and 3 move the remaining lower-risk jobs." Borderline. The bullet before it has a "so" clause, so the pair does not mirror word for word, and "lower-risk" follows from "highest-risk jobs first".
- content-t5.md. "After phase 3, no scheduled job runs as a cron script on the two unowned hosts." Borderline. The sentence is negative, and it states the end state that leadership is approving.

### Task 3

- content-t1.md. "Run `npm run plugins:check` before you open a PR." Borderline. The time clause follows the instruction, and the step reads in the order the agent acts.
- content-t3.md. "The build has this check because engine imports broke every plugin the last time a provider changed its SDK." Borderline. "This check" is a second name for the `E_ENGINE_IMPORT` failure, and it sits directly after that failure.
- content-t4.md. "This rule exists because engine imports broke every plugin the last time a provider changed its SDK." Borderline. "This rule" points at the prohibition in the same step, and the sentence gives its cause.
- content-t5.md. "The build blocks these imports because they broke every plugin the last time a provider changed its SDK." Borderline. "Blocks" renames "fails", and the sentence stays inside the same step.
- content-t2.md. "In each handler, get the engine from `ctx.engine`." Plain pass. The instruction names the place and the identifier.

### Task 4

- content-t5.md. "Phase 1 of the queue migration covers the 8 highest-risk jobs, and the invoice export is one of them." Borderline. It overlaps the subject, and it adds the phase and the invoice export.
- content-t3.md. "These 8 jobs are phase 1 of the 3-phase queue migration." Borderline. "These" points at the subject line, and the sentence adds which phase the commit is.
- content-t2.md. "The other 35 cron scripts stay on cron until phases 2 and 3." Borderline. The sentence states what the commit leaves out, which a reader of a phase 1 commit would ask, and 43 minus 8 is 35.
- content-t4.md. "- Add a test asserting that the invoice export pages the on-call engineer after its third failed attempt." Plain pass. A commit bullet may open on its verb.
- content-t1.md. "The invoice export is one of the 8 jobs." Plain pass. The sentence gives one given fact.

### Task 5

- content-t1.md. "The payment provider posts a webhook and `processEvent(req.body)` throws for any reason, because every error goes to the `catch` block." Borderline. "Goes to" is the usual wording for control flow into a catch block.
- content-t5.md. "When `processEvent(req.body)` throws, the handler logs a warning and still returns 200, so the payment provider stops retrying and the event is dropped (`api/webhooks.ts:41`)." Borderline. "Dropped" is the common word for a lost message, and the cause and effect run in order.
- content-t2.md. "A retry delivers the same event again, so `processEvent` has to handle a repeat delivery." Borderline. The repeat delivery follows from the suggested fix, and the sentence names what the fix requires.
- content-t3.md. "Return a 5xx status from the `catch` block, so the provider retries the event." Borderline. The task says a 200 stops retries, so a retry after a 5xx follows from the given fact.
- content-t4.md. "No path in the new block sends a non-200 status." Borderline. "The new block" is a second name for the `catch` block, and the sentence before names that block.

### Task 6

- content-t3.md. "A reinstall with `--force` replaces the installed file with the repo copy, which drops that edit." Borderline. "Drops" means deletes in common use, and the actor is the reinstall.
- content-t5.md. "Any edit you made between the markers is lost." Borderline. The sentence is passive, and the sentence before names the installer as the actor.
- content-t4.md. "Should I run the reinstall with `--force`?" Borderline. The question stands alone as a paragraph, and it asks for the approval the turn needs.
- content-t2.md. "Keep the validator." Borderline. The two-word opener is terse, and it puts the disagreement first.
- content-t1.md. "The install is fixed." Borderline. The four-word verdict names the install, which the reinstall changed, and the next sentence gives the passing test.

## Sentence types

1. A single-clause statement of one fact. About 28% of all sentences. This reader does not find it grating.
   - content-t4.md. "Each failed job gets 3 retries."
   - content-t2.md. "The 43 scripts run on two hosts that nobody owns."
   - content-t1.md. "The invoice export is one of the 8 jobs."
2. Two clauses joined by ", and". About 20%. Grating when the two clauses have no relation.
   - content-t1.md. "The migration takes 6 weeks, and the service costs $400 a month."
   - content-t4.md. "Relay requires Node 20 or later, and you install both packages from npm."
   - content-t3.md. "`--dry-run` prints every copy and merge the install would make, and it writes nothing."
3. A cause and effect joined by "so" or "because". About 14%. Grating when the "because" attaches to the wrong clause or the cause was invented.
   - content-t2.md. "I did not run `evals/run.py gates`, so the gates are not verified."
   - content-t5.md. "Yes, because your CLAUDE.md already has the Toby marker block."
   - content-t3.md. "Model providers are swappable engines in Relay, so a plugin never imports an engine directly."
4. An instruction in the imperative. About 12%. This reader does not find it grating.
   - content-t1.md. "Return a 5xx status from the `catch` block, so the provider retries the event."
   - content-t4.md. "Approve the 6-week migration to the queue service."
   - content-t5.md. "Create `plugins/<name>/` and add `manifest.json` and `handlers.ts` to it."
5. A restatement of a title, subject line, or earlier sentence. About 8%. Grating.
   - content-t5.md. "A customer found the failure before any engineer did."
   - content-t2.md. "6 silent failures out of 43 scripts is 14% in one quarter."
   - content-t1.md. "Phase 1 of the queue migration moves the 8 highest-risk cron jobs to the queue service."
6. A definition in the form "X is a Y" or "X consists of Y". About 5%. Grating when the definition is one bare word or a figure.
   - content-t1.md. "A new agent in Relay is a plugin."
   - content-t3.md. "Model providers are swappable engines in Relay, so a plugin never imports an engine directly."
   - content-t4.md. "Each plugin is a directory at `plugins/<name>/` that contains `manifest.json` and `handlers.ts`."
7. A sentence that opens on its condition or time. About 5%. This reader does not find it grating.
   - content-t3.md. "If you want to keep the edit, copy it into the repo's `skills/toby-voice/SKILL.md` first."
   - content-t4.md. "When `processEvent` throws, the handler logs a warning and returns 200, so the payment provider stops retrying and the event is never processed (`api/webhooks.ts:41`)."
   - content-t5.md. "If your runs take longer than 1.9 seconds, the extra time comes from another step."
8. A statement of what does not happen. About 4%. Grating when no reader assumed it happens, and acceptable in a review Guard, where the missing check is the finding.
   - content-t1.md. "A plugin never imports an engine directly."
   - content-t5.md. "The diff has no rethrow and no non-200 response, and a warning in the log does not make the provider retry."
   - content-t2.md. "No line in the diff sends a non-2xx status after a failure."
9. A question to the user. About 3%. This reader does not find it grating.
   - content-t4.md. "Which step is slowing you down?"
   - content-t3.md. "Should I reinstall?"
   - content-t1.md. "Should I copy the edit into `skills/toby-voice/SKILL.md` first, or reinstall now?"
10. A label followed by text. About 1%. Grating when the text after the label is a fragment, and acceptable when it is a whole sentence.
    - content-t2.md. "Unknown: what the 9-day invoice gap cost."
    - content-t3.md. "**Guard.** The `catch` block calls `logger.warn(err)`, and execution then continues to `res.status(200).send()`."
    - content-t4.md. "**Fires when.** The payment provider sends a webhook to this route, and `processEvent(req.body)` throws while handling it."
