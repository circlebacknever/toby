## Per file

Task numbers come from `evals/suites/content.md` for the `content-*.md` files and from `evals/suites/voice.md` for the `voice-*.md` files.

### content-s1.md

11 distracting sentences.

1. "The runtime handles tool calling, memory, and the pause for human approval before a destructive action." Task 1, verb used as a metaphor. The rules name "pause" as something an agent does not do, and the approval fact already appears two sentences earlier.
2. "Model providers are engines, and an app can swap one engine for another." Task 1, thing defined by one bare word. "Are engines" defines the providers by a single noun and says nothing about what they do.
3. "Each new agent is a plugin, which consists of a JSON manifest and a set of handler functions." Task 1, thing defined by one bare word. "Each new agent is a plugin" is the `A plugin is data.` form that the rules replace with "New agents are created as plugins."
4. "A silent failure sends no alert, so nobody hears about it from the script." Task 2, restates what a word already says. "Silent" already means no alert, so the bullet spends a line defining the title's own adjective.
5. "A job that succeeds on a retry pages nobody." Task 2, mirrored pair. It is built to match the next bullet phrase for phrase, and nobody expected a page on success.
6. "A job that fails its last retry pages the on-call engineer, so an engineer learns of the failure from the page." Task 2, mirrored pair. It matches the bullet above, and the "so" clause repeats that a page tells the engineer.
7. "Follow these steps to add one." Task 3, section narration. The numbered list follows directly, so the sentence tells the reader nothing.
8. "The build fails a plugin that does with `E_ENGINE_IMPORT`." Task 3, ambiguous sentence structure. "That does with" reads as one phrase until the reader backs up and attaches "with" to "fails".
9. "Phase 1 of 3 in the queue migration." Task 4, noun phrase standing as a sentence. It has no verb.
10. "**Guard** is missing." Task 5, dropped article. The bold label becomes the subject without "the", so the sentence reads as a clipped note.
11. "This failure has one cause, so the fix is small." Task 6 turn 4, deferred content. It announces a cause without naming it, and "the fix is small" is a size the facts never state.

### content-s2.md

6 distracting sentences.

1. "The service tries a failing job 3 times and pages the on-call engineer on the final failure." Task 2, restates the slide title. The title already says the service pages on-call when a job fails its third attempt.
2. "A job on the queue service either succeeds on a retry or pages someone, so a failure cannot stay silent." Task 2, closing verdict. "A failure cannot stay silent" is a payoff line, and it echoes the word "silently" from slide 2.
3. "Phase 1 moves the highest-risk jobs, so the jobs that cost the most when they fail get retries and paging first." Task 2, fact the task did not give. The facts say "highest-risk" and never define risk as cost.
4. "Follow these steps to add a plugin." Task 3, section narration. The numbered list follows directly and needs no announcement.
5. "Guard: none." Task 5, noun phrase standing as a sentence. A label and one word stand where a sentence belongs.
6. "One file causes this failure." Task 6 turn 4, deferred content. It says one file is at fault and holds the file back until the next sentence.

### content-s3.md

10 distracting sentences.

1. "This deck proposes moving all 43 jobs to a queue service." Task 2, section narration. It describes the deck to the people reading the deck, where the bullet could state the proposal itself.
2. "None of the 6 failures raised an alert." Task 2, restates what a word already says. The title's "silently" already means no alert was raised.
3. "The final failure pages the on-call engineer." Task 2, wrong actor. The queue service sends the page, and this sentence gives that action to the failure.
4. "Last quarter's invoice export failure would have paged the on-call engineer on its final retry." Task 2, wrong actor. The failure sends the page again, and "Last quarter's invoice export failure" is a four-word noun stack.
5. "Each plugin lives in its own directory and consists of two files." Task 3, deferred content. It names "two files" and holds back which two until step 1.
6. "The build fails any plugin that does with `E_ENGINE_IMPORT`." Task 3, ambiguous sentence structure. "That does with" reads as one phrase until the reader attaches "with" to "fails".
7. "Move 8 highest-risk cron jobs to the queue service" Task 4, dropped article. The subject line drops "the" before "8", which the other four files keep.
8. "**Guard.** None in the diff." Task 5, noun phrase standing as a sentence. "None in the diff" has no verb.
9. "This finding rests on reading the diff, and I did not run the handler." Task 5, verb used as a metaphor. A finding does not rest on anything, and the caveat sits as a closing line after the fix and the question.
10. "This failure has a single cause." Task 6 turn 4, deferred content. It announces a cause and holds it back until the next sentence.

### content-s4.md

10 distracting sentences.

1. "The scripts run on two hosts, and no team owns either host." Task 2, restates the slide title. The title already says the scripts run on hosts that no team owns.
2. "None of the 6 failures sent an alert." Task 2, restates what a word already says. "Silently" in the title already says no alert went out.
3. "A failed invoice export would page the on-call engineer." Task 2, run of short sentences with no connector. It sits beside the next sentence with the contrast between them unstated.
4. "Last quarter, a customer found that failure after 9 days." Task 2, run of short sentences with no connector. The reader has to supply the "whereas" that links it to the sentence before.
5. "This proposal asks leadership to approve the 6-week migration." Task 2, section narration. It sits in the subtitle slot and describes the proposal to the leaders it addresses.
6. "Without the migration, the 43 scripts stay on two hosts that no team owns." Task 2, closing verdict. It is the last bullet of the deck, and it repeats slide 1 as a warning.
7. "Phase 1 of the queue migration moves the 8 highest-risk cron jobs to the queue service, including the invoice export." Task 4, ambiguous sentence structure. "Including the invoice export" attaches to "the queue service" on first read.
8. "`api/webhooks.ts:41`" Task 5, noun phrase standing as a sentence. The finding opens on a bare path with no claim.
9. "**Guard:** None." Task 5, noun phrase standing as a sentence. A label and one word stand where a sentence belongs.
10. "One file causes this failure." Task 6 turn 4, deferred content. It holds the file back until the next sentence.

### content-s5.md

11 distracting sentences.

1. "The `@relay/ui` package contains the generative UI components that agents share." Task 1, fact the task did not give. The facts say "shared generative UI components" and never say that agents share them.
2. "Each new agent is a plugin, which consists of a JSON manifest and a set of handler functions." Task 1, thing defined by one bare word. It opens on the `A plugin is data.` form that the rules replace.
3. "A script that fails on either host sends no alert to anyone." Task 2, fact the task did not give. The facts say 6 scripts failed silently and say nothing about every script on both hosts.
4. "Under the queue service, the invoice export failure would have paged on-call before a customer found it." Task 2, wrong actor. The failure sends the page in this sentence, and "Under the queue service" is an odd preposition for a job running on it.
5. "Approve the $400 monthly cost." Task 2, mirrored pair. It and the next bullet both open on "Approve the" as a matched set.
6. "Approve the 6-week migration, starting with phase 1." Task 2, mirrored pair. It matches the bullet above, and one sentence could hold both approvals.
7. "**Guard.** None." Task 5, noun phrase standing as a sentence. A label and one word stand where a sentence belongs.
8. "This rests on reading the diff at api/webhooks.ts:41, because the handler did not run." Task 5, verb used as a metaphor. "Rests on" is figurative, "This" has no noun, and "the handler did not run" hides who did not run it.
9. "The last 290 lines of that novel are mine." Task 6 turn 3, fact the task did not give. The user asked which lines are theirs, and the reply leads by claiming the installed block as Toby's own.
10. "This failure comes from one file." Task 6 turn 4, deferred content. It holds the file back until the next sentence.
11. "The install test is fixed." Task 6 turn 5, claim of fixed that the facts do not support. The reinstall changed the installed skill and left the test untouched, so "the test is fixed" names the wrong thing.

### voice-s1.md

3 distracting sentences.

1. "One behavior changes." Task 3, deferred content. It names a category and holds the behavior back until the next sentence.
2. "The test sends requests past the limit and checks that the response status is 429." Task 3, fact the task did not give. The task says a test for the 429 path was added and does not say how the test works.
3. "A change to the base image, the runtime version, the config, or the traffic on Tuesday would not appear in the diff." Task 4, closing caveat. It qualifies the second sentence and sits at the end, after the next steps.

### voice-s2.md

5 distracting sentences.

1. "My machine uses a different timezone, so the test passes locally every time." Task 1, fact the task did not give. The task never names the local timezone, and a different timezone alone does not guarantee a pass.
2. "I am guessing the mismatch only shows at certain hours of the day, and I have not checked that." Task 1, pronoun without a noun. "That" ends the sentence alone, where the rules require a noun after it.
3. "The four route handlers now call the rate-limit middleware and contain no rate-limit code." Task 3, fact the task did not give. The task does not say that the handlers call the middleware.
4. "A new test sends requests past the limit and asserts that the response is 429." Task 3, fact the task did not give. The task does not describe how the test works.
5. "After this PR, requests to all four routes count against one limit, so clients that call several routes will get 429 responses sooner." Task 3, restates an earlier sentence. The first sentence of the section already says the limit applies once per account across all routes.

### voice-s3.md

5 distracting sentences.

1. "Adds a middleware that runs the rate-limit check for the four routes." Task 3, dropped subject. The bullet drops "This PR", and the rules say to keep the subject.
2. "Deletes the token-bucket code that each of the four handlers had its own copy of." Task 3, dropped subject. It has no subject, and "had its own copy of" leaves the sentence ending on a preposition.
3. "Adds a test for the 429 response, which had no test before this PR." Task 3, dropped subject. The bullet drops "This PR" like the two above it.
4. "I can also check whether that deploy changed a dependency version or a cache size, and whether traffic rose that week." Task 4, closing offer. It offers more work at the end of the reply, and "a cache size" is a guess the task did not raise.
5. "None of those would show up in the code diff." Task 4, closing caveat. It is the last line, "those" has no noun, and it repeats that the diff shows nothing.

### voice-s4.md

2 distracting sentences.

1. "Until the upserts land, leave the job without a retry, because a failed run costs less to repair than a doubled one." Task 2, closing verdict. "Land" is a verb used as a metaphor, and "a doubled one" is a turn of phrase built to end the reply.
2. "Before this PR, each route kept a separate limit for each account, so an account got the full allowance on every route." Task 3, second name for one thing. "Allowance" renames "limit" inside the same sentence, so the reader looks for a second quantity.

### voice-s5.md

1 distracting sentence.

1. "An account that spreads requests across those routes now shares one bucket, so it can reach a 429 sooner." Task 3, verb used in a non-literal sense. The four routes share the bucket, and an account has nobody to share it with.

## Across files

64 sentences across 10 files.

1. Fact the task did not give, 8
2. Deferred content, where a cause, a file, or a category is announced before it is named, 7
3. Noun phrase or label standing as a sentence, 6
4. Dropped article or subject, 5
5. Section narration, 4
6. Mirrored pair, 4
7. Verb used as a metaphor or in a non-literal sense, 4
8. Restates what a word already says, 3
9. Restates the title or an earlier sentence, 3
10. Thing defined by one bare word, 3
11. Wrong actor for the verb, 3
12. Ambiguous sentence structure, 3
13. Closing verdict, 3
14. Run of short sentences with no connector, 2
15. Closing caveat, 2
16. Closing offer, 1
17. Pronoun without a noun, 1
18. Second name for one thing, 1
19. Claim of fixed that the facts do not support, 1

The three slogan forms in this list, the mirrored pair, the run of short sentences, and the bare-word definition, total 9.

## Passed

### Content task 1, platform overview

1. content-s3, "Model providers are swappable engines, and a plugin never imports an engine directly." Borderline. The "and" leaves the relation between the halves unstated, but both halves are given facts and the definition has an adjective that says what engines allow.
2. content-s5, "Relay needs Node 20 or later, and this npm command installs both packages:" Borderline. The colon introduces the code block that follows, so no second clause is welded on.
3. content-s1, "A plugin never imports an engine directly, so swapping the engine leaves every plugin unchanged." Borderline. The consequence follows from the given fact that plugins never import an engine.
4. content-s2, "Each model provider runs as a swappable engine." Borderline. "Runs as" is loose wording for "is", but no figurative action is involved.
5. content-s4, "Relay requires Node 20 or later, and npm installs both packages with one command." Borderline. Both halves belong to the install step, which is the relation the reader needs.

### Content task 2, deck

1. content-s3, "The queue service reports every failed job to the on-call engineer" Borderline. "Every failed job" stretches the fact about the final failure, but the bullets under it state the retry rule and the title makes no clever claim.
2. content-s2, "A customer reported the missing invoice export before any engineer noticed it." Borderline. "Reported" extends the given "noticed" slightly, and the order of events matches the facts.
3. content-s4, "A queue service retries failed jobs and pages the on-call engineer when they keep failing" Borderline. "Keep failing" is vague, but the title is a descriptive sentence about what the service does.
4. content-s1, "The export ran as a cron script on one of the two unowned hosts." Borderline. It combines two given facts, since the export was a failed cron script and all cron scripts run on those hosts.
5. content-s5, "Six cron scripts failed silently last quarter" Plain pass. The title is a descriptive sentence with the count in it.

### Content task 3, contributor rules

1. content-s1, "Each plugin lives in `plugins/<name>/` and consists of `manifest.json` and `handlers.ts`." Borderline. "Lives in" is the task's own wording and the common way to say where files sit.
2. content-s2, "The build fails any plugin that imports from `engines/` with `E_ENGINE_IMPORT`." Borderline. "With" could attach to "imports", but spelling out the import keeps the first reading correct.
3. content-s5, "The build enforces the `engines/` rule because engine imports broke every plugin the last time a provider changed its SDK." Borderline. The reason sits below the steps, away from step 3, but it states the failure the rule prevents.
4. content-s4, "Engine imports broke every plugin the last time a provider changed its SDK, so the build fails any plugin that imports from `engines/` with `E_ENGINE_IMPORT`." Plain pass. It gives cause and then consequence inside the step where the reader acts.
5. content-s3, "Before you open a PR, run `npm run plugins:check`." Plain pass. The condition comes before the instruction.

### Content task 4, commit message

1. content-s5, "This commit deletes the crontab entries for those 8 jobs, so the queue service is now the only thing that runs them." Borderline. "Only thing" is vague, but the claim follows from deleting the crontab entries.
2. content-s2, "The remaining 35 cron jobs move in phases 2 and 3." Borderline. The 35 is 43 minus 8, and it tells a reader of the log which jobs still run on cron.
3. content-s1, "It deletes their crontab entries, so cron no longer runs them." Plain pass. The second clause states the effect of the deletion.
4. content-s3, "The 8 jobs include the invoice export." Plain pass. It is a given fact in one clause.
5. content-s4, "A new test asserts that the invoice export pages the on-call engineer after its third failed attempt." Plain pass. It restates the given test in full.

### Content task 5, review finding

1. content-s1, "`api/webhooks.ts:41` loses a payment event whenever `processEvent` throws." Borderline. A file location is the subject of "loses", but the sentence puts the location, trigger, and consequence first.
2. content-s3, "The `catch` block calls `logger.warn(err)` and then falls through to `res.status(200).send()`." Borderline. "Falls through" borrows the switch-statement term, and the control flow it describes is accurate.
3. content-s2, "Consequence: the handler logs a warning and returns 200, so the payment provider stops retrying and the event is never processed." Borderline. The colon follows a label, and the words after it form a whole sentence.
4. content-s5, "The `catch` has no condition, so every error type reaches `res.status(200).send()`." Plain pass. It names the code and the consequence with a connector.
5. content-s4, "Send a 5xx from the catch block so the provider retries the event." Plain pass. It states the action and the reason.

### Content task 6, six replies

1. content-s3, "The install failure is fixed, because `scripts/test-install.sh` passed after I reinstalled with `--force`." Borderline. "Fixed" applies to the install failure, which the rerun test passed, and the next sentence marks the gates not verified.
2. content-s2, "Deleting the validator would cost more than the 1.9 seconds it takes." Borderline. The disagreement comes first, and the next sentence gives the 3 regressions as the cost.
3. content-s1, "Keep the validator, because it takes 1.9 seconds and the gates it feeds caught 3 regressions last week before they shipped." Borderline. "Feeds" is the task's wording and means supplying input to a machine, which is a literal sense of the verb.
4. content-s4, "Keep the validator." Borderline. It is a bare verdict opener, but the user proposed deleting the validator and the evidence follows in the next sentence.
5. content-s5, "Everything outside the markers stays as it is." Borderline. It says what the installer leaves alone, and the user asked whether the overwrite was safe.

### Voice task 1, status update

1. voice-s3, "At a failure rate of one in nine, 30 passing runs would leave about a 3 percent chance that the flake is still there." Borderline. "The flake" is common shorthand for a flaky test, and 8/9 to the 30th power is about 0.029.
2. voice-s1, "I am guessing that the failing runs land near midnight, when the local date and the UTC date differ." Borderline. "Land" is loose, but the guess is labelled and the next sentence says it is unchecked.
3. voice-s4, "The timezone difference explains why CI and local disagree, but it does not yet explain why only one CI run in nine fails." Borderline. The two halves echo each other, but the second half names an open question a reader would assume was answered.
4. voice-s5, "Next I will format the timestamp in UTC inside the test, then rerun the CI job ten times." Borderline. The sentence is plain, though ten runs at one failure in nine leave about a 31 percent chance that all pass with the bug still present.
5. voice-s2, "Then I will run the CI job 30 times and count the failures." Plain pass. It names the step and the count.

### Voice task 2, pushback

1. voice-s3, "So two failures a week become two batches of duplicate rows a week, and duplicates are harder to find than a failed job." Borderline. The first clause echoes its own wording, but it turns the user's number into a cost.
2. voice-s5, "After either change, the retry wrapper is safe to add." Borderline. It ends the reply on a verdict, but it states the condition the user needs before adding the retry.
3. voice-s1, "You can make a retry safe without finding the cause of the failures." Borderline. The user said "I don't have time to chase it", so the "without" answers a position the user held.
4. voice-s2, "The smaller fix is to make a second run safe first." Borderline. "Smaller" has no stated comparison, and the reader has to infer the retry wrapper.
5. voice-s4, "A partial run writes rows before it fails, and the retry writes those rows again." Plain pass. It states the mechanism with named actors.

### Voice task 3, PR description

1. voice-s3, "Before merging, check whether any client depends on the old per-route limit, and raise the limit value if one does." Borderline. Raising the limit is advice the facts leave open, and the sentence states no new fact about the code.
2. voice-s5, "The four route handlers no longer check the limit themselves, and this PR deletes the token-bucket code that each handler had a copy of." Borderline. The first clause states a removal that a reviewer checks for in the diff.
3. voice-s2, "This PR deletes the duplicated token-bucket code from those handlers, so the middleware has the only copy." Borderline. "The only copy" is inferred from the deletion.
4. voice-s1, "As a result, a client that spreads requests across the four routes will get a 429 response sooner." Plain pass. It states the behavior change for a client with a connector.
5. voice-s4, "A client that spreads requests across several routes will now receive 429 responses sooner." Plain pass. It names who sees the change and what they see.

### Voice task 4, "I don't know"

1. voice-s4, "I have no profiler data to tell those apart." Borderline. "Those" has no noun, but the three candidates sit in the sentence directly before.
2. voice-s3, "Without that data I cannot tell whether the growth comes from our code or from something around it." Borderline. "Something around it" is vague, and the next paragraph names dependency version, cache size, and traffic.
3. voice-s2, "I read the diff, and nothing in it allocates, so the cause is somewhere the diff does not show." Borderline. The method takes one clause before the finding, which the reader needs to judge it.
4. voice-s1, "That comparison shows whether the deploy is the cause at all." Plain pass. It names the question the comparison answers.
5. voice-s5, "Nothing in the diff allocates, and I have no profiler data, so any cause I name now would be a guess." Plain pass. It names both unknowns and what follows from them.

## Sentence types

1. Cause and effect joined by "so" or "because". About 30 percent. Not grating.
   - content-s1, "It deletes their crontab entries, so cron no longer runs them."
   - voice-s2, "A retry around the whole sync job will write duplicate rows, because the job is not idempotent."
   - content-s2, "Plugins never import an engine directly, so you can swap the model provider without changing plugin code."
2. One plain fact in one clause. About 22 percent. Not grating alone, and grating when two or three sit in a row with no connector.
   - content-s1, "Relay compiles the manifest at build time."
   - content-s3, "The service retries a failed job 3 times."
   - voice-s2, "I have not fixed it yet."
3. Two facts joined by "and" with the relation unstated. About 12 percent. Mostly passes, and grates when the halves have a causal link the "and" hides.
   - content-s4, "Relay requires Node 20 or later, and npm installs both packages with one command."
   - content-s3, "Model providers are swappable engines, and a plugin never imports an engine directly."
   - voice-s3, "Nothing in the diff allocates, and I have no profiler data."
4. Instruction, often with the condition first. About 10 percent. Not grating, except when paired bullets repeat the same opening verb.
   - content-s3, "Before you open a PR, run `npm run plugins:check`."
   - voice-s4, "If each row has a stable source ID, switch the inserts to upserts keyed on that ID."
   - content-s1, "Return a 5xx status from the `catch` block so the provider retries the event."
5. Descriptive title or subject line. About 8 percent. Not grating.
   - content-s2, "6 cron scripts failed silently last quarter"
   - content-s5, "The migration runs in 3 phases over 6 weeks, with the highest-risk jobs first"
   - voice-s5, "Move rate-limit checks into one middleware"
6. Statement of what is unknown or unverified. About 5 percent. Not grating.
   - content-s1, "I have not run `evals/run.py gates`, so the gates are not verified."
   - voice-s1, "I have not checked the CI run times."
   - voice-s4, "I don't know why memory use rose 40 percent after last Tuesday's deploy."
7. Before-and-after comparison. About 3 percent. Not grating, except when the "after" half repeats a sentence already written.
   - voice-s1, "Before this PR, each route kept a separate limit for each account, so an account could use its full limit on every route."
   - voice-s3, "Before this PR, each route kept a separate bucket per account, so an account could use its full limit on all four routes."
   - voice-s2, "After this PR, requests to all four routes count against one limit, so clients that call several routes will get 429 responses sooner."
8. Question to the user. About 3 percent. Not grating.
   - voice-s1, "Which one fits the sync job's writes?"
   - content-s5, "Should I run the reinstall?"
   - content-s3, "Is `processEvent` safe to run twice for the same event?"
9. Short opener that announces a cause, a file, or a category before naming it. About 2 percent. Grating.
   - content-s2, "One file causes this failure."
   - content-s3, "This failure has a single cause."
   - voice-s1, "One behavior changes."
10. Label or noun phrase standing where a sentence belongs. About 2 percent. Grating.
    - content-s2, "Guard: none."
    - content-s3, "**Guard.** None in the diff."
    - content-s1, "Phase 1 of 3 in the queue migration."
11. Bullet that restates a word from the title. About 1 percent. Grating.
    - content-s1, "A silent failure sends no alert, so nobody hears about it from the script."
    - content-s3, "None of the 6 failures raised an alert."
    - content-s4, "None of the 6 failures sent an alert."
12. Sentence that describes the document or its list. About 1 percent. Grating.
    - content-s1, "Follow these steps to add one."
    - content-s3, "This deck proposes moving all 43 jobs to a queue service."
    - content-s4, "This proposal asks leadership to approve the 6-week migration."
13. Closing verdict, caveat, or offer. About 1 percent. Grating.
    - content-s2, "A job on the queue service either succeeds on a retry or pages someone, so a failure cannot stay silent."
    - content-s4, "Without the migration, the 43 scripts stay on two hosts that no team owns."
    - voice-s3, "I can also check whether that deploy changed a dependency version or a cache size, and whether traffic rose that week."
