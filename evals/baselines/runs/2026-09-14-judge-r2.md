## Per file

Task numbers use "C" for content tasks and "V" for voice tasks.

### content-s1.md

14 distracting sentences.

- C1: "Model providers are engines, and an app can swap one engine for another." One bare word defines the thing, which reads as a slogan, and the task never said an app does the swapping.
- C1: "A plugin never imports an engine directly, so swapping the engine leaves every plugin unchanged." Denial in a README overview, since no reader assumed a plugin imports an engine.
- C2: "A silent failure sends no alert, so nobody hears about it from the script." The bullet restates the title "failed silently" and adds no fact.
- C2: "A job that succeeds on a retry pages nobody." Mirrored pair with the next bullet, and the task never said a successful retry pages nobody.
- C2: "A job that fails its last retry pages the on-call engineer, so an engineer learns of the failure from the page." Mirrored pair, and the bullet restates the slide title.
- C2: "Phase 1 moves the highest-risk jobs to the queue service." The bullet restates the title "starting with the highest-risk jobs".
- C2: "The $400 a month pays for retries and on-call paging on every scheduled job." Given-fact failure, because the task gave the price and nothing about what it covers.
- C3: "Each plugin lives in `plugins/<name>/` and consists of `manifest.json` and `handlers.ts`." Metaphor verb `lives in`.
- C3: "Follow these steps to add one." Sentence about the document.
- C4: "Phase 1 of 3 in the queue migration." Noun phrase standing as a sentence, and it is the guide's own failing example.
- C5: "**Fires when** `processEvent(req.body)` throws on any webhook the payment provider sends." Label fused into a sentence with no subject.
- C5: "**Guard** is missing." Label used as a subject with no article, and "guard" is review jargon the reader has to look up.
- C6: "This failure has one cause, so the fix is small." Setup sentence that holds back the cause, and "the fix is small" was never checked.
- C6: "Keep the validator, because it takes 1.9 seconds and the gates it feeds caught 3 regressions last week before they shipped." Metaphor verb `feeds`, and 1.9 seconds is offered as a reason to keep it.

### content-s2.md

12 distracting sentences.

- C1: "Plugins never import an engine directly, so you can swap the model provider without changing plugin code." Denial in a README overview, and the consequence is inferred.
- C2: "The service tries a failing job 3 times and pages the on-call engineer on the final failure." The bullet restates the slide title.
- C2: "A job on the queue service either succeeds on a retry or pages someone, so a failure cannot stay silent." The bullet restates the title as a slogan conclusion.
- C2: "Phase 1 moves the highest-risk jobs, so the jobs that cost the most when they fail get retries and paging first." Given-fact failure, because the task never defined high risk as cost, and the bullet restates the title.
- C2: "The $400 a month pays for retries and paging on all 43 jobs." Given-fact failure about what the price covers.
- C3: "Each plugin lives in `plugins/<name>/` and contains two files, `manifest.json` and `handlers.ts`." Metaphor verb `lives in`.
- C3: "Follow these steps to add a plugin." Sentence about the document.
- C5: "Guard: none." Label standing as a sentence, and it is the guide's own failing example.
- C6: "Everything outside the markers stays as it is." Restates the sentence before it in reverse.
- C6: "One file causes this failure." Setup sentence, and it is the guide's own failing example.
- C6: "Deleting the validator would cost more than the 1.9 seconds it takes." The position is deferred behind a comparison the reader has to work out.
- C6: "Last week the gates it feeds caught 3 regressions before they shipped." Metaphor verb `feeds`.

### content-s3.md

18 distracting sentences.

- C1: "Model providers are swappable engines, and a plugin never imports an engine directly." Denial in a README overview, and `and` joins two claims with no stated relation.
- C2: "No team is responsible for noticing when one of these scripts fails." The bullet restates the title "no team owns".
- C2: "This deck proposes moving all 43 jobs to a queue service." Sentence about the document.
- C2: "None of the 6 failures raised an alert." The bullet restates the title "failed silently".
- C2: "The queue service reports every failed job to the on-call engineer" Given-fact failure in a title, because only the final failure after 3 retries pages.
- C2: "The service retries a failed job 3 times." Clipped run with the next bullet and no connector.
- C2: "The final failure pages the on-call engineer." Clipped run with the bullet before it.
- C2: "Last quarter's invoice export failure would have paged the on-call engineer on its final retry." Given-fact failure, since nobody knows the retries would have failed too.
- C2: "Phase 1 moves the highest-risk jobs, so they get retries and paging first." The bullet restates the title.
- C2: "The $400 a month pays for retries and on-call paging on all 43 jobs." Given-fact failure about what the price covers.
- C3: "Each plugin lives in its own directory and consists of two files." Metaphor verb `lives in`, and the sentence names a count while holding back the directory and the files.
- C5: "**Consequence.**" Label standing as a sentence.
- C5: "**Fires when.**" Label standing as a sentence, with no subject or object.
- C5: "**Guard.** None in the diff." Label and fragment.
- C5: "The `catch` block calls `logger.warn(err)` and then falls through to `res.status(200).send()`." Metaphor verb `falls through`.
- C5: "This finding rests on reading the diff, and I did not run the handler." Metaphor verb `rests on`, placed as a closing caveat.
- C6: "This failure has a single cause." Setup sentence.
- C6: "It takes 1.9 seconds, and last week the gates it feeds caught 3 regressions before they shipped." Metaphor verb `feeds`.

### content-s4.md

15 distracting sentences.

- C1: "Relay requires Node 20 or later, and npm installs both packages with one command." `and` joins two unrelated claims, so the reader has to supply the relation.
- C1: "A plugin never imports an engine directly, because model providers are swappable engines." Denial in a README overview, and the `because` states a cause the task did not give.
- C2: "The scripts run on two hosts, and no team owns either host." The bullet restates the title "on hosts that no team owns".
- C2: "None of the 6 failures sent an alert." The bullet restates the title "failed silently".
- C2: "If the final attempt fails, the service pages the on-call engineer." The bullet restates the title "pages the on-call engineer when they keep failing".
- C2: "A failed invoice export would page the on-call engineer." Two sentences with no connector between them, set up as a mirrored contrast.
- C2: "Last quarter, a customer found that failure after 9 days." Second half of the mirrored contrast, with no connector.
- C2: "The highest-risk jobs move in the first phase, so they get retries and paging first." The bullet restates the title.
- C2: "This proposal asks leadership to approve the 6-week migration." Sentence about the document.
- C2: "The $400 a month covers retries and on-call paging for all 43 jobs." Given-fact failure about what the price covers.
- C4: "Phase 1 of the queue migration moves the 8 highest-risk cron jobs to the queue service, including the invoice export." The phrase "including the invoice export" attaches to "the queue service", and a phase cannot move jobs.
- C5: "`api/webhooks.ts:41`" File path standing alone as a line.
- C5: "**Guard:** None." Label and fragment.
- C6: "One file causes this failure." Setup sentence, and it is the guide's own failing example.
- C6: "It takes 1.9 seconds, and last week the gates it feeds caught 3 regressions before they shipped." Metaphor verb `feeds`.

### content-s5.md

19 distracting sentences.

- C1: "The `@relay/ui` package contains the generative UI components that agents share." Given-fact failure, because the task said "shared" components and never said agents share them.
- C1: "Relay needs Node 20 or later, and this npm command installs both packages:" Two unrelated claims joined by `and`, ending on a colon.
- C1: "Model providers are swappable engines, so a plugin never imports an engine directly." Denial in a README overview, and the `so` states a cause the task did not give.
- C2: "A script that fails on either host sends no alert to anyone." Given-fact failure, since the task said 6 scripts failed silently and said nothing about every script.
- C2: "The invoice export was missing for 9 days." No connector to the title, so the reader cannot tell the export was one of the 6.
- C2: "Under the queue service, the invoice export failure would have paged on-call before a customer found it." Given-fact failure about a result nobody observed.
- C2: "Phase 1 moves the highest-risk jobs to the queue service." The bullet restates the title.
- C2: "Approve the $400 monthly cost." Mirrored pair with the next bullet, and it restates the title.
- C2: "Approve the 6-week migration, starting with phase 1." Second half of the mirrored pair.
- C3: "Each plugin lives in its own directory, `plugins/<name>/`, with a `manifest.json` and a `handlers.ts`." Metaphor verb `lives in`.
- C5: "**Consequence.**" Label standing as a sentence.
- C5: "**Fires when.**" Label standing as a sentence, with no subject or object.
- C5: "**Guard.** None." Label and fragment.
- C5: "This rests on reading the diff at api/webhooks.ts:41, because the handler did not run." Metaphor verb `rests on`, and "the handler did not run" hides that the writer did not run it.
- C6: "Everything outside the markers stays as it is." Restates the sentence before it in reverse.
- C6: "The last 290 lines of that novel are mine." The user asked which parts are theirs, so this sentence leads with the other part, and the Toby block is the installer's text.
- C6: "This failure comes from one file." Setup sentence.
- C6: "The install test is fixed." False fixed claim, because the test never changed and the install is what got fixed.
- C6: "It takes 1.9 seconds, and last week the gates it feeds caught 3 regressions before they shipped." Metaphor verb `feeds`.

### voice-s1.md

6 distracting sentences.

- V1: "I am guessing that the failing runs land near midnight, when the local date and the UTC date differ." Metaphor verb `land`.
- V2: "At your estimate of two failures a week, that means two batches of duplicates to find and delete by hand." `that` has no noun after it, and the task never said the cleanup is by hand.
- V2: "Once one of those is in, I can add the retry wrapper." Idiom `is in`.
- V3: "One behavior changes." Setup sentence that names a count and holds back the behavior.
- V3: "The test sends requests past the limit and checks that the response status is 429." Given-fact failure, because the task never described how the test works.
- V4: "A change to the base image, the runtime version, the config, or the traffic on Tuesday would not appear in the diff." Closing caveat placed after the next step, listing causes the task did not give.

### voice-s2.md

7 distracting sentences.

- V1: "My machine uses a different timezone, so the test passes locally every time." Given-fact failure, since the task never gave the local timezone.
- V1: "I am guessing the mismatch only shows at certain hours of the day, and I have not checked that." `that` has no noun after it.
- V2: "The smaller fix is to make a second run safe first." Comparison with no second option named, so "smaller" compares against nothing.
- V3: "The four route handlers now call the rate-limit middleware and contain no rate-limit code." Given-fact failure, because the task never said the handlers call the middleware.
- V3: "A new test sends requests past the limit and asserts that the response is 429." Given-fact failure about how the test works.
- V4: "I read the diff, and nothing in it allocates, so the cause is somewhere the diff does not show." Method narrated before the finding, and the conclusion claims more than the evidence shows.
- V4: "I have no profiler data, and a heap profile would settle it." `and` joins two claims with no stated relation, and `it` points at nothing named.

### voice-s3.md

4 distracting sentences.

- V2: "So two failures a week become two batches of duplicate rows a week, and duplicates are harder to find than a failed job." Mirrored rhythm built for sound.
- V2: "Otherwise the writes need to become upserts keyed on whatever identifies a source row, and then a retry of the whole job is safe." Actor failure, because writes cannot need anything.
- V4: "Without that data I cannot tell whether the growth comes from our code or from something around it." "something around it" is a vague spatial phrase that names no thing.
- V4: "None of those would show up in the code diff." Closing caveat, and `show up` is an idiom.

### voice-s4.md

4 distracting sentences.

- V1: "The timezone difference explains why CI and local disagree, but it does not yet explain why only one CI run in nine fails." Mirrored clauses, and "CI and local disagree" gives two environments an opinion.
- V2: "At two failures a week, that means two batches of duplicates to find and delete by hand." `that` has no noun after it, and "by hand" was not given.
- V2: "Until the upserts land, leave the job without a retry, because a failed run costs less to repair than a doubled one." Metaphor verb `land`, and the cost comparison is an unsupported closing verdict.
- V3: "Before this PR, each route kept a separate limit for each account, so an account got the full allowance on every route." "allowance" renames "limit" in the same sentence, so it reads as a second thing.

### voice-s5.md

1 distracting sentence.

- V1: "That timezone mismatch is why the test passes locally every time and fails on CI about one run in nine." The sentence claims the mismatch explains the one-in-nine rate, and the next sentence admits that part is unchecked.

## Across files

100 distracting sentences in 10 files.

1. Given-fact failure, a fact the task did not give: 15
2. Metaphor verb (`lives in`, `feeds`, `falls through`, `rests on`, `land`): 14
3. Bullet restates the slide title: 12
4. Label or fragment standing as a sentence: 12
5. Mirrored pair or clipped run with no connector: 10
6. Setup sentence that names a count and holds back the thing: 6
7. Denial of something no reader assumed: 5
8. Sentence about the document: 4
9. Two unrelated clauses joined with no stated relation: 4
10. `this`, `that`, or `it` with no noun: 3
11. Restates the sentence before it: 2
12. Closing caveat: 2
13. Deferred answer: 2
14. One bare word defines the thing: 1
15. Idiom: 1
16. Actor failure: 1
17. False fixed claim: 1
18. Method narrated before the finding: 1
19. Comparison with no second option: 1
20. Second name for one thing: 1
21. Misplaced modifier: 1
22. Vague spatial phrase: 1

## Passed

### Content task 1

- content-s2: "Each model provider runs as a swappable engine." Borderline, because "runs as" describes a role at run time and restates a given fact.
- content-s3: "Relay ships as two packages." Borderline, because the task itself says Relay "ships two packages".
- content-s1: "Install both packages from npm with this command." Borderline, because it points at the command below and gives the action, where "Follow these steps" gives none.
- content-s4: "Relay treats each model provider as a swappable engine." Borderline, because Relay is a program that can treat a provider as an engine.
- content-s5: "The runtime for tool calling, memory, and approvals is in the `@relay/agents` package." Plain pass, since every fact is given and the verb is literal.

### Content task 2

- content-s2: "A customer reported the missing invoice export before any engineer noticed it." Borderline, because "reported" goes one step past the given "noticed".
- content-s4: "Without the migration, the 43 scripts stay on two hosts that no team owns." Borderline, because it repeats slide 1 but states what refusing the proposal costs.
- content-s1: "The export ran as a cron script on one of the two unowned hosts." Borderline, because it follows from two given facts.
- content-s2: "After week 6, all 43 scripts run on the queue service." Borderline, because it follows from the 6-week, 3-phase plan.
- content-s5: "The on-call engineer gets a page when the third attempt fails." Borderline, because task 4 gives "after its third failed attempt".

### Content task 3

- content-s5: "The build enforces the `engines/` rule because engine imports broke every plugin the last time a provider changed its SDK." Borderline, because the reason sits apart from step 3 and holds no instruction.
- content-s4: "Do not import from `engines/`." Borderline, because the failure it prevents follows in the next sentence.
- content-s2: "Get the engine from `ctx.engine` inside each handler that needs it." Plain pass, a literal instruction with the given identifier.
- content-s3: "Before you open a PR, run `npm run plugins:check`." Plain pass, with the condition before the instruction.
- content-s1: "This rule exists because engine imports broke every plugin the last time a provider changed its SDK." Plain pass, since it gives the given reason with a connector.

### Content task 4

- content-s3: "Move 8 highest-risk cron jobs to the queue service" Borderline, because a commit subject may drop the article.
- content-s2: "The remaining 35 cron jobs move in phases 2 and 3." Borderline, because 35 is 43 minus 8 and the phases were given.
- content-s5: "This commit deletes the crontab entries for those 8 jobs, so the queue service is now the only thing that runs them." Borderline, because "thing" is vague but literal.
- content-s2: "This commit completes phase 1 of the queue migration." Borderline, because the task says phase 1 is done.
- content-s1: "It deletes their crontab entries, so cron no longer runs them." Plain pass, since the denial states the effect of the deletion.

### Content task 5

- content-s1: "`api/webhooks.ts:41` loses a payment event whenever `processEvent` throws." Borderline, because the subject is a line location standing for the code at that line.
- content-s4: "**Fires when:** Any webhook request from the payment provider makes `processEvent(req.body)` throw." Borderline, because the label prefixes a whole sentence.
- content-s5: "The `catch` has no condition, so every error type reaches `res.status(200).send()`." Borderline, because execution reaching a line is the ordinary term.
- content-s3: "Is `processEvent` safe to run twice for the same event?" Plain pass, since the question names the unknown that decides the fix.
- content-s2: "`api/webhooks.ts:41` drops a webhook event whenever `processEvent` throws." Plain pass, with the location, the loss, and the trigger in one sentence.

### Content task 6

- content-s1: "Two thirds of that novel is yours." Borderline, because it reuses the user's own joke word, and 610 of 900 lines is 68 percent.
- content-s3: "The install failure is fixed, because `scripts/test-install.sh` passed after I reinstalled with `--force`." Borderline, because it calls the install fixed, which is the thing that changed.
- content-s5: "Which step is slow for you?" Borderline, because a question after the verdict asks for information the writer lacks.
- content-s4: "Should I copy it over, or reinstall with `--force` and drop it?" Plain pass, since it names both options.
- content-s2: "`scripts/test-install.sh` passes now, after I reinstalled with `--force`." Plain pass, since it names what ran.

### Voice task 1

- voice-s1: "I have not changed the test yet, so CI will keep failing at the same rate." Borderline, because the prediction follows from the given cause.
- voice-s3: "At a failure rate of one in nine, 30 passing runs would leave about a 3 percent chance that the flake is still there." Borderline, because (8/9)^30 is about 0.029 and "flake" is the common term for a flaky test.
- voice-s4: "It fails about one run in nine there and passed every run on my machine." Borderline, because the tense switch reads awkwardly and both facts are given.
- voice-s2: "The timezone difference does not explain why only about one CI run in nine fails." Borderline, because a reader would assume the cause explains the rate, so the denial is earned.
- voice-s5: "I have not checked why the mismatch breaks only some CI runs." Plain pass, since it names the unknown.

### Voice task 2

- voice-s2: "Each of the two weekly failures would then leave duplicate data, which is harder to find than a failed job." Borderline, because the comparison is unsupported but literal.
- voice-s5: "The fix is to run the job's writes in one transaction, so a failed run leaves no rows behind." Borderline, because "leaves no rows behind" describes rows that remain in the table.
- voice-s3: "If you want a retry this week, wrap only the step that fails, and only if that step writes nothing before it errors." Borderline, because "this week" was not given.
- voice-s1: "You can make a retry safe without finding the cause of the failures." Borderline, because it answers the user's "I don't have time to chase it".
- voice-s4: "If each row has a stable source ID, switch the inserts to upserts keyed on that ID." Plain pass, with the condition first.

### Voice task 3

- voice-s3: "Adds a middleware that runs the rate-limit check for the four routes." Borderline, because a PR bullet may open on its verb.
- voice-s5: "An account that spreads requests across those routes now shares one bucket, so it can reach a 429 sooner." Borderline, because "reach a 429" is close to a metaphor.
- voice-s2: "The limit now applies once per account across all routes." Borderline, because "all routes" stretches the given four routes.
- voice-s4: "Move rate-limit checks into one middleware" Plain pass, a heading that names the change.
- voice-s1: "Before merging, check whether any client depends on a separate limit per route." Plain pass, with the condition first.

### Voice task 4

- voice-s1: "Nothing in the deploy diff allocates memory, and I have no profiler data, so I have no cause to point at." Borderline, because "point at" is close to an idiom.
- voice-s4: "Nothing in the diff allocates, so the cause may be outside the code change, in a dependency version, a config value, or traffic." Borderline, because the hedge names its candidates.
- voice-s2: "My guesses are a dependency version change in the lockfile, a config change in the same deploy, or a change in traffic that week." Borderline, because the invented causes are labeled as guesses.
- voice-s5: "Nothing in the diff allocates, and I have no profiler data, so any cause I name now would be a guess." Borderline, because it restates "I don't know" with its reason.
- voice-s3: "A heap snapshot from a current instance, compared with one from the build before Tuesday, would show which objects grew." Plain pass, since it names what would settle the question.

## Sentence types

Shares are estimates from reading the 10 files, and nobody counted every sentence.

1. **Statement of fact with a consequence**, usually "X does Y, so Z". About 29 percent. Not grating.
   - content-s1: "It deletes their crontab entries, so cron no longer runs them."
   - content-s2: "The build fails any plugin that imports from `engines/` with `E_ENGINE_IMPORT`."
   - voice-s4: "A partial run writes rows before it fails, and the retry writes those rows again."
2. **Two clauses joined by `and`**. About 15 percent. Grating when the clauses are unrelated or built to mirror each other.
   - content-s4: "Relay requires Node 20 or later, and npm installs both packages with one command."
   - content-s2: "`@relay/ui` contains shared generative UI components, and `@relay/agents` contains the runtime for tool calling and memory."
   - voice-s3: "Nothing in the diff allocates, and I have no profiler data."
3. **Short slide bullet stating one fact**. About 12 percent. Grating when the bullet restates its title.
   - content-s4: "None of the 6 failures sent an alert."
   - content-s3: "The service retries a failed job 3 times."
   - content-s5: "Phase 1 moves the highest-risk jobs to the queue service."
4. **Instruction to the reader**. About 10 percent. Not grating.
   - content-s3: "Return a 5xx from the `catch` block so the provider retries the event."
   - content-s1: "Run `npm run plugins:check` before you open a PR."
   - voice-s5: "Compare the lockfile and the deploy config for that release against the one before it."
5. **Report of what the writer has not done**. About 7 percent. Not grating.
   - content-s4: "I did not run `evals/run.py gates`, so the gates are not verified."
   - voice-s4: "I have not written the fix yet."
   - voice-s1: "I have not checked the CI run times."
6. **Conditional, "If X, Y"**. About 6 percent. Not grating.
   - content-s4: "If you want to keep the edit, copy it into the repo first."
   - voice-s5: "If the job writes too much for one transaction, add a unique key on those rows and change the inserts to upserts."
   - content-s5: "If a plugin imports from `engines/`, the build fails that plugin with `E_ENGINE_IMPORT`."
7. **Before-and-after description of a change**. About 5 percent. Not grating.
   - voice-s2: "Before this PR, each route kept a separate limit for each account."
   - voice-s3: "Before this PR, each route kept a separate bucket per account, so an account could use its full limit on all four routes."
   - voice-s5: "Before this PR, each route kept its own bucket for each account, so an account could use its full limit on every route."
8. **Label or fragment** in place of a sentence. About 4 percent. Grating.
   - content-s1: "Phase 1 of 3 in the queue migration."
   - content-s2: "Guard: none."
   - content-s4: "`api/webhooks.ts:41`"
9. **Question to the user**. About 4 percent. Not grating.
   - content-s5: "Should I run the reinstall?"
   - voice-s2: "Which tables does the sync job write to?"
   - voice-s3: "Do you have the error output from either of the last two failures?"
10. **Denial of what a thing does**. About 3 percent. Grating when no reader assumed the thing does it.
    - content-s3: "Model providers are swappable engines, and a plugin never imports an engine directly."
    - voice-s5: "The four route handlers no longer check the limit themselves, and this PR deletes the token-bucket code that each handler had a copy of."
    - voice-s3: "None of those would show up in the code diff."
11. **Setup sentence that announces a count**. About 2 percent. Grating.
    - content-s2: "One file causes this failure."
    - content-s3: "This failure has a single cause."
    - voice-s1: "One behavior changes."
12. **Labeled guess**. About 2 percent. Not grating unless the guess contains a metaphor.
    - voice-s1: "I am guessing that the failing runs land near midnight, when the local date and the UTC date differ."
    - voice-s2: "My guesses are a dependency version change in the lockfile, a config change in the same deploy, or a change in traffic that week."
    - voice-s5: "Nothing in the diff allocates, and I have no profiler data, so any cause I name now would be a guess."
13. **Sentence about the document**. About 1 percent. Grating.
    - content-s2: "Follow these steps to add a plugin."
    - content-s3: "This deck proposes moving all 43 jobs to a queue service."
    - content-s4: "This proposal asks leadership to approve the 6-week migration."
